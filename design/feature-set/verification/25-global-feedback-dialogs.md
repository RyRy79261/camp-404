# Verification — 25 global-feedback-dialogs

**Verdict:** accurate  ·  checked 78 claims, verified 76.
The doc is exceptionally reliable: file:line citations, enum members, token strings, regex patterns, schema bounds, copy strings, and the real-vs-test backend split all check out against source. The only defects are two cosmetic wording nits (an embellished comment paraphrase and a minor index-ordering presentation); nothing that would mislead a rebuild.

## Inaccuracies
| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | "Comment notes a previously-removed floating button — shake is now the ONLY trigger (`feedback-gate.tsx:13-15`)." | The JSDoc at those lines says "Shake is the only trigger (matching RyRy79261/intake-tracker's ShakeToReport)." It does NOT mention any previously-removed floating button. The "removal" framing comes from git history, not the comment. The functional claim (shake-only, no floating button) is correct — grep confirms no button-based open path exists. | apps/web/app/feedback-gate.tsx:12-21 |
| low | Indexes "used: `notification_deliveries_user_ack_idx` … , `notification_deliveries_user_read_idx`, `notification_deliveries_broadcast_idx` (`schema.ts:864-877`)." Lists user_ack first. | All three indexes exist and the cited range is right, but the source defines them in the order user_read (`:865`), user_ack (`:871`), broadcast (`:875`). Only `user_ack_idx` matches the gate's exact `(userId, acknowledgedAt)` predicate; the doc correctly identifies that one. Ordering is presentational only. | packages/db/src/schema.ts:864-877 |

## Omissions
| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The transport-level catch in `handleSubmit` returns its own distinct error string "Couldn't send your report just now. Please try again." when the server-action invocation itself rejects (network/runtime), separate from the action's typed `{ok:false}` errors. The doc covers the action's error strings exhaustively but does not call out this client-side fallback string. | apps/web/components/feedback/report-bug-dialog.tsx:99-104 |
| low | `notification_deliveries` also carries a `broadcastUserUniq` unique index ("one delivery per (broadcast, user)") not mentioned in the data-model section; not load-bearing for this unit (used by the fan-out worker). | packages/db/src/schema.ts:881-882 |

## Spot-confirmed
- Mount points: `AcknowledgementGate` at layout.tsx:52, `FeedbackGate aiAvailable={!!process.env.ANTHROPIC_API_KEY}` at layout.tsx:55, siblings inside `<Providers>` (layout.tsx:50-56). Comment at :53-54 matches the doc's "server-only env check" claim.
- AcknowledgementGate: `POLL_INTERVAL_MS = 45_000` (acknowledgement-gate.tsx:26); `fetch("/api/notifications/pending", { cache: "no-store" })` (:40-42); interval + visibilitychange→visible + window focus refetch (:56-61); `requestIdRef` monotonic de-race at :35/:38/:45; `acknowledgeDelivery` bumps `requestIdRef.current++` (:95), filters queue (:97), `router.refresh()` (:99), non-ok `return` (:93); body-scroll-lock restores `previous` value (:75/:79) and resets scroll to top (:77); `if (!current) return null` (:83); header Megaphone + "Camp announcement" uppercase (:117-120); title `current.title` (:124); meta `From {senderName} · ` omitted when null + `new Date(...).toLocaleString()` (:126-129); body `whitespace-pre-wrap` (:131-133); acknowledge button at end-of-scroll (:135-153) with `{queue.length - 1} more after this.` when `queue.length > 1` (:138-142); `role="dialog" aria-modal="true" aria-labelledby="ack-title"`, `z-[100]`, `max-w-2xl` (:106-114); Loader2 spinner + `disabled={acking}` (:148-152).
- FeedbackGate: `signedIn = !isPending && !!session` (:23-24); `useShakeGesture({ enabled: signedIn && !open, onShake: () => setOpen(true) })` (:27); `if (!signedIn) return null` (:41); iOS one-time `pointerdown` → `requestMotionPermission()` gated on `signedIn && motionPermissionNeeded()` (:32-39); renders `<ReportBugDialog open onOpenChange aiAvailable />` (:43-49); defence-in-depth comment at :17-20.
- ReportBugDialog: DB-free / GitHub-issue comment (:39-44); kind toggle two buttons Bug(`Bug`)/Feature(`Lightbulb`), `variant default/outline`, `aria-pressed`, `role="group" aria-label="Report type"` (:162-189); title copy "Report a bug"/"Request a feature" (:151-153); label "What went wrong?"/"What would you like to see?" (:194-196); placeholders verbatim (:204-208); textarea `rows={6}` `maxLength={DESCRIPTION_MAX}` (:198-209); "Dictate instead" → `RecorderPanel` with no promptKey (:213-232), comment at :214-216; `appendTranscript` trims/ignores-empty/sets dictated/newline-joins/clamps to DESCRIPTION_MAX (:75-83); AI checkbox rendered only when `aiAvailable` (:235), `useAi` init `true` (:56), copy "Improve with AI" / "Restructures your report into a clear title and steps before filing." (:246-250); submit passes `useAi: aiAvailable && useAi`, `route: window.location.pathname` in a transition (:85-106); success view `CheckCircle2`+"Report filed", `Issue #{number} was created on GitHub. Thanks!` vs "Thanks — your report was sent." (:122-133), link `View issue #{number}` vs "Open the tracker" (:135-143), "Done" closes (:144-146); form reset on closed→open (:64-73); mid-send dismissal guard `if (!next && isPending) return` (:111-119) and Cancel `disabled={isPending}` (:269); `canSubmit = description.trim().length > 0 && !isPending` (:108).
- use-shake-gesture: `MotionPermissionResult = "granted"|"denied"|"unsupported"` (:14); `motionPermissionNeeded()` true only when `DeviceMotionEvent.requestPermission` is a function (:21-32); `requestMotionPermission()` returns "unsupported" off-window, "granted" when absent, catches reject → "denied" (:34-52); `createShakeDetector` magnitude `√(x²+y²+z²)` (:76-80), `delta > threshold` records jolt (:96-97), prune `now - t <= windowMs` (:101), fire when `jolts.length >= requiredJolts && now - lastShakeAt >= cooldownMs`, stamps + clears + returns true (:102-111); `useShakeGesture` defaults `threshold=8, requiredJolts=5, windowMs=800, cooldownMs=3000` (:134-139), `SAMPLE_THROTTLE_MS = 60` (:153), reads `accelerationIncludingGravity`, bails on null axis (:157-158), throttles (:161-162), `onShakeRef` (:141-142).
- submitFeedbackAction: `"use server"` (:1); auth gate "Please sign in to send feedback." (:50-51); burst `feedback:${user.id}` `{ limit: 3 }` → "You're sending these quickly — give it a minute and try again." (:56-62); daily `feedback-day:${user.id}` `{ limit: 20, windowMs: 86_400_000 }` → "You've filed a lot of reports today — please try again tomorrow." (:63-72); `InputSchema.safeParse` → first issue or "Invalid input." (:74-80); `sanitizeReportText(description, DESCRIPTION_MAX)` empty → "Please describe the issue." (:86-89); `campUser?.id || "unlinked"` reporterRef (:94-95); E2E bypass `{ ok:true, number:0, url:"https://github.com/RyRy79261/camp-404/issues" }` (:98-100); `useAi ? structureWithAi(kind, sanitized) : null` (:103); `buildFeedbackIssue({ kind, description: sanitized, dictated: dictated ?? false, reporterRef, route, structured })` (:105-112); missing token → "Feedback isn't set up yet. Let a camp captain know." (:114-121); repo split !== 2 → "Feedback isn't configured correctly. Let a camp captain know." (:123-132); POST `https://api.github.com/repos/${owner}/${name}/issues`, `Authorization: Bearer`, `Accept: application/vnd.github+json`, `X-GitHub-Api-Version: 2022-11-28`, `User-Agent: camp-404-feedback`, body `{title,body,labels}`, `AbortSignal.timeout(8000)` (:134-155); 201 → `GithubIssueSchema` `{number, html_url: url}`, validate-fail → "Your report was filed, but we couldn't read GitHub's reply.", success → `{ok:true, number, url:html_url}` (:157-169); 401/403|404/410/other and thrown-error strings all verbatim (:175-188); `FeedbackResult` union (:15-17); `DEFAULT_REPO = "RyRy79261/camp-404"` (:32); `route: z.string().max(300).optional()` (:27); `description .trim().min(1,"Please describe the issue.").max(DESCRIPTION_MAX)` (:21-25).
- feedback-ai: `model: MODELS.haiku`, `max_tokens: 1024`, `temperature: 0`, `SYSTEM_PROMPT`, single `format_report` tool, `tool_choice: { type:"tool", name }`, `{ timeout: 30_000 }` (:76-92); extracts tool_use block or null (:94-97); `StructuredSchema.safeParse` → data or null (:99-100); catch → null (:101-104); `StructuredSchema` bounds title 1-140, summary 1-2000, steps ≤500 max 20, expected/actual ≤1000, severity enum (:14-21); `FORMAT_TOOL` required `["title","summary"]` (:52); SYSTEM_PROMPT rules verbatim (:56-65); `"server-only"` import (:1).
- anthropic: `MODELS.haiku = "claude-haiku-4-5-20251001"`, `MODELS.opus = "claude-opus-4-8"` (:18-21).
- github-feedback: pure/no-I/O + public-repo PII comment (:1-7); `FeedbackKind = "bug"|"feature"` (:9); `StructuredReport.severity` enum (:18); `DESCRIPTION_MAX = 5000` (:21), `TITLE_MAX = 100` private (:22), `ISSUE_BODY_MAX = 60_000` with "GitHub's hard limit is 65536" comment (:23); `labelsFor` bug→`["bug","from-app"]`, feature→`["enhancement","from-app"]` + auto-create comment (:25-32); `redactPii` ordered patterns Bearer/JWT/sk-pk(16+)/gh[posu](20+)/AKIA(16)/xox[baprs](10+)/url-params(token|key|secret|sig|signature|password|access_token|code|auth)/base64(40+)/t.me|wa.me/@handle(2+)/email/intl-phone(+ consume all groups)/local-phone/13-digit-id/SSN-id/16-digit-card (:39-76); `sanitizeReportText` redact→strip `<[^>]*>`→trim→slice, empty→"" (:79-85); `fenced` defuses ``` → `''' ` (:88-90); `inlineCode` strips backticks + collapses newlines (:98-100); `mdInline` defuses fences + collapses newlines (:109-111); `fallbackTitle` "Bug report"/"Feature request" (:133-135); plain title = first line sliced to TITLE_MAX or fallback (:138-143); structured re-clamps summary 2000/steps 500/expected-actual 1000/title 100 (:151-169); footer `[..., reporter:`id`, from:`route`].join(" · ")` with "(voice-dictated)" (:183-189); body `[...sections, "---", footer].join("\n\n").slice(0, ISSUE_BODY_MAX)` (:195-197).
- error.tsx: default export `Error({error, reset})` (:11-17); `console.error(error)` + `headingRef.current?.focus()` with `tabIndex={-1}` (:19-26/:31-35); heading "Something went sideways.", body "An unexpected error tripped us up. Try again — if it keeps happening, let a camp captain know." (:36-41); "Try again" `onClick={reset}` + outline "Back to camp" `<Link href="/">` (:43-48); `max-w-lg` shell (:29).
- Data model: `notification_deliveries` table all columns as documented — id uuid PK (:833), broadcastId→broadcasts.id cascade (:834-836), userId→users.id cascade notNull (:837-839), title/body notNull (:841-842), channel notNull (:843), presentation notNull default "feed" (:846-848), pushStatus (:849), refType/refId (:853-854), readAt nullable (:856), acknowledgedAt nullable (:860), createdAt notNull defaultNow (:862); `getPendingAcknowledgements` selects displayName as senderName (:412), left joins (:416-420), `orderBy createdAt` (:428), filters userId + presentation="acknowledge" + isNull acknowledgedAt (:421-427); `acknowledgeDelivery` sets `{acknowledgedAt: now, readAt: now}` owner+presentation+isNull scoped, returns rows.length>0 (:437-458); `PendingAcknowledgement` interface (:390-396); `broadcast_presentation` pgEnum `["acknowledge","popup","feed"]` (:166-170); `AnnouncementPresentation` derived from enumValues (broadcasts.ts:29-30).
- Routes: pending GET unauthenticated → `{pending:[]}` not 401 (route.ts:16-18), no-camp-access → `{pending:[]}` via `hasCampAccess` (:23-25); acknowledge POST `Body = z.object({ deliveryId: z.string().uuid() })` 400 on invalid (:13/:21-24), 401 unauthenticated (:17-19), no-camp-access → `{ok:false}` (:28-30), scoped to `campUser.id` (:31-34).
- RecorderPanel states: requesting "Allow microphone…", processing "Transcribing…", recording "Recording", error "Tap to retry", idle "Tap to record" (:59-68); Mic/Square/Loader2 + Waveform + mm:ss timer (:91-114); close/record disabled while recording/busy (:85/:96); invoked without promptKey from the dialog.
- Backend split: `lib/notifications.ts` routes real-vs-test via `isE2ETestMode()` (notifications.ts:117-119); `isE2ETestMode()` = `process.env.E2E_TEST_MODE === "1"` (test-mode.ts:11-13); test-store mirrors `getPendingAcknowledgements`/`acknowledgeDelivery` (test-store.ts:472/:500) — the doc correctly frames the test path as E2E-only, not user-facing.
- Auth shape: `getAuthenticatedUser` returns `{ id, primaryEmail: string | null, ... }` (auth.ts:25-35); `reporterRef` uses `findCampUserByAuthId(user.id)?.id` (users.ts:174-179); `hasCampAccess(user, email)` = god-email || inviteCode (users.ts:219-224).

## Low-confidence / could-not-verify
- Whether `MODELS.haiku` string `"claude-haiku-4-5-20251001"` is a currently-valid Anthropic model id is an upstream/API fact, not verifiable from source (the string literal itself is confirmed in anthropic.ts:20).
- The doc's "previously-removed floating button" framing is a git-history claim; the current source has no floating button (verified by absence), but whether one ever existed is outside the working tree.
- Runtime behavior of `AbortSignal.timeout` / GitHub status mapping is verified at the code level only; not exercised against the live GitHub API.
