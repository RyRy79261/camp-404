# Verification — 20 question-field-kinds
**Verdict:** accurate  ·  checked 78 claims, verified 75.
The doc is a faithful, digit-exact map of the questionnaire field renderer, the Zod schema, the live catalogue, and the PII split. All 10 kinds, every enum/option value, every maxLength, every validator string, and the data-model attribution check out against real production code. The only defects are one off-by-one country count and a "Files covered" path/line ambiguity (two files both named `questionnaire.ts`); both are cosmetic.

## Inaccuracies
| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | "199 entries in `apps/web/lib/countries.ts`" | The `COUNTRIES` array holds **198** country entries (ZA pinned at line 31, last at line 228 `ZW`). A naive `grep -c "value:"` returns 199 only because it also counts the `value: string;` type field on the `Country` interface (line 8). | apps/web/lib/countries.ts:31-228 |
| low | "Files covered: `packages/types/src/questionnaire.ts`" but the catalogue/literal-value citations (e.g. "questionnaire.ts:60", "TEAMS 30-39") silently refer to a *different* file, `apps/web/lib/questionnaire.ts`. The doc never disambiguates the two same-named files when citing bare "questionnaire.ts:NN". | The schema/helpers live in `packages/types/src/questionnaire.ts`; the catalogue literals live in `apps/web/lib/questionnaire.ts`. Both are in "Files covered" so the values are correct, but a rebuilder following "questionnaire.ts:60" against the types file finds `helper: z.string().optional()` for SliderQuestion, not `version`. | packages/types/src/questionnaire.ts:1 vs apps/web/lib/questionnaire.ts:60 |
| low | Several catalogue maxLength line cites are one line high: bio.statement "2000 (145)", ideas "2000 (163)", history.other_burns "1000 (323)", dietary.notes "1000 (381)". | Actual maxLength lines: bio.statement=144, ideas=162, history.other_burns=322, dietary.notes=382. The cited line is the adjacent `required:` line. All four **values** (2000/2000/1000/1000) are correct. | apps/web/lib/questionnaire.ts:144,162,322,382 |

## Omissions
| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The in-code JSDoc for the `fullScreen` prop on `QuestionField` lists only "single scale, single long_text" (omits `image`), whereas the doc text and the actual `image`-kind container both treat image as full-screen-aware. Not wrong in the doc; noting the code comment lags the doc's (correct) inclusion of image. | apps/web/components/questionnaire/question.tsx:35-41 |
| low | `splitIdNumber` (the PII write-boundary the doc attributes to the "server action") is shared by three callers, not just onboarding: the onboarding action, `lib/forms.ts` (replay flow), and `lib/mcp/tools/profile.ts`. Doc says "enforced at the write boundary (server action)" — accurate but understates the reuse. | packages/db/src/id-documents.ts:19-25; apps/web/lib/forms.ts:73; apps/web/lib/mcp/tools/profile.ts:72 |

## Spot-confirmed
- Exactly **10 kinds** in `z.discriminatedUnion("kind", […])`: slider, single_select, multi_select, short_text, long_text, date, scale, toggle, combobox, image — questionnaire.ts:137-148.
- `QuestionField`: `fieldId = \`q-${question.id}\`` (question.tsx:50); `<Label htmlFor>` wraps `question.prompt` (60-61); required marker `"required" in question && question.required` → `<span className="ml-1 text-[color:var(--color-primary)]">*</span>` (62-64); helper `<p className="text-xs text-[color:var(--color-muted-foreground)]">` (66-70); error `<p className="text-xs text-destructive" role="alert">` (78-82); fullScreen flips `flex flex-1 flex-col gap-2` vs `flex flex-col gap-2` (53-58).
- slider arm: `current = typeof value === "number" ? value : question.min`; `onChange(v[0] ?? current)`; 3-up labels `minLabel ?? min`, live `current` (`aria-live="polite"`, `font-medium`), `maxLabel ?? max` — question.tsx:101-127.
- single_select placeholder `"Choose one…"` (question.tsx:135); multi_select `new Set`, add on `checked === true` else delete, `onChange(Array.from(next))`, `checkboxId = \`${id}-${o.value}\`` (146-174); short_text `<Input maxLength=…>` value `""` fallback (175-183); date `<Input type="date">` (194-202); combobox placeholder `?? "Select…"` / search `?? "Search…"` (221-231); image centered `flex flex-1 flex-col items-center justify-center py-4`, value `null` fallback (232-240).
- Switch has no `default` arm; falls through to implicit `undefined` for an unhandled kind — question.tsx:100-241.
- Pure helpers verbatim: `flattenQuestions` skips intro pages (224-230); `displayResponseValue` `EMPTY_DISPLAY` for undefined/null/"" then single_select|toggle|combobox→option label, scale→step label, multi_select→empty-`"—"`/joined `", "`, default arrays join else `String(value)` (239-266); `diffResponses` catalogue-order, `{fieldId, label: q.prompt, from, to}` (298-316); `sameValue` empty↔empty equal, arrays as sorted sets, else `===` (277-289); `isEmptyValue` undefined/null/""/[] (268-275). `EMPTY_DISPLAY = "—"` (232).
- `validateOne` strings all exact: "This question is required", slider "Expected a number"/`Must be between ${q.min} and ${q.max}`, single_select/toggle/combobox "Expected a choice"/"Not a valid option", multi_select "Expected a list of choices"/"Pick at least one option" (filters unknowns), short/long_text "Expected text"/`Max ${q.maxLength} characters`, date "Expected a date"/"Use yyyy-mm-dd"/`/^\d{4}-\d{2}-\d{2}$/`/`Date.parse`/"Not a real date", scale "Pick a level"/"Not a valid level", image "Expected an image URL" with NO format check — questionnaire.ts:354-435.
- `validateResponses` `safeParse` → `_root: "Malformed response payload"`, skips intro, keys errors by `q.id`, only sets `responses[q.id]` when value !== undefined, drops unknown keys — questionnaire.ts:324-352.
- Per-kind schema defaults all exact: slider `step.positive().default(1)`, `required.default(true)`; single_select/scale/toggle/combobox `options/steps.min(2)`, `required.default(true)`; multi_select `required.default(false)`; short_text `maxLength.int().positive().default(120)` required true; long_text `default(1000)` required false; date required true; image required false — questionnaire.ts:7-135.
- Page schemas: QuestionsPage `questions.min(1)` (153-160), IntroPage heading/body (165-171), Questionnaire `version.min(1)`/`pages.min(1)` (179-183).
- Response union `number | string | string[] | boolean | null` with `boolean` present-but-emitted-by-no-kind (187-193); `QuestionnaireResponses = z.record(z.string(), …)` (198-201).
- Catalogue: `version: "2026.05.29-v8"` (60); TEAMS 8 values 30-39 (kitchen…ministry_of_vibes, labels exact incl. "Power & Lighting", "Ministry of Memes"); DIETARY_INGREDIENTS 12 values 44-57 (dairy…spicy, labels exact incl. "Nightshades (tomato, pepper, aubergine)", "Chilli / heat"); COUNTRY_OPTIONS `${countryFlag(c.value)} ${c.label}` 6-9; team_interest slider `min:0,max:5,step:1,minLabel:"Not for me",maxLabel:"Sign me up",required:false` 179-189; cooking steps create/teach/follow/burn 201-206; hardware steps design/build/assist/novice 222-227; intent 6 steps definite…not_coming 338-345; id.type passport/sa_id 113-118; logistics.driving yes/no/maybe 251-255; onsite_before/after 262-277; camp404_years 2019/2022-2026 294-301; afrikaburn_count 0/1_2/3_5/6_plus 308-313; phone/id.number maxLength=40 (97/126).
- `id.number` PII split fully confirmed: `validateOne` only length-checks it as short_text; `validateIdNumber` is imported ONLY by `wizard.tsx` (client) and `id-validation.ts` itself — never by `validateOne`. Server action calls `splitIdNumber` removing `ID_NUMBER_KEY` while retaining `ID_TYPE_KEY`, then `setIdDocuments` → `idColumnsFor` routes sa_id→`saIdEncrypted`, else→`passportEncrypted` — actions.ts:52-65; id-documents.ts:7-49; wizard.tsx:12,90-96.
- `validateIdNumber`: trim→"Document number is required"; passport `/^[A-Z0-9]{6,12}$/i`→"Letters and digits only — typically 6–12 characters."; sa_id `/^\d{13}$/`→"Must be exactly 13 digits.", YYMMDD month 1-12/day 1-31→"First six digits aren't a valid YYMMDD date.", SA Luhn (odd 1-based summed, even concatenated ×2 digit-summed, check `(10-total%10)%10`)→"Check digit doesn't match — double-check the number."; neither type→"Pick the ID document type first" — id-validation.ts:25-102.
- `burner_profiles` table: `userId` PK FK→users cascade, `version: text notNull`, `responses: jsonb.$type<Record<string,unknown>>().notNull().default({})`, `startedAt`/`completedAt?`/`updatedAt` — schema.ts:352-364 (note: real path `packages/db/src/schema.ts`). `passportEncrypted`/`saIdEncrypted` text columns — schema.ts:241-242.
- Sub-components: ToggleField `role="radiogroup"` + `role="radio" aria-checked` equal-width `flex-1` (249-288); ScaleField mobile `grid h-[70dvh] grid-cols-[1fr_auto_1fr]` empty `aria-hidden` left gutter + vertical Slider + `<ol aria-hidden>`, desktop `repeat(N,minmax(0,1fr))` reversed labels + horizontal Slider, `sliderValue = steps.length-1-currentIndex`, untouched `Math.floor(steps.length/2)` (297-403); LongTextField `rows={6}` default / `min-h-[40dvh] flex-1 resize-none` fullScreen, `appendTranscript` trim/no-op-if-empty/join `"\n"` unless `/\n\s*$/`/`.slice(0,maxLength)`, `promptKey="questionnaire"` (418-477); Combobox Popover+cmdk, `role="combobox"`, `emptyMessage="Nothing found."`, `Check` on selected, accepts-but-renderer-never-sets `disabled` (41-114); AvatarUpload `h-40 w-40` dashed, `accept="image/*"`, `cropResizeToSquare`, POST multipart `image` to `/api/uploads/avatar`, `uploading` spinner + "Uploading…", remove X→`onChange(null)` (24-147); Slider Radix wrapper horizontal+vertical+multi-thumb, defaults `min=0`/`max=100`, vertical `min-h-44`, thumb `size-4` (8-61).
- RecorderPanel/use-voice-recorder adjacency: states `idle|requesting|recording|processing|error` (use-voice-recorder.ts:5-10); `maxDurationMs` default `120_000` (56); labels "Allow microphone…"/"Transcribing…"/"Recording"/"Tap to retry"/"Tap to record" (recorder-panel.tsx:59-68); mm:ss timer (70-73); POST `/api/voice/transcribe` (198); close X `disabled={isRecording || isBusy}` (85). `promptKey="questionnaire"` passed at question.tsx:461 (doc said 460 — off by one, value correct).
- Wizard touchpoints: `validatePageLocally` intro-passes, required-missing → "This question is required", id.number cross-field calls `validateIdNumber` (79-101); `isFullScreen` = intro OR single-question scale/long_text/image (149-155); `nextLabel` "Skip" when lone optional unanswered else "Next" (160-172); `setResponse` clears the field's error (69-77); reserved keys `_form`/`_root` documented as collision-free vs dotted ids (16-20); fields not disabled mid-`isPending` (only Back/Next) (250,255).

## Low-confidence / could-not-verify
- The doc's narrative that `boolean` in the response union is "vestigial / future-proofing" is an inference; code confirms no kind emits boolean but does not state intent. Reasonable, not code-asserted.
- The empty left gutter in ScaleField's mobile grid being "reserved for a future secondary label set" is taken verbatim from the in-code comment (question.tsx:327-333); whether it is truly future-planned vs dead is a design question, not code-decidable.
- The `Date.parse` leniency note ("2026-02-31 parses; regex only enforces shape") is a correct reading of the code path but the actual runtime parse result is engine-dependent; not exercised by a test I located.
- "Stored value = public image URL (Vercel Blob in prod)" and country flag rendering depend on prod env/blob config not readable from source alone (the route exists at apps/web/app/api/uploads/avatar/route.ts; its blob backend was not opened).
