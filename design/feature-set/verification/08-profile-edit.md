# Verification — 08 profile-edit

**Verdict:** accurate  ·  checked 64 claims, verified 61.
The doc is highly reliable and digit-exact across gating, both server actions, the avatar pipeline, and the full `sanitiseAccount` erasure (field-for-field, including the NOT-NULL reimbursement scrub). The only defects are two cosmetic provenance mistakes in the closing "Sub-components" section (wrong importer of the actions; wrong location of the unit test) plus one prepared-but-unused `className` prop that the doc presents as a usable override. Nothing breaks a rebuild.

## Inaccuracies
| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | "Server actions `updateProfile` + `deleteOwnAccount` live in the shared `app/profile/actions.ts` (**the `/profile` view also imports from here**)" | The only importers of either action are `edit-form.tsx` and `delete-account.tsx`. `app/profile/page.tsx` imports none of them (it imports `next/link`, `@/lib/auth`, `@/lib/users`, `@/lib/initials`, UI cards/button — no `../actions`). | doc:141 vs apps/web/app/profile/page.tsx:1-18; grep confirms no other importers |
| low | "`sanitisedUserPatch` + `lostCatName` are also imported by **unit tests in `packages/db`**" | The only test importing them is `apps/web/lib/__tests__/account.test.ts` (in apps/web). No test file under `packages/db` imports them. | doc:142 vs apps/web/lib/__tests__/account.test.ts:2 |
| low | "`AvatarUpload` … accepts a `className` diameter override (default `h-40 w-40`)" — framed as a usable variant lever | The `className` prop exists (avatar-upload.tsx:14) and is applied (avatar-upload.tsx:86), but **no real consumer passes it**: edit-form.tsx:31 and question.tsx:235 both call `<AvatarUpload value=… onChange=… />` with no `className`. Prepared-but-unused. | avatar-upload.tsx:14,86; edit-form.tsx:31; question.tsx:235 |

## Omissions
| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The button's dashed-vs-solid border toggles on `value && "border-solid"` (solid only when a *saved* URL exists, not on local preview) — a minor visual detail the doc's empty-state description ("dashed border") does not fully capture. | avatar-upload.tsx:84 |
| low | Avatar upload route 401 body is `{ error: "Unauthorized" }` (doc lists the 401 status code but not that this gate is `getAuthenticatedUser` returning null, i.e. any unauthenticated POST). Owned by unit 22, so acceptable. | apps/web/app/api/uploads/avatar/route.ts:25-28 |

## Spot-confirmed
- `export const dynamic = "force-dynamic"` at page.tsx:14; gate sequence auth → ensureCampUser → `!hasCampAccess` redirect `/signup/required` → `!profile?.completedAt` redirect `/onboarding/questionnaire` → `!isApproved` redirect `/pending-approval`, in exactly that order — page.tsx:17-28.
- `getAuthenticatedUserOrRedirect` redirects unauthenticated to `/auth/sign-in` — auth.ts:40-44.
- `ensureCampUser`: god accounts auto-created approved (users.ts:70-80); else synthetic non-persisted row with `id: ""` (users.ts:86-94).
- `hasCampAccess` = `isGodEmail(email) || !!user.inviteCode` (users.ts:219-224); `isApproved` = `isGodEmail || approvalStatus === "approved"` (users.ts:231-236) — rejected is terminal.
- `initialDisplayName = campUser.displayName ?? authUser.primaryEmail ?? ""` — page.tsx:30-31.
- Layout `mx-auto … min-h-[100dvh] … max-w-md … px-4 py-8` (page.tsx:34); header "Edit profile" + "Update your photo and how your name shows up around camp." (page.tsx:36-39); Danger-zone `h2` in `--color-destructive`, label "Danger zone" (page.tsx:52-54).
- ProfileEditForm props `initialDisplayName: string`, `initialImageUrl: string | null` (edit-form.tsx:12-15); `imageUrl` state from `initialImageUrl` (edit-form.tsx:23); hidden input `name="profileImageUrl" value={imageUrl ?? ""}` (edit-form.tsx:32); `useActionState(updateProfile, null)` → `[state, formAction, isPending]` (edit-form.tsx:24-27).
- Display-name `<Input id/name="displayName" defaultValue={initialDisplayName} maxLength={80} required disabled={isPending}>` (edit-form.tsx:36-43); error banner `state && !state.ok` → `role="alert"` in `--color-destructive` (edit-form.tsx:46-50); ghost Cancel `<Link href="/profile">` + submit "Saving…"/"Save changes" (edit-form.tsx:52-59).
- AvatarUpload props `value`, `onChange`, `className?` (avatar-upload.tsx:8-15); file input `type="file" accept="image/*" class="sr-only"` (avatar-upload.tsx:138-144); `handleFile` clears error → `setUploading(true)` → `cropResizeToSquare(file)` → object-URL preview → POST `FormData{ image: File("avatar.webp", blob.type) }` → `!res.ok` throws `data?.error ?? "Upload failed"` → success `onChange(data.url)` → finally clears uploading + resets input (avatar-upload.tsx:39-68).
- `displaySrc = preview ?? value` (avatar-upload.tsx:70); 401-mid-onboarding comment (avatar-upload.tsx:28-30); object-URL revoke `useEffect` (avatar-upload.tsx:32-37); remove `X` button when `displaySrc && !uploading` → clears error/preview, `onChange(null)` (avatar-upload.tsx:107-120); spinner `Loader2` over `bg-black/40` while uploading (avatar-upload.tsx:100-104); empty-state `Camera` + "Add photo" (avatar-upload.tsx:94-99); text-button label cycle "Upload a photo" → "Uploading…" → "Change photo" (avatar-upload.tsx:129); inline error `role="alert"` (avatar-upload.tsx:132-136).
- `updateProfile` re-gates auth + invite only (no onboarding/approval re-check) — actions.ts:28-32; `name = typeof rawName === "string" ? rawName.trim() : ""` (actions.ts:34-35); empty → "Display name can't be empty." (actions.ts:36); `>80` → "Display name must be 80 characters or fewer." via template literal (actions.ts:37-39); `image` trim then `setDisplayName` + `setProfileImage(…, image.length > 0 ? image : null)` (actions.ts:41-45); `redirect("/profile")` (actions.ts:47); failure-only return shape.
- `deleteOwnAccount` re-gates auth + invite only (actions.ts:59-63); `formData.get("confirm") !== "DELETE"` → `{ ok: false, error: "Type DELETE to confirm." }` (actions.ts:64-66), exact case-sensitive, no trim; `deleteAccount(campUser.id)` then `redirect("/auth/sign-out")` (actions.ts:67-68).
- DeleteAccountForm `useActionState(deleteOwnAccount, null)` → `[state, action, pending]` (delete-account.tsx:10-13); copy "This permanently erases your personal data…Lost Cat…can't be undone. Type **DELETE** to confirm." (delete-account.tsx:17-22); `<Input id/name="confirm" placeholder="DELETE" autoComplete="off">`, no `required`/`maxLength` (delete-account.tsx:25-30); destructive submit "Deleting…"/"Delete my account" (delete-account.tsx:40-42).
- `deleteAccount` wrapper: `if (isE2ETestMode()) return { lostCatNumber: 0 }` else `sanitiseAccount(userId)` (lib/account.ts:10-13); is the sole caller of `sanitiseAccount` (grep).
- `sanitiseAccount` runs in one `createPooledDb()` transaction with `pool.end()` in `finally` (account.ts:55-58,127-129); `lostCatNumber = (max(users.lost_cat_number) ?? 0) + 1` (account.ts:59-64); patch via `sanitisedUserPatch`; deletes `burnerProfiles, dietaryRequirements, driverProfiles, pushTokens, notificationDeliveries, questionnaireEdits, requiredActions, teamMemberships, carMembers (driverUserId OR memberUserId), workshopRsvps, broadcastTargets, questionnaireActivationTargets` (account.ts:74-116); `reimbursements.accountDetailsEncrypted = ""` where `submitterId = userId` (account.ts:120-123); returns `{ lostCatNumber }` (account.ts:125).
- `sanitisedUserPatch` field set verbatim: `displayName = lostCatName(N)`, `authUserId = "deleted:<userId>"`, `profileImageUrl/passportEncrypted/saIdEncrypted/eftDetailsEncrypted/emergencyContacts/telegramHandle/telegramUserId/termsVersion/termsConsentedAt = null`, `sanitised = true`, `sanitisedAt = now`, `lostCatNumber`, `updatedAt = now` — account.ts:26-42; keeps `id` + `inviteCode` (comment account.ts:18-20).
- `lostCatName(n) → "Lost Cat #${n}"` (account.ts:11-13); `SanitiseResult = { lostCatNumber: number }` (account.ts:45-47).
- `reimbursements.accountDetailsEncrypted` is `text(...).notNull()` (schema.ts:693) — confirms the empty-string (not null) scrub rationale. `carMembers` PK is `[driverUserId, memberUserId]` (schema.ts:430-439).
- `users` table at schema.ts:220-303; `displayName` (`display_name`), `profileImageUrl` (`profile_image_url`), `inviteCode` (`invite_code`), `approvalStatus` (`approval_status` enum, default `"approved"` NOT NULL), `id` uuid defaultRandom, `authUserId` (`auth_user_id` text NOT NULL unique), `lostCatNumber` (`lost_cat_number` integer), `sanitised`/`sanitisedAt` present.
- Edit writers `setUserProfileImage`/`setUserDisplayName` set the field + `updatedAt = new Date()` (burner-profile.ts:102-122); test-store equivalents set same field + `updatedAt` (test-store.ts:220-237).
- `MAX_NAME_LENGTH = 80` (actions.ts:16) mirrored by `maxLength={80}` (edit-form.tsx:40); `UpdateProfileResult`/`DeleteAccountResult = { ok: false; error: string }` (actions.ts:13-14).
- Avatar route contract: `MAX_BYTES = 5 * 1024 * 1024` (route.ts:10); per-user `rateLimit(…, { limit: 20 })` (route.ts:30), per-IP `{ limit: 40 }` (route.ts:40); statuses 401/429/400/415/413/502 (route.ts:27,34,46,54,59,62,65,91); image-only `file.type.startsWith("image/")` (route.ts:61); proxy URL `/api/avatar?pathname=…` (route.ts:96-97); E2E/no-token echo `avatars/<userId>/test-avatar.webp` (route.ts:72-75).
- `cropResizeToSquare` defaults `size = 512`, `quality = 0.85`, output `image/webp` (lib/image.ts:17-37).
- `CampUser` shape `id, authUserId, displayName, profileImageUrl, inviteCode, rank ("captain"|"member"), approvalStatus ("pending"|"approved"|"rejected")` (users.ts:39-47,32-33).
- AvatarUpload is genuinely shared with onboarding — imported by questionnaire/question.tsx:28 for the `"image"` question type (question.tsx:235).

## Low-confidence / could-not-verify
- **Action vs page gating asymmetry** (doc:133): the doc's own low-confidence flag is correct — `updateProfile`/`deleteOwnAccount` re-gate only on auth + invite (actions.ts:28-32,59-63) and never re-read burner profile or approval. Source confirms the asymmetry; whether it is intentional vs a hole is a design question, and no test asserts it.
- **Monotonic / never-reused `lostCatNumber` under concurrency** (doc:130): `max + 1` inside a Drizzle transaction (account.ts:59-64) is correct as written, but the actual isolation guarantee (whether two concurrent `pool` transactions can both read the same max and collide) depends on Postgres default isolation level (READ COMMITTED) — not enforced by code here. Behaviorally plausible but not provable from this file alone.
- **`/api/avatar` proxy 401 behavior** (doc:42,134): the claim that the proxy 401s for a not-yet-approved member mid-onboarding is sourced from a code comment, not verified against the proxy route itself (owned by unit 22 — out of scope here).
