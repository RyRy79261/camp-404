# Verification — 02 auth-shells

**Verdict:** minor-gaps  ·  checked 71 claims, verified 67.
The doc is highly reliable: line refs, copy strings, payloads, validation rules, callbackURL targets, cookie config and env vars are digit-exact against source. Defects cluster in the "Sub-components / variants" / "Auth shell chrome" sections, where two reuse-hook claims about `AuthShell` (`footer` orphaned, `hideBack` caller enumeration) and one helper-attribution claim are stale — `signup/required` and `pending-approval` are real `AuthShell` callers the doc omits, and `footer` is in fact passed by `signup/required`.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| medium | "`AuthShell`'s `footer` prop is **currently unused / orphaned** (no caller passes it)" (lines 62, 152) | `signup/required/page.tsx` passes `footer="Camp 404 is invite-only."` to `AuthShell`. The prop is live, not orphaned. | apps/web/app/signup/required/page.tsx:30 |
| medium | "`getAuthenticatedUser` / `getAuthenticatedUserOrRedirect` … used by `/auth/page.tsx`" (line 156) | `/auth/page.tsx` imports and calls only `getAuthenticatedUser` (line 21). `getAuthenticatedUserOrRedirect` is never called by any `/auth/*` page — it is used by downstream protected pages (pending-approval, onboarding, profile, tools, captains, signup/required, etc.). | apps/web/app/auth/page.tsx:2,21 ; apps/web/lib/auth.ts:40 |
| low | "`hideBack` is passed `true` by every current caller (**sign-in & sign-up**)" (lines 60, 152) — enumeration | `hideBack` callers are `auth/[path]/page.tsx` (sign-in, sign-up), `pending-approval/page.tsx:49`, and `signup/required/page.tsx:30`. The "(sign-in & sign-up)" list is incomplete. NOTE: the downstream conclusion — Back button never renders in the live flow — still holds, since all four callers pass `hideBack`. | apps/web/app/pending-approval/page.tsx:49 ; apps/web/app/signup/required/page.tsx:30 |
| low | "the Back button never renders **in the live flow today**" framed as auth-shell-specific (line 152) | Accurate today (all 4 callers pass `hideBack`), but it is a cross-unit invariant spanning pending-approval + signup/required, not just the two auth forms the section scopes it to. Flagged for completeness. | apps/web/components/auth-shell.tsx:35 |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | `AuthShell` has two more live callers beyond the auth forms: `pending-approval/page.tsx` (hideBack) and `signup/required/page.tsx` (hideBack + footer). The doc's "Sub-components" treatment implies sign-in/sign-up are the only consumers. | apps/web/app/pending-approval/page.tsx:49 ; apps/web/app/signup/required/page.tsx:30 |
| low | `POST /api/test/login` defaults: `id ?? "test-stack-${Date.now()}"`, and `displayName ?? body.email ?? null`. Doc describes the cookie shape `{ id, primaryEmail, displayName }` (line 131) but not the server-side defaulting of those fields. Cosmetic. | apps/web/app/api/test/login/route.ts:22-26 |

## Spot-confirmed
- `/auth/page.tsx`: `export const dynamic = "force-dynamic"` (line 6); `getAuthenticatedUser()` (21); `if (user) redirect("/")` (22); else `redirect("/auth/sign-in")` (23). All exact.
- `[path]/page.tsx`: `dynamic = "force-dynamic"` (10) with dynamicParams-default comment (7-9); `params: Promise<{ path: string }>` awaited (15-17); `path === "sign-up"` → `<AuthShell hideBack><SignUpForm/></AuthShell>` (19-28) with open-signup comment (20-22); `path === "sign-in"` → `<AuthShell hideBack><Suspense fallback={null}><SignInForm/></Suspense></AuthShell>` (30-38); fallback `<main className="mx-auto flex min-h-[100dvh] w-full max-w-md flex-col items-center justify-center px-6 py-12"><AuthView path={path}/></main>` (43-47); "side trips we haven't (yet) built bespoke screens for" comment (40-42); `AuthView` from `@neondatabase/auth/react/ui` (2). All exact.
- sign-in-form.tsx: `"use client"` (1); `safeCallbackUrl` guard returns `/` on falsy/non-`/`/`//` else passthrough (17-22); `safeCallbackUrl(searchParams.get("callbackURL"))` (27); `useSession()` → `session` + `isPending: sessionPending` (28); auto-forward effect `if (sessionPending) return; if (!session?.user) return; if (callbackURL === "/") return; window.location.replace(callbackURL)` (38-43); validation `"Email is required"` / `"Password is required"` (50-56); `signIn.email({ email: trimmedEmail, password, callbackURL })` (61-65); `result.error.message ?? "Sign in failed"` (67); `router.replace(callbackURL); router.refresh()` (71-72); catch fallback `"Sign in failed"` (74); `handleGoogle` → `signIn.social({ provider: "google", callbackURL: "/auth" })` (86-89) with verifier-round-trip comment (83-85), catch `"Google sign in failed"` (91); Email `type="email" placeholder="you@example.com" autoComplete="email" required` (109-112); Password `autoComplete="current-password" required` (132-133); "Forgot your password?" → `/auth/forgot-password` (123-126); heading "Welcome back" (99) / "Sign in to your Camp 404 account." (101); submit `loading ? "Signing in…" : "Sign in"` (147); "Or continue with" divider (156); outline "Continue with Google" + `GoogleMark` (160-169); `role="alert"` error `text-[color:var(--color-destructive)]` (140-143); GoogleMark 24×24 single-`<path>` `fill="currentColor"` `aria-hidden` (174-188). All exact.
- sign-up-form.tsx: name-as-email comment (11-17); state `email/password/confirmPassword/error/loading`, no `useSession`/callbackURL (19-24); validation incl. `password !== confirmPassword → "Passwords do not match"` (39-41); `signUp.email({ email: trimmedEmail, password, name: trimmedEmail, callbackURL: "/" })` (46-51); `"Sign up failed"` fallback (53,60); `router.replace("/"); router.refresh()` (57-58); `handleGoogle` identical to sign-in's social call → `/auth`, catch `"Google sign up failed"` (73-78); Confirm-password `autoComplete="new-password" required` (125-126); heading "Create your account" (86) / subtext "Set a password or continue with Google. We'll ask the rest in the questionnaire." (88-89); submit `loading ? "Creating account…" : "Create account"` (140); footer "Already have an account? Sign in" → `/auth/sign-in` (164-172); GoogleMark duplicated verbatim (177-191). All exact.
- auth-shell.tsx: `"use client"` (1); props `{ children, className?, footer?, hideBack? }` (10-29); outer `flex min-h-svh flex-col items-center justify-center bg-[color:var(--color-muted)] p-6 md:p-10` (33); inner `cn("w-full max-w-sm", className)` (34); Back rendered `!hideBack` — `Button variant="ghost" size="sm"` + `ArrowLeft` + "Back" + `onClick={() => router.back()}` (35-47); `<Card className="overflow-hidden p-0"><CardContent className="p-6 md:p-8">{children}</CardContent></Card>` (48-50); footer `<p className="px-6 pt-4 text-center text-xs …">{footer}</p>` (51-55). All exact.
- providers.tsx: `"use client"` (1); `NeonAuthUIProvider` from `@neondatabase/auth/react/ui` (3); props `authClient={authClient}`, `navigate={router.push}`, `replace={router.replace}`, `onSessionChange={() => router.refresh()}`, `redirectTo="/"`, `Link={Link}` (14-21). All exact.
- layout.tsx: `<Providers>` wraps children + `<AcknowledgementGate />` + `<FeedbackGate aiAvailable={!!process.env.ANTHROPIC_API_KEY} />` (50-56); `<html lang="en" suppressHydrationWarning>` with next-themes/dark comment (45-48). Exact.
- proxy.ts: `export default auth.middleware({ loginUrl: "/auth/sign-in" })` (10) with "ONLY inside this proxy" comment (3-5); `matcher: ["/auth", "/auth/:path*", "/mcp/:path*"]` (18). Exact.
- route.ts (api/auth): `export const { GET, POST } = auth.handler();` (8) with proxy comment (3-7). Exact.
- neon-auth.ts: `createNeonAuth({ baseUrl, cookies: { secret, sameSite: "lax" } })` (25-35); `sameSite: "lax"` with cross-site comment (29-33); `PLACEHOLDER_BASE_URL = "https://build-placeholder.neon-auth.invalid"` (21); `PLACEHOLDER_COOKIE_SECRET = "build-placeholder-secret-build-placeholder-secret"` // 50 chars (22-23). Exact. `@neondatabase/auth` is a real dep at `0.4.1-beta` (apps/web/package.json:29).
- test-mode.ts: `TEST_USER_COOKIE = "camp404_test_user"` (9); `isE2ETestMode()` returns `process.env.E2E_TEST_MODE === "1"` (11-13). Exact.
- api/test/login/route.ts: POST gated on `isE2ETestMode()` → 404 (18-19); cookie set `httpOnly: true`, `secure: process.env.NODE_ENV === "production"`, `sameSite: "lax"`, `path: "/"`, `maxAge: 60 * 60` (29-35); URL-encoded JSON `{ id, primaryEmail, displayName }` (22-29); DELETE clears + same 404 gate (39-45). Exact.
- lib/auth.ts: `AuthenticatedUser { id: string; primaryEmail: string | null; displayName: string | null }` (13-17); `getAuthenticatedUser` E2E-cookie precedence then `auth.getSession()` (25-37); mapping `id ← session.user.id`, `primaryEmail ← session.user.email ?? null`, `displayName ← session.user.name ?? null` (32-36); `readTestUserCookie` requires non-empty string `id` (50-57). Exact.
- landing-hero.tsx: single CTA `<a href="/auth/sign-in">Are you lost?</a>` (32-34); "Error 404 — Camp not found" copy (26); "$ awaiting input_" (39). CTA contract pinned by `tests/e2e/home.spec.ts` (asserts link `Are you lost?` has href `/auth/sign-in` and lands on `/auth/sign-in`). Exact. `LandingHero` rendered by `app/page.tsx:33` only when `!user`.
- Enums: social provider `"google"` only (no other `signIn.social` provider in source); bespoke `[path]` branches exactly `"sign-up"`/`"sign-in"` else `<AuthView>`; callbackURL targets — Google → `"/auth"`, email sign-up → `"/"`, email sign-in → sanitised query (default `"/"`). All exact.

## Low-confidence / could-not-verify
- Behavior of `@neondatabase/auth` internals (`createNeonAuth`, `auth.handler()`, `auth.middleware()`, `<AuthView>`, `NeonAuthUIProvider`, `useSession`, `signIn.email`/`signUp.email`/`signIn.social`) is upstream-package and not inspected; the doc's descriptions of the verifier→cookie exchange, the `next-themes`/dark-mode injection via the provider, and the Better Auth required-`name` constraint are taken on trust from comments + package contract, not verified against package source.
- Claim that the proxy verifier exchange is the "ONLY place" the exchange runs (proxy.ts:3-5, doc line 70) is asserted by source comment; not independently confirmed against the Neon Auth package.
- Claim that Better Auth imposes its own server-side password length/strength rules (doc line 136) — not verifiable from app source; depends on upstream defaults.
