# Verification — 27 notifications-engine

**Verdict:** accurate  ·  checked 71 claims, verified 70.
The doc is an exceptionally faithful description of the broadcast→delivery→inbox engine: every cited file, line range, enum member, schema default, error string, and control-flow branch was confirmed against production source. The single non-confirmation is a behavior the doc itself already flags as low-confidence (refType/refId deep-link wiring is built data-side but unread by any UI), so there is no high- or medium-severity defect.

## Inaccuracies
| severity | doc claim | code reality | file:line |
|---|---|---|---|
| _(none)_ | — | — | — |

No inaccuracies found. Every spot-check (below) confirmed the doc digit-for-digit, including line ranges, which were unusually precise.

## Omissions
| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | `broadcasts.team` column is typed `teamEnum`, not free text; doc's data-model row says "`team` `teamEnum`" so this is actually captured. No real omission. | packages/db/src/schema.ts:774 |
| low | `notification_deliveries.deliveredAt` exists (doc lists it) but is never written by this engine (only the out-of-scope push worker would). Doc credits it correctly as a column; not flagged as written-here, so no misattribution. | packages/db/src/schema.ts:861 |

No material omissions. The doc even pre-empts the obvious gaps (dead enum values, test-store divergences, unbuilt deep-link).

## Spot-confirmed
- **Facade** `notifications.ts`: `NotificationsBackend` iface at 38-69; `realBackend` 71-82; `testBackend` 84-115; `backend()` picks via `isE2ETestMode()` 117-119; exactly **10** exported functions 121-181 (countUnread, listInbox, markRead, getPendingAcknowledgements, acknowledgeDelivery, listAnnouncements, createAnnouncementDraft, updateAnnouncementDraft, deleteAnnouncementDraft, publishAnnouncement); re-exported types 30-36; rationale comment ("same real-vs-test backend split `lib/users.ts` uses…") 23-28. All correct.
- **`isE2ETestMode()`** = `process.env.E2E_TEST_MODE === "1"` (apps/web/lib/test-mode.ts).
- **`computeAudience`** (audience.ts:29-64): PURE, no DB; `real` set = `!isSystem && !sanitised` (34-36); `everyone`→all real (40-41); `team`→filter by `m.team === broadcast.team` else `[]` when team falsy (43-48); `team_leads`→`isLead` (50-51); `drivers`→`driverUserIds` (52-54); `individual`→`targetUserIds` (55-57); default `[]` (59-60); final filter de-dups via `new Set` and keeps `real.has(id) && id !== senderId` (63). `BroadcastScope` type 6-11.
- **`resolveAudience`** (broadcasts.ts:52-94): `createHttpDb`; reads users(id,isSystem,sanitised), all teamMemberships(userId,team,isLead), driverProfiles where `intendsToDrive=true`, and broadcastTargets only when `scope==='individual'`; then calls `computeAudience`.
- **`isOwnedAnnouncementDraft`** (broadcasts.ts:36-44): id + senderId + kind='announcement' + scope='everyone' + `isNull(publishedAt)`.
- **`listAnnouncements`** (117-149): filters `kind='announcement'`, `orderBy(desc(createdAt))`, leftJoin sender displayName, `recipientCount`/`acknowledgedCount` as correlated `count(*)::int` subqueries (acknowledged adds `acknowledged_at is not null`), both `?? 0`.
- **`createAnnouncementDraft`** (159-175): inserts kind='announcement', scope='everyone', returns `{ id }`.
- **`updateAnnouncementDraft`** (181-199) / **`deleteAnnouncementDraft`** (202-212): write gated on `isOwnedAnnouncementDraft`, return `rows.length > 0`.
- **`publishAnnouncement`** (228-290): `createPooledDb`, transaction, claim sets `publishedAt=new Date(), dispatchedAt=new Date()` (238) returning id/title/body/channel/presentation; no row → `{ ok:false, error:"Draft not found, already published, or not yours." }` (250-253); `resolveAudience({id, scope:'everyone', team:null}, senderId)`; `recipientIds.length===0` → `{ ok:true, recipientCount:0 }` with no insert (265-267); bulk insert copies title/body/channel/presentation + `refType:"announcement"`, `refId: broadcast.id`, `.onConflictDoNothing()` (269-283); `pool.end()` in `finally` (288).
- **`dispatchDueBroadcasts(now = new Date())`** (306-386): selects `isNotNull(publishedAt) AND isNull(dispatchedAt) AND (isNull(sendAt) OR lte(sendAt, now))` (324-333); `due.length===0` → `{dispatched:0,deliveries:0}` (335); per broadcast resolves its own scope/team; atomic claim `UPDATE … dispatchedAt=now WHERE id=? AND isNull(dispatchedAt)` returning id, `!claimed[0]` → `false` skip (347-357); insert only when `recipientIds.length > 0` (358) with `refType: b.refType ?? null`, `refId: b.refId ?? b.id`, `.onConflictDoNothing()`; tallies; `pool.end()` finally.
- **`getPendingAcknowledgements`** (403-430): `userId=? AND presentation='acknowledge' AND isNull(acknowledgedAt)`, `orderBy(createdAt)` ascending (oldest-first, 428), leftJoin broadcast→sender, returns `{deliveryId,title,body,senderName,createdAt}`.
- **`acknowledgeDelivery`** (437-458): sets `acknowledgedAt=now, readAt=now` where `id=? AND userId=? AND presentation='acknowledge' AND isNull(acknowledgedAt)`; returns `rows.length>0`. Variant- and owner-scoped exactly as documented.
- **`listInbox`** (472-493): all deliveries for user, `desc(createdAt)`, leftJoin sender, returns `{id,title,body,presentation,senderName,readAt,acknowledgedAt,createdAt}`.
- **`countUnread`** (496-508): `count(*)::int` where `userId=? AND isNull(readAt)`, `?? 0`.
- **`markRead`** (516-529): `ids.length===0` early return (517); sets `readAt=new Date()` where `userId=? AND inArray(id, ids) AND isNull(readAt)`.
- **GET /api/notifications/pending** (pending/route.ts): `runtime="nodejs"` (6), `dynamic="force-dynamic"` (7); `!user` → `{ pending: [] }` (16-17); `!hasCampAccess` → `{ pending: [] }` (23-24) with the exact synthetic-empty-id rationale comment; else `getPendingAcknowledgements(campUser.id)` (26).
- **POST /api/notifications/acknowledge** (acknowledge/route.ts): `runtime="nodejs"` (7); `Body = z.object({ deliveryId: z.string().uuid() })` (13); `!user` → `401 { error:"unauthorized" }` (17-18); invalid → `400 { error:"invalid" }` (22-23); `!hasCampAccess` → `{ ok:false }` (29); else `{ ok }` (31-35).
- **GET /api/cron/notifications/dispatch** (dispatch/route.ts): `runtime="nodejs"` (5); `assertCron(req)` deny-guard (14-15); on pass `{ ok:true, ...result }` (16-17). Imports `dispatchDueBroadcasts` directly from `@camp404/db/broadcasts` (2) — the one production caller that bypasses the facade, consistent with the doc treating it as the cron entry point.
- **Cron auth** (cron-auth.ts): `isAuthorizedCron` 12-22 — `if (!secret) return false` (fails closed), length-guard, `timingSafeEqual` constant-time; `assertCron` 31-38 returns 401 `NextResponse` or null. Doc's cite of 12-22 for the compare is exact.
- **Captain actions** (actions.ts): `requireCaptain` 23-40 with the four error strings "Not signed in." (27), "Your account isn't camp-active yet." (30), "Your account is still awaiting approval." (34, gated by `isApproved`), "Captain access only." (`rank !== 'captain'`, 37); returns `{ ok:true, captainId }` (39). `saveDraftAction`/`updateDraftAction` `safeParse` → `issues[0]?.message ?? "Invalid."` (51, 72). update/delete surface "Draft not found or already published." (81, 94). `publishAction` passes the `PublishResult` error verbatim (`if (!result.ok) return result`) and on success returns `{ ok:true, data:{ recipientCount } }` (110-113). All four `revalidatePath("/captains/announcements")`.
- **AcknowledgementGate** (acknowledgement-gate.tsx): `POLL_INTERVAL_MS = 45_000` (26); `requestIdRef` monotonic token (35), superseded-drop `if (requestId !== requestIdRef.current) return` (45); poll on mount + 45s interval + visibilitychange/focus (54-67); scroll lock `body.style.overflow="hidden"` + reset to top + restore prior overflow on cleanup (73-81); `acknowledge` posts, bumps `requestIdRef.current++` to drop in-flight poll (94-95), filters the item, `router.refresh()` (85-103); `disabled={acking}` (148); `Loader2` spinner while `acking` (150); `if (!current) return null` (83). Mounted once in `app/layout.tsx:52`.
- **Inbox page** (notifications/page.tsx): `getAuthenticatedUserOrRedirect` (25), `redirect("/signup/required")` when `!hasCampAccess` (27-29); snapshot then `markRead(campUser.id, items.map(i=>i.id))` (33-37); "No notifications yet." (54); icons Megaphone/MessageSquare/Bell by presentation (18-22); "New" pill + highlighted border when `readAt===null` (58-81); footer "· acknowledged" / "· awaiting acknowledgement" (93-98).
- **HomeHeader** (home-header.tsx): Link `href="/notifications"` (27), badge `notifications > 99 ? "99+" : notifications` (41), falsy count hides badge (36-43). Genuinely live: fed `notifications={unreadNotifications}` from `countUnread(campUser.id)` in `app/page.tsx:68,88-92`.
- **Types** (announcement.ts): `AnnouncementPresentation = z.enum(["acknowledge","popup","feed"])` (8-12); `ComposeAnnouncementInput` (23-27): title trimmed min1 "Give it a title." max120, body trimmed min1 "Write the announcement." max5000, `presentation` default `"acknowledge"`; comment on camp-wide-only subset 16-22.
- **Schema enums**: `broadcast_kind` 128-134 = announcement|team_message|lead_directive|reminder|system (5); `broadcast_scope` 136-142 = everyone|team|team_leads|drivers|individual (5); `notification_channel` 144-148 = push|in_app|both; `push_delivery_status` 150-155 = queued|sent|failed|skipped; `broadcast_presentation` 166-170 = acknowledge|popup|feed.
- **Schema tables**: `broadcasts` 763-807 — senderId onDelete:"set null"/nullable, kind/scope notNull, `team teamEnum`, channel default `"both"` (778), presentation default `"feed"` (785), refType/refId, publishedAt/dispatchedAt/sendAt nullable, createdAt defaultNow; indexes `broadcasts_sender_idx`/`broadcasts_created_at_idx` (804-805). `broadcast_targets` 810-823 cascade FKs, PK (broadcastId,userId). `notification_deliveries` 830-887 — broadcastId nullable cascade, userId notNull cascade, channel notNull (843), presentation default `"feed"` (848), pushStatus default `"queued"` (851), readAt/acknowledgedAt/deliveredAt nullable; indexes user_read_idx, user_ack_idx, broadcast_idx, and partial unique `notification_deliveries_broadcast_user_uniq` ON (broadcastId,userId) `WHERE broadcast_id IS NOT NULL` (881-885). `push_tokens` 734-753.
- **Test-store divergences (test-store.ts):** `publishBroadcast` 404-438 fans out to `[...usersByAuthId.values()].filter(u => u.id !== input.senderId)` — no system/sanitised/real-set filter, no scope (421-423); `TestBroadcast` 64-72 has no scope/team/channel/refType/dispatchedAt/sendAt; `TestDelivery` 74-84 lacks channel/pushStatus/refType/refId/deliveredAt; no `dispatchDueBroadcasts` equivalent (grep confirms zero `dispatch`/`sendAt`/`scope`/`channel`/`pushStatus` tokens); acknowledgeDelivery/getPendingAcknowledgements/countUnread/markRead/listInbox match real variant/owner/order/unread semantics (500-556); `reset()` clears broadcasts+deliveries 558-567.
- **Dead-but-defined**: `broadcast_kind` values team_message/lead_directive/reminder/system and scopes team/team_leads/drivers/individual are resolvable in `computeAudience` but no compose path wires them — only announcement/everyone is composed (confirmed: only `kind:"announcement", scope:"everyone"` inserts exist in broadcasts.ts).

## Low-confidence / could-not-verify
- **refType/refId deep-link (doc's own low-confidence note, confirmed correct):** publish hard-codes `refType:"announcement"`/`refId: broadcast.id` (broadcasts.ts:279-280) and dispatch copies `b.refType ?? null`/`b.refId ?? b.id` (369-370), but a grep across `apps/web/app` + `apps/web/components` (.ts/.tsx) finds **zero** UI readers of `refType`/`refId`/`ref_type`/`ref_id`, and neither `InboxItem` (broadcasts.ts:460-469) nor `PendingAcknowledgement` (390-396) even expose them. The wiring is built data-side but unread by any presentation handler — exactly as the doc's HTML comment states. This is the one claim the source does not actively confirm as user-facing; the doc correctly demotes it to low-confidence.
- **`dispatchDueBroadcasts` is only reached via the cron route**, whose execution depends on a real `CRON_SECRET` and an external scheduler (vercel.json cron config) — not verifiable from source alone, but the route + guard + worker chain is fully present and correctly wired.
- **`deliveredAt` / `pushStatus` / `push`/`in_app` channel** are carried on rows but exercised only by the out-of-scope push worker; the doc scopes them out and does not claim this engine touches them, which the read confirms (no writes to those fields anywhere in broadcasts.ts beyond schema defaults).
