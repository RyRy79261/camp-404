# Verification — 24 nav-chrome-layout

**Verdict:** accurate  ·  checked 78 claims, verified 76.
This is an unusually faithful document. Every load-bearing claim — layout shell, metadata, manifest, OG/icon renderers, both error boundaries, both gates, the shake state machine, the feedback action + GitHub assembly, the notification routes/queries, the FCM SW handler, and the boot-time env assertion — was confirmed digit-for-digit against the real source. The only defects are two cosmetic line-range off-by-a-few citations; no fabricated features, no dead code presented as live, no wrong-subsystem attribution.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | `structureWithAi … feedback-ai.ts:71-103 returns StructuredReport \| null` (lines 128, and §Sub-components) | Function body actually spans lines 71-105 (the `try` returns `parsed.success ? parsed.data : null` at L100, `catch` returns null at L104). Off by two lines; behavior and return type are correct. | apps/web/lib/feedback-ai.ts:71-105 |
| low | Issue assembly cites `github-feedback.ts:28-32,178-199` for `buildFeedbackIssue` | `buildFeedbackIssue` runs 178-200 (closing brace + return at 199-200); `labelsFor` is 28-32. Trivial range clip. | apps/web/lib/github-feedback.ts:178-200 |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | Doc lists the SW `onBackgroundMessage` notification body but does not note the FCM compat scripts are loaded from `https://www.gstatic.com/firebasejs/12.14.0/...` (both firebase-app-compat and firebase-messaging-compat). Version `12.14.0` is correctly cited; the gstatic origin is not. Non-material. | apps/web/app/firebase-messaging-sw.js/route.ts:22-23 |
| low | `buildFeedbackIssue` footer joins parts with ` · ` and the body joins `[...sections, "---", footer]` with `\n\n`; doc says "a `---` + PII-free footer (provenance, voice-dictated marker, opaque reporter id, sanitized route)" which is accurate but omits the exact `·` separator. Cosmetic. | apps/web/lib/github-feedback.ts:183-197 |

## Spot-confirmed
- Root layout tree `<html lang="en" suppressHydrationWarning>` → `<body>` → `<Providers>{children}<AcknowledgementGate /><FeedbackGate aiAvailable={!!process.env.ANTHROPIC_API_KEY} /></Providers>`; gates are siblings of `{children}` (layout.tsx:48-58). `suppressHydrationWarning` comment matches (layout.tsx:45-46). `import "@camp404/ui/styles.css"` (layout.tsx:5).
- `aiAvailable = !!process.env.ANTHROPIC_API_KEY` server-side, passed to `FeedbackGate` (layout.tsx:55) — confirmed.
- Metadata: `metadataBase = new URL("https://camp-404.com")` via `SITE_URL` (layout.tsx:7,14); `title "Camp 404"`, `description "A calm command centre for a chaotic desert."` via `SITE_DESCRIPTION`, `applicationName "Camp 404"` (layout.tsx:8,15-17). openGraph `type:"website"`, `siteName/title "Camp 404"`, `url:"/"`, `locale:"en_GB"` (layout.tsx:18-25). twitter `card:"summary_large_image"` (layout.tsx:26-30). not-found metadata `{ title: "Page not found" }` (not-found.tsx:7).
- Viewport: `width:"device-width"`, `initialScale:1`, `maximumScale:1`, `themeColor:"#0d061e"` (layout.tsx:33-38).
- Providers: `"use client"`, wraps `NeonAuthUIProvider` with `authClient`, `navigate={router.push}`, `replace={router.replace}`, `onSessionChange={() => router.refresh()}`, `redirectTo="/"`, `Link={Link}` (providers.tsx:1,14-21). `authClient = createAuthClient()` same-origin (auth-client.ts:14).
- Manifest: `name/short_name "Camp 404"`, `description`, `start_url:"/"`, `display:"standalone"`, `background_color`/`theme_color "#0d061e"`; icons `[{/icon.svg, sizes:"any", image/svg+xml, purpose:"any"}, {/apple-icon, "180x180", image/png, purpose:"maskable"}]` (manifest.ts:6-27).
- og-image hex consts: `BACKGROUND "#0d061e"`, `FOREGROUND "#f7ecf3"`, `MUTED "#b29ab0"`, `MAGENTA "rgba(255, 0, 140, 0.92)"`, `CYAN "rgba(0, 220, 255, 0.92)"` (og-image.tsx:13-17). `SHARE_SIZE {1200,630}`, `SHARE_CONTENT_TYPE "image/png"`, `SHARE_ALT` exact string (og-image.tsx:19-22).
- `renderShareImage`: kicker "Camp 404", "Error 404 — Camp not found", `Glitch404 fontSize={320} split={14} glow={80}`, tagline, radial magenta bloom (og-image.tsx:25-108, 70/82/89/102). `renderSquareIcon(size)`: `fontSize=Math.round(size*0.46)`, `split=Math.max(2, Math.round(size*0.035))`, bg edge-to-edge (og-image.tsx:111-132). `Glitch404({fontSize, split, glow=0})`: magenta `translate(-split)`, cyan `translate(+split)`, foreground on top; textShadow only when glow truthy with the literal Satori comment (og-image.tsx:135-180).
- File-based routes: opengraph-image re-exports `alt/size/contentType`, returns `renderShareImage()` (opengraph-image.tsx:10-16); twitter-image identical (twitter-image.tsx:9-15); apple-icon `size={width:180,height:180}`, `contentType:"image/png"`, returns `renderSquareIcon(180)` (apple-icon.tsx:5-10). Exactly 3 file-based routes + static icon.svg (ls of app/).
- icon.svg: 64×64 `rx="12"` `#0d061e`, monospace, `font-size="27"`, `font-weight="900"`, `letter-spacing="-1.5"`; texts x=30.5 `#ff008c`, x=33.5 `#00dcff`, x=32 `#f7ecf3`; `aria-label="Camp 404"` (icon.svg:1-17).
- not-found.tsx: `main.mx-auto … max-w-lg`, `404` in `--color-primary`, "You're properly lost.", exact copy, `<Button asChild><Link href="/">Back to camp` (not-found.tsx:13-29).
- error.tsx: `"use client"`, `console.error(error)` + `headingRef.current?.focus()` (error.tsx:19-26), heading "Something went sideways.", copy, `<Button onClick={reset}>Try again</Button>` + outline "Back to camp" → `/` (error.tsx:36-47), `tabIndex={-1}` heading (error.tsx:33).
- global-error.tsx: `"use client"`, own `<html lang="en">`/`<body>`, inline `background:"#0d061e"`, `color:"#f6eef7"`, system-ui font, button `background:"#ef1ec1"` with the `--color-primary oklch(0.65 0.27 340)` comment (global-error.tsx:24-69); `console.error` + focus (17-21); heading "Camp 404 hit a snag.", copy, `<button onClick={reset}>Try again</button>` (42-69); `tabIndex={-1}` (44).
- AcknowledgementGate: `POLL_INTERVAL_MS = 45_000` (ack-gate.tsx:26); fetch `/api/notifications/pending` `cache:"no-store"` on load + interval + `visibilitychange→visible` + window `focus` (40-67); monotonic `requestIdRef` with `if (requestId !== requestIdRef.current) return` (38,45); `current = queue[0]` (69); `role="dialog" aria-modal="true" aria-labelledby="ack-title"`, `z-[100]`, `bg-[color:var(--color-background)]`, `fixed inset-0` (106-111); scroll lock captures previous overflow, sets `"hidden"`, `scrollTo({top:0})`, restores on cleanup (73-81); Megaphone + "Camp announcement"; `#ack-title` = title; subline `From {senderName} · {toLocaleString()}` with prefix omitted when null (123-129); `whitespace-pre-wrap` body (131); Acknowledge at end of scroll, `{queue.length-1} more after this.` when `queue.length > 1` (137-142); `acknowledge()` POSTs `{deliveryId}`, bumps `requestIdRef.current++`, filters queue, `router.refresh()` (85-99); `if (!current) return null` (83); container `max-w-2xl` (114).
- FeedbackGate: `signedIn = !isPending && !!session` from `authClient.useSession()` (feedback-gate.tsx:23-24); `useShakeGesture({ enabled: signedIn && !open, onShake: () => setOpen(true) })` (27); iOS perm: `if (!signedIn || !motionPermissionNeeded()) return`, `pointerdown` `{ once: true }` (32-39); `if (!signedIn) return null` (41); comment "Shake is the only trigger (matching RyRy79261/intake-tracker's ShakeToReport)" (15) and "server action enforces auth too, as defence in depth" (18-20). No floating button anywhere; commit 655c5e1 message literally "remove the floating button (shake-only) + restore 'Improve with AI'", commit 141c676 confirmed.
- use-shake-gesture: reads `accelerationIncludingGravity`, null-guards x/y/z (156-158); `createShakeDetector` counts jolts where `delta = |mag - lastMagnitude| > threshold`, magnitude rotation-invariant per docstring (60-99); fires when `jolts.length >= requiredJolts && now - lastShakeAt >= cooldownMs` with `>=` inclusive comment (101-111); defaults `threshold=8`, `requiredJolts=5` (with "deliberate, vigorous shake" comment), `windowMs=800`, `cooldownMs=3000` (134-139); `SAMPLE_THROTTLE_MS = 60` (153,161); `motionPermissionNeeded()` true only when `DeviceMotionEvent.requestPermission` is a function (21-32); `requestMotionPermission()` returns `"granted"|"denied"|"unsupported"`, non-iOS returns `"granted"` without prompt (34-52).
- ReportBugDialog: props `{ open, onOpenChange, defaultKind="bug", aiAvailable=false }` (dialog.tsx:46-51); kind toggle `role="group" aria-label="Report type"`, `aria-pressed` (162-189); Textarea `rows={6}`, `maxLength={DESCRIPTION_MAX}`, kind-specific label/placeholder (198-209); "Dictate instead" Mic → RecorderPanel, transcript appended, NO `promptKey` (passes only `onTranscript`/`onDismiss`; RecorderPanel does accept optional `promptKey?`) (213-232; recorder-panel.tsx:15); "Improve with AI" Checkbox only when `aiAvailable`, default `useAi=true` (56,235-253); `appendTranscript` newline-joins, re-caps to `DESCRIPTION_MAX`, sets `dictated=true` (75-83); `handleSubmit` in `useTransition`, calls action with `useAi: aiAvailable && useAi`, `route: window.location.pathname`, success/typed-error/transport-reject paths (85-106); `canSubmit = description.trim().length > 0 && !isPending` (108); dismiss blocked while `isPending` (113-119); success view "Report filed", `#${number} … created on GitHub` when `number > 0` else "Thanks — your report was sent.", link "View issue #…"/"Open the tracker", Done (122-147); reset on open transition (64-73).
- Feedback action: returns `{ ok:true; number; url } | { ok:false; error }` (actions.ts:15-17); no user → "Please sign in to send feedback." (50-51); burst `feedback:{id}` limit 3 → "You're sending these quickly…", daily `feedback-day:{id}` limit 20 / `windowMs:86_400_000` → "You've filed a lot of reports today…" (56-72); rate-limit default window 60_000 (rate-limit.ts:44); InputSchema kind enum `["bug","feature"]`, description `.trim().min(1,"Please describe the issue.").max(DESCRIPTION_MAX)`, `dictated?`/`useAi?` boolean, `route?` `.max(300)` (19-30); `sanitizeReportText` = redactPii → strip HTML → trim → cap, empty → "Please describe the issue." (github-feedback.ts:79-85; actions.ts:86-89); `reporterRef = campUser?.id || "unlinked"` (94-95); E2E short-circuit `{ ok:true, number:0, url:"https://github.com/RyRy79261/camp-404/issues" }` (98-100); `structured = useAi ? await structureWithAi(...) : null` (103); GitHub token gate + `DEFAULT_REPO "RyRy79261/camp-404"` + exactly-2-segment split (114-132); POST `https://api.github.com/repos/{owner}/{name}/issues`, `AbortSignal.timeout(8000)`, `X-GitHub-Api-Version: 2022-11-28`, `User-Agent: camp-404-feedback`, body `{title,body,labels}` (135-155); status map 201/401/403-404/410/other + catch (157-188).
- redactPii ordering (secrets-first then PII): Bearer/JWT/sk-pk/gh[posu]_/AKIA/xox/token-URL/long-blob/messenger-link/@handle then email/intl+local phone/13-9-digit-id/credit-card (github-feedback.ts:39-75); markdown defusers `fenced` (``` → `'''` plus a trailing space), `inlineCode`, `mdInline` (88-111); `GithubIssueSchema = { number, html_url: url }`, malformed 201 → "…couldn't read GitHub's reply." (actions.ts:36-39,157-168); labels bug `["bug","from-app"]`/feature `["enhancement","from-app"]`, `TITLE_MAX=100`, `ISSUE_BODY_MAX=60_000` (github-feedback.ts:22-32,141,197). `StructuredReport.severity ∈ "critical"|"high"|"medium"|"low"` (github-feedback.ts:18; feedback-ai.ts:20).
- pending route: `runtime="nodejs"`, `dynamic="force-dynamic"`; no user → `{pending:[]}`; `!hasCampAccess(campUser, user.primaryEmail)` → `{pending:[]}`; else `getPendingAcknowledgements(campUser.id)` (pending/route.ts:6-28). acknowledge route: `runtime="nodejs"`, `z.object({deliveryId: z.string().uuid()})`, no user → 401 `{error:"unauthorized"}`, bad body → 400 `{error:"invalid"}`, no camp access → `{ok:false}`, else `acknowledgeDelivery({deliveryId,userId:campUser.id})` (acknowledge/route.ts:7-36). `ensureCampUser` returns synthetic `id:""` (users.ts:87); `hasCampAccess(user,email)` (users.ts:219-224).
- FCM SW route: `runtime="nodejs"`, `dynamic="force-dynamic"` (1-2); interpolates `NEXT_PUBLIC_FIREBASE_*` each `?? ""` (12-18); Firebase compat `12.14.0`; `onBackgroundMessage` title `n.title || "Camp 404"`, `body: n.body || ""`, `icon:"/icon.svg"`, `data: payload.data || {}` (28-35); headers `Content-Type: text/javascript; charset=utf-8`, `Cache-Control: public, max-age=0, must-revalidate`, `Service-Worker-Allowed: "/"` (37-43); MOBILE_BUILD static-export `output:"export"` confirmed (next.config.ts:3,26).
- instrumentation: `register()` calls `assertServerEnv()` only when `NEXT_RUNTIME === "nodejs"` AND `NEXT_PHASE !== "phase-production-build"` (instrumentation.ts:16-21). env.ts: no-op when `E2E_TEST_MODE === "1"` (35); single REQUIRED var `PGCRYPTO_KEY`, `minLength:16`, exact hint text (20-26); throws multi-line listing missing/short vars (49-55).
- globals.css `@layer base`: `* { border-[color:var(--color-border)] }`, body `bg-[color:var(--color-background)] text-[color:var(--color-foreground)]`, `font-feature-settings: "rlig" 1, "calt" 1`, date/time picker indicator `filter: invert(0.85)` (globals.css:42-55). `--color-primary: oklch(0.65 0.27 340)` (globals.css:14) matches global-error comment.
- Data model: `getPendingAcknowledgements` selects `id→deliveryId`, `title`, `body`, `users.displayName→senderName`, `createdAt`; filters `userId =`, `presentation = 'acknowledge'`, `acknowledgedAt IS NULL`; `orderBy createdAt` ascending (broadcasts.ts:403-429). `acknowledgeDelivery` sets `acknowledgedAt=now, readAt=now`; predicate `id`+`userId`+`presentation='acknowledge'`+`acknowledgedAt IS NULL`; returns `rows.length > 0` (broadcasts.ts:437-457). `notification_deliveries` columns: id uuid PK, broadcastId FK cascade nullable, userId FK cascade notNull, title/body text notNull, channel enum notNull, presentation enum notNull default `"feed"`, pushStatus default `"queued"`, refType text, refId uuid, readAt/acknowledgedAt/deliveredAt ts, createdAt notNull defaultNow (schema.ts:830-863). `broadcast_presentation` enum = `["acknowledge","popup","feed"]` (schema.ts:166-170). `PendingAcknowledgement` interface `{deliveryId, title, body, senderName: string|null, createdAt: Date}` (broadcasts.ts:390-396) mirrors client `PendingItem` (ack-gate.tsx:18-24).
- Facade routes through `@camp404/db/broadcasts` (real) or `testStore` under E2E (notifications.ts:71-119); `getPendingAcknowledgements`/`acknowledgeDelivery` re-exported (133-144).

## Low-confidence / could-not-verify
- OKLCH→hex equivalences (`#0d061e` ≡ `oklch(0.15 0.05 295)`, `#ef1ec1` ≡ `oklch(0.65 0.27 340)`, `#f7ecf3`/`#f6eef7` ≡ foreground tokens, etc.): the doc faithfully reports what the code comments assert (og-image.tsx:12-17, global-error.tsx:62), but the numerical color-space conversion was not independently recomputed here. The hex strings themselves are quoted verbatim and correct.
- Whether the FeedbackGate iOS-permission `pointerdown` truly fires "once on the first interaction" in real Safari/WKWebView is runtime/browser behavior, not statically verifiable; the `{ once: true }` wiring is correct (feedback-gate.tsx:37).
- "querying with the empty id 500s on a real DB" (the rationale for the no-camp-access guard): the guard and synthetic `id:""` are confirmed in code, but the claim that the empty id specifically 500s against the live Neon DB is an asserted DB behavior not exercisable here.
- The doc's claim that popup/feed presentations are handled by "a different surface, not in this unit" is consistent with the enum + the gate filtering only `"acknowledge"`, but the existence/location of the popup-handling surface was not traced (outside this unit's files).
