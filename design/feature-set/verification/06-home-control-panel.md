# Verification — 06 home-control-panel

**Verdict:** minor-gaps  ·  checked 71 claims, verified 67.
The doc is highly reliable and digit-exact on enums, tokens, gating order, and component internals; nearly every cited fact reproduces against source. The single real defect cluster is the camp_member layer's navigation: two of its four "live" tile destinations (`/members`, `/meals`) point to routes that do not exist in the app, yet the doc presents them as working navigation without the 404 caveat it carefully applies elsewhere.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| medium | camp_member tiles navigate to live destinations: `My Teams` → `/members`, `My Tasks` → `/meals` (lines 91, 130 present these as working `<a href>` navigation, only team_lead/captain tiles flagged as inert) | No `app/members/page.tsx` and no `app/meals/page.tsx` exist anywhere under `apps/web` (no route group, no `route.ts`, and there is **no `middleware.ts`** to rewrite). Both hrefs hit Next's 404. So two of the four "working" camp_member tiles are dead links — the same ugly-truth caveat the doc gives team_lead/captain should apply here. | `apps/web/app/page.tsx:107-117` (hrefs) · routes absent (grep: no `members`/`meals` files) |
| low | `clamp(initialLayer, 0, layers.length-1)` (line 36) | Actual upper bound is `Math.max(layers.length - 1, 0)` — guards an empty `layers` array; the `-1`-only form would clamp to `-1`. Cosmetic simplification. | `packages/ui/src/components/control-panel.tsx:111-113` |
| low | `FeedbackGate aiAvailable={!!ANTHROPIC_API_KEY}` (line 166) | Source reads `!!process.env.ANTHROPIC_API_KEY` — same semantics, abbreviated in doc. | `apps/web/app/layout.tsx:55` |
| low | required_actions filter cited at `activations.ts:218-225` (line 144) | The WHERE `and(...)` block is `activations.ts:219-223`, ordered at `:225`; function spans `203-226`. Content (status='pending' AND blocking=true, ASC createdAt) is exactly right; line range drifts by a couple. | `packages/db/src/activations.ts:203-226` |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | `ControlPanelHeader` (the orphan default header) is in fact referenced once — by a Storybook story, not just "not referenced from page.tsx". The doc's low-confidence note ("whether used by any other route") resolves to: only `control-grid.stories.tsx`, never a real route. Strengthens, not weakens, the orphan claim. | `packages/ui/src/components/control-grid.stories.tsx:14,107` |
| low | Doc lists firebase `VAPID_KEY` source but doesn't note it is `process.env.NEXT_PUBLIC_FIREBASE_VAPID_KEY` (a public env, undefined → `registerToken` returns false early). Minor; behaviour ("renders the button but token registration no-ops without a key") is implied by the `!VAPID_KEY` guard. | `apps/web/lib/firebase-client.ts:23,27` |

## Spot-confirmed
- `export const dynamic = "force-dynamic"` at `page.tsx:27`; HomePage gating spine `page.tsx:29-63` in stated order: `getAuthenticatedUser` → `null`→`<LandingHero/>` (`:30-34`); `ensureCampUser`+invite gate `!hasCampAccess`→`redirect("/signup/required")` (`:39-42`); `nextGate(await getPendingRequiredActions(campUser.id))`→`redirect(gate)` (`:47-48`); legacy `!profile?.completedAt`→`redirect("/onboarding/questionnaire")` (`:53-56`); `!isApproved`→`redirect("/pending-approval")` (`:61-63`). All verbatim.
- Viewer-rank derivation `page.tsx:73-78`: `rank === "captain"` → `"captain"` else `await isTeamLead` → `"team_lead"` else `"camp_member"`. Confirmed.
- `unreadPromise = countUnread(campUser.id)` kicked off `page.tsx:68`, awaited `page.tsx:80`; `initialsFrom(campUser.displayName ?? user.primaryEmail)` `page.tsx:65`; render passes `centre={{ label: "TALK" }}`, then `<EnablePush/>` `page.tsx:84-98` — no `onPress`/`onRelease`/`onQuadrantSelect` passed (TALK + hrefless tiles inert). Confirmed.
- Home layer config `page.tsx:103-179` verbatim: camp_member TL `My Teams`/"Your crews"/`Users`/`/members`, TR `My Tasks`/"What's on you"/`ListChecks`/`/meals`, BL `My Profile`/"You & your data"/`UserRound`/`/onboarding/questionnaire`, BR `Tools`/"Meals, expenses…"/`Wrench`/`/tools`; team_lead all four hrefless (`Team Roster`/`Team Tasks`/`Lead Profile`/`Team Tools`); captain TL `Camp Management`→`/captains/camp-management`, TR `Camp Tasks` (no href), BL `Finances` (no href), BR `Camp Tools`→`/captains/tools`. Exact.
- `/profile`, `/notifications`, `/tools`, `/captains/camp-management`, `/captains/tools`, `/onboarding/questionnaire`, `/pending-approval`, `/signup/required` all have real `page.tsx`. `/auth/sign-in` served by `auth/[path]/page.tsx` `path === "sign-in"` branch.
- `ControlPanel` root `flex h-[100dvh] w-full flex-col` on `--color-background` `control-panel.tsx:127-132`; layout.tsx renders `{children}` directly in `<body>` with no `max-w-lg` wrapper (`layout.tsx:49-56`). Confirmed.
- `const [active] = useState(clamp(...))` `:111-113`; `const layer = layers[active]; if (!layer) return null` `:115-116`; `unlocked = rankLevel(viewerRank) >= rankLevel(layer.rank)` `:118`; `selectLayer` sets active + `onLayerChange?.(index, next)` `:120-124`; grid `grid-cols-2 grid-rows-2 gap-px` on `--color-border`, `key={active}`, `animate-[cp-layer-in_200ms_ease-out]` `:136-139`. Confirmed.
- HeaderBar `h-14`, border-b `--color-border`, `px-4`, title `text-base font-semibold tracking-tight` default `"Camp 404"` `:104,187-188`; header slot in `flex items-center gap-2` `:189`.
- `HomeHeader`: Bell `Link`→`/notifications`, `Bell h-5 w-5`, aria `Notifications (${notifications} unread)`/`Notifications` `home-header.tsx:26-44`; badge only when `notifications` truthy, `notifications > 99 ? "99+" : notifications`, `--color-primary`/`--color-primary-foreground`, pinned `-right-0.5 -top-0.5` `:36-43`; avatar `Link`→`/profile` aria `"Your profile"`, `Avatar h-8 w-8`, `AvatarImage` when `imageUrl` else `AvatarFallback text-xs` `:45-54`.
- `CentreButton`: rendered only when `centre` supplied `:166`; `absolute left-1/2 top-1/2`, `aspect-square w-[22%] min-w-[5rem] max-w-[7rem]`, `--color-primary` fill, `ring-4 ring-[--color-background]` `:194-205`; icon default `<Mic h-5 w-5>`, label default `"TALK"` `:195-196`; `onPointerDown→onPress`, `onPointerUp/Leave/Cancel→onRelease` `:201-204`; aria `ariaLabel ?? label` `:200`.
- `LayerTabBar`: `<nav aria-label="Switch rank view">`, `h-14`, border-t `:227-229`; one button/layer with `RANK_TAB_LABEL`; locked `rankLevel(viewerRank) < rankLevel(layer.rank)` → `Lock h-3 w-3` + aria `"${RANK_LABEL} view (locked, view only)"`, unlocked `"${RANK_LABEL} view"` `:233,240-244,253-254`; active `aria-pressed` + underline pill `:239,256-261`; `onClick → onSelect(index)` regardless of lock `:238`.
- `QuadrantTile`: icon row + `Lock h-3.5 w-3.5` when locked, label `text-base font-semibold`, optional hint `text-xs` muted `:296-311`; `CORNER_ALIGN` tl/tr/bl/br exact `:269-274`; badge gate `badge != null && !== "" && !== 0` `:313-314`, `BADGE_CORNER` tl/bl→`top-3 left-3`, tr/br→`top-3 right-3` `:278-283`, `--color-primary` fill `:318`; render modes: locked→`<div aria-disabled="true" opacity-45>` `:331-337`, href→plain `<a href onClick={onSelect}>` (not Next Link) `:340-350`, else→`<button onClick={onSelect}>` `:353-362`.
- `EnablePush`: state enum `"loading"|"unavailable"|"default"|"granted"|"denied"` `:16`; support detection + permission read `:48-73`; renders nothing unless `state === "default"` `:100`; `secondary`/`sm` "Enable notifications" Button `:102-124`; `registerToken` registers `/firebase-messaging-sw.js`, gets token w/`VAPID_KEY`, POSTs `{token, platform:"web"}` to `/api/push/tokens` `:25-42`; foreground `onMessage` only while granted, zod `FcmNotification {title: string().min(1), body?: string}`, `Notification` w/`icon:"/icon.svg"`, single subscription cleaned on unmount `:20-23,78-98`.
- `LandingHero`: returned when no session (`page.tsx:33`); `Camp 404` + `Error 404 — Camp not found`; single CTA `Button size="lg"` "Are you lost?" → `<a href="/auth/sign-in">` `landing-hero.tsx:32-34`; `$ awaiting input_` cursor; inline `glitchStyles` `:77-230`.
- `initialsFrom`: splits `/[\s@.]+/`, first 2 parts' first letter uppercased, `"?"` fallback `initials.ts:6-17`.
- Enums: `rankEnum ["captain","member"]` `schema.ts:31`; `approvalStatusEnum ["pending","approved","rejected"]` `:41-45`; `requiredActionTypeEnum ["questionnaire","acknowledgement","payment","profile_update"]` `:99-104`; `requiredActionStatusEnum ["pending","completed","waived","expired"]` `:106-111`.
- `ACTION_ROUTES = { burner_profile: "/onboarding/questionnaire" }` only built key; dietary/driver commented future; `nextGate` skips non-blocking and unmapped keys `required-actions.ts:7-11,23-29`.
- `ControlPanelRank "camp_member"|"team_lead"|"captain"` `:13`; `RANK_ORDER ["camp_member","team_lead","captain"]` `:16`; `RANK_LABEL` Camp Member/Team Lead/Captain `:18-22`; `RANK_TAB_LABEL` Me/Team Lead/Captain `:25-29`; `ControlPanelCorner "tl"|"tr"|"bl"|"br"` `:35`; defaults `viewerRank="camp_member"` `:102`, `initialLayer=0` `:103`, `title="Camp 404"` `:104`.
- Data model: `CampUser` fields + defaults `lib/users.ts:39-47`; god branch creates `rank:"member"`, `approvalStatus:"approved"` `:70-80`; synthetic non-persisted row `id:""`, `inviteCode:null` `:84-95`; `hasCampAccess = isGodEmail || !!inviteCode` `:219-224`; `isApproved = isGodEmail || status === "approved"` `:231-236`; `getPendingRequiredActions` returns `[]` under E2E `:215`; test `isTeamLead` always `false` `:448`.
- `isTeamLead` DB existence check `user_id = userId AND is_lead = true LIMIT 1` `roster.ts:204-217`; `countUnread = count(*)::int WHERE user_id = userId AND read_at IS NULL` `broadcasts.ts:496-508`; `PendingRequiredAction {actionKey,type,title,version,blocking,dueAt,createdAt}` `activations.ts:16-24`.
- `getAuthenticatedUser`: E2E test cookie precedence then Neon Auth `auth.getSession()` `lib/auth.ts:25-37`; `AuthenticatedUser {id, primaryEmail, displayName}` `:13-17`.
- `isGodEmail` case-insensitive CSV of `GOD_EMAILS` `access-control.ts:28-32` (doc cites `:28-30`; the env read is `:30`, close).
- Brand tokens exact: `--color-background: oklch(0.15 0.05 295)` `:12`, `--color-primary: oklch(0.65 0.27 340)` `:14`, `--color-primary-foreground: oklch(0.99 0.005 340)` `:15`, `--color-accent: oklch(0.62 0.18 255)` `:29`, `--radius: 0.625rem` `:39` (all `packages/ui/src/styles/globals.css`).

## Low-confidence / could-not-verify
- The doc's `<!-- low-confidence -->` about whether a `max-w-lg` wrapper is *expected* on this surface is a design-intent question, not a code fact; code-side it is correctly described (none applied). Left as the author's open design note.
- The doc treats `/members` and `/meals` as in-scope destinations but does not say whether they are intentionally-stubbed-but-routed or simply unbuilt. I confirmed the files are absent and there is no rewrite layer, so they 404 today; whether that is a known-future-route vs an oversight is a roadmap question I can't resolve from source. The behaviour (dead links) is certain; the intent is not.
- E2E test-mode behaviour (`getPendingRequiredActions` → `[]`, `isTeamLead` → `false`, in-memory `countUnread`) is confirmed in the facade code, but the doc's framing ("renders without a real DB during Playwright runs") is a test-harness claim I verified only at the backend-selection boundary, not by running the suite.
