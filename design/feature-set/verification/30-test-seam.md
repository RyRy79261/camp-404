# Verification — 30 test-seam

**Verdict:** minor-gaps  ·  checked 78 claims, verified 74.
This doc is highly reliable: every route handler, validation string, store method, enum, cookie option, and in-process consumer branch checks out digit-for-digit against the real source. Defects cluster in two spots — one repeated factual error ("13-page" wizard; the questionnaire defines 12 pages) and one self-contradicting claim about `invited_email` having "no schema column" (it does, on the very line range the doc cites). Schema line citations for the broadcasts/deliveries tables are off by ~14-20 lines (mid-table slices, not table starts) but still land on the right fields.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| medium | "skips the 13-page wizard walk" / "Shortcuts the 13-page burner-profile wizard" / "complete-onboarding ... skip the 13-page wizard" — repeated 3× (doc lines 9, 59, 169) | `QUESTIONNAIRE.pages` defines exactly **12** page objects (profile_photo, about_you, bio, burn_ideas, team_interests_intro, team_interests, cooking_competency, hardware_competency, leadership_logistics, burn_history, burn_intent, dietary). The wizard renders `questionnaire.pages.length` pages with no extra review/summary page. The "13" matches a stale source comment in `complete-onboarding/route.ts:6-8` but not the actual data. | questionnaire.ts:61-386 (12 pages); wizard.tsx:65,187 |
| medium | "`invitedEmail` exists in-store with **no obvious schema column** at the cited offsets" (doc line 160) | `invited_email` IS a real schema column: `invitedEmail: text("invited_email")` at schema.ts:**331**, squarely inside the invite_codes table range (312-342) the doc itself cites as 313-336. The "orphan through the HTTP seam" part (SeedBody never forwards it) is correct, but the "no schema column" assertion is false. | schema.ts:331 |
| low | TestBroadcast "mirrors `broadcasts`, schema.ts:**783-795**" | `broadcasts` table is defined at schema.ts:**763-807**; 783-795 is a mid-table slice (lands on `presentation`/`publishedAt`, real fields, but not the table bounds). | schema.ts:763-807 |
| low | TestDelivery "mirrors `notification_deliveries`, schema.ts:**844-860**" | `notificationDeliveries` table is at schema.ts:**830-863**; 844-860 is a mid-table slice (comment→`acknowledgedAt`), not the table definition. | schema.ts:830-863 |
| low | TestUser "mirrors `users`, schema.ts:**223-274**"; TestInviteCode "mirrors `invite_codes`, schema.ts:**313-336**" | `users` table starts at schema.ts:**220** (range 223-274 omits id/auth_user_id at 221-222); `inviteCodes` starts at schema.ts:**312** (off by 1). All claimed columns are present and correctly attributed; only the range starts are a line or two inside the table. | schema.ts:220, 312 |
| low | "`lib/push.ts` — push **subscribe/notify** are no-ops in test mode (push.ts:23, 32)" | The functions are `registerPushToken` (line 23) and `unregisterPushToken` (line 32) — register/unregister, not "subscribe/notify". No "notify" no-op exists in push.ts. Line numbers and no-op behavior are correct. | push.ts:23,32 |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The forms.ts test-mode branch lives in functions named `recordFormEdit` / `listFormEdits`, not `recordQuestionnaireEdit` / `listQuestionnaireEdits` (those are the testStore method names). Doc line 98 says "`recordQuestionnaireEdit` / `listQuestionnaireEdits` route to testStore" — conflates the consumer fn name with the store method name. Behavior + lines (144, 156-158) correct. | forms.ts:137,151 |
| low | The TestUser→`users` mirror omits that the real `users` table also carries `is_system`, `membership_tier`, encrypted EFT, skills, emergency_contacts, etc. — the doc frames TestUser as the mirror of the full table but it only mirrors the access/identity subset. (Defensible scoping, but unstated.) | schema.ts:220-310 |

## Spot-confirmed
- `isE2ETestMode()` returns `process.env.E2E_TEST_MODE === "1"` (test-mode.ts:11-13); `TEST_USER_COOKIE = "camp404_test_user"` (test-mode.ts:9); `import "server-only"` (test-mode.ts:1). ✓
- `GLOBAL_KEY = "__camp404TestStore__"` (test-store.ts:106); `globalState()` lazily inits at 108-126; stable Map/array bindings + primitive `nextSerial` read through `S` (128-137); globalThis rationale comment verbatim (97-105). ✓
- `nextId()` → `` `test-user-${S.nextSerial++}` `` (146-148); edit id `` `test-edit-${S.nextSerial++}` `` (289); broadcast uuid (363), delivery uuid (426) via `crypto.randomUUID()`. ✓
- `nextSerial` starts at 1 (122), reset to 1 (566); `reset()` clears 4 maps + truncates 3 arrays via `.length = 0` (558-567). ✓
- POST/DELETE /api/test/login: `runtime="nodejs"` (9), 404 guards (18-20, 40-42), ``id ?? `test-stack-${Date.now()}` ``, `primaryEmail = body.email ?? null`, `displayName = body.displayName ?? body.email ?? null` (22-26), cookie `httpOnly:true, secure: NODE_ENV==="production", sameSite:"lax", path:"/", maxAge:60*60` (29-35), `encodeURIComponent(JSON.stringify(user))`, returns `{ok:true,user}` (36); DELETE deletes cookie, `{ok:true}` (43-45). ✓
- /api/test/reset: runtime nodejs (8), 404 (11-13), `testStore.reset()` + cookie delete + `{ok:true}` (14-17). ✓
- /api/test/seed-invite: runtime (8), SeedBody (10-18, NO `invitedEmail`), 404 (21-23), parse→{} (24), `code is required` 400 (25-30), maps `createdByUserId/note/maxUses ?? null`, `expiresAt ? new Date(...) : null`, `assignedRank ?? null`, `requiresApproval ?? false` (31-39), returns full row (40). ✓
- /api/test/complete-onboarding: runtime (11), 404 (18-20), `authUserId is required` (22-27), `No user for authUserId ${...}` 404 (28-34), `upsertProfile({userId,version:"e2e-test",responses:{},markComplete:true})` (35-40). ✓
- /api/test/set-rank: runtime (10), 404 (18-20), validation `body.rank !== "captain" && !== "member"` → `authUserId and rank (captain|member) required` (22-27), 404 not-found (28-34), `setUserRank` (35-36). ✓
- /api/test/set-approval: runtime (11), 404 (18-20), validation against `pending|approved|rejected` → `authUserId and status (pending|approved|rejected) required` (23-33), 404 not-found (34-40), calls bare `setUserApprovalStatus` (NOT `setUserApproval`, so decider fields stay null) (41). ✓ Verified `setUserApproval` is the stamping variant (test-store.ts:205-219) and set-approval does not call it.
- /api/test/inspect: runtime (9), 404 (12-14), reads `authUserId`+`code` (15-17), `{user:null}` on miss (21), invite fallback `findUsableInviteCode ?? {code, createdByUserId:null}` (22-24), code branch (32-36), else 400 `Pass ?authUserId=... or ?code=...` (38-41). ✓
- `_helpers.ts`: exactly 6 exported fns — `login` (page.request, throws `login failed: ${status}`, 32-35), `resetTestState` (38-42), `completeOnboarding` (throws `completeOnboarding failed`, 55-65), `redeemInviteAtGate` (73-81), `setRank` (throws `setRank failed`, 89-98), `logoutAll` = `context.clearCookies()` with NO DELETE call (101-103); `LoginUser {id?,email?,displayName?}` (19-23). Cookie-jar caveat comment (11-17). No seed-invite/set-approval/inspect helper exists. ✓
- `redeemInviteAtGate` targets the real gate: label "Invite code" (invite-gate-form.tsx:39), button "Enter camp" (line 58), navigates `/signup/required`. ✓
- auth.ts: `getAuthenticatedUser` reads test cookie first in test mode (25-37); `readTestUserCookie` requires non-empty string `id`, defaults `primaryEmail`/`displayName` to null, JSON-decodes via `decodeURIComponent` (46-61, check at 52). `AuthenticatedUser {id, primaryEmail: string|null, displayName: string|null}` (13-17). ✓
- users.ts: `store = isE2ETestMode() ? testBackend : realBackend` in ensureCampUser (64), redeemInviteForUser (118), getBurnerProfile (165), findCampUserByAuthId (177). Synthetic `id:""` non-persisted row (86-94). `testBackend.isTeamLead()` returns `false` with the verbatim comment (447-450). `setUserApproval` test mapping (428-430). `seedBurnerProfileAction`/`satisfyBurnerProfileAction` no-op + `getPendingRequiredActions` returns `[]` under test mode (192-217). Stale comment re: legacy completedAt fallback (187-191). ✓
- Home gate: under test mode `getPendingRequiredActions`→`[]` so `nextGate(...)` is null and the legacy `!profile?.completedAt` check redirects to `/onboarding/questionnaire` (page.tsx:47-56). ✓
- access-control.ts: `consumeDbCode` → `testStore.consumeInviteCode(code)` in test mode (63-68); env codes short-circuit via `isEnvCode` before the store (48-49). ✓
- notifications.ts: full `testBackend` mapping all 10 announcement/inbox methods to `testStore` (84-115); `backend()` switch (117-119). ✓
- forms.ts test-mode branches at 144 / 156-158; invite/check route `testStore.findUsableInviteCode(raw)` (57-59); feedback actions short-circuit `{ok:true, number:0, url: \`https://github.com/${DEFAULT_REPO}/issues\`}` (98-100); account.ts `{lostCatNumber:0}` (account.ts:11); avatar route bypass (46); uploads/avatar route bypass (72). ✓
- Enums: `rankEnum = ["captain","member"]` (schema.ts:31); `approvalStatusEnum("approval_status") = ["pending","approved","rejected"]` (schema.ts:41-45); `broadcastPresentationEnum("broadcast_presentation") = ["acknowledge","popup","feed"]` (schema.ts:166-170). TestRank/TestApprovalStatus/TestPresentation literals match (test-store.ts:9,10,59). `is_lead` at schema.ts:455. ✓
- invite_codes columns all confirmed: code (315), created_by_user_id (316), note (319), max_uses (320), use_count default 0 (321), expires_at (322), revoked_at (323), assigned_rank (327), invited_email (331), requires_approval default false (336). ✓
- `findUsableInviteCode` usability rules: null if not found / revoked / `expiresAt <= now` / `maxUses !== null && useCount >= maxUses` (339-346); `consumeInviteCode` re-checks then `useCount += 1` (347-352). ✓
- `publishBroadcast` excludes sender (`u.id !== senderId`), returns `{ok:true, recipientCount}` else `{ok:false, error:"Draft not found, already published, or not yours."}` (404-438); draft update/delete require `publishedAt === null` + matching senderId (374-403). ✓
- Acknowledge rules: pending/acknowledge only consider `presentation === "acknowledge"` && `acknowledgedAt === null`; acknowledging stamps both `acknowledgedAt` and `readAt` (472-513). ✓
- `upsertProfile` updates in place preserving `startedAt`, only sets `completedAt` when `markComplete` (241-264); `listQuestionnaireEdits` default `limit = 20`, sorted desc by createdAt (298-309). `createUser` defaults `rank ?? "member"`, `approvalStatus ?? "approved"`, profileImageUrl null, decider null (162-174). `seedInviteCode` defaults (323-335). ✓
- playwright.config.ts: env `E2E_TEST_MODE:"1"` (52), `INVITE_CODES:"TEST-INVITE"` (53), `GOD_EMAILS:"god@example.com"` (54); `fullyParallel:false` (16), `workers:1` (17), `retries: CI?2:0` (18), `timeout:60_000` (10), `expect.timeout:10_000` (25). ✓
- `components/__tests__/wizard.test.tsx` exists (component-layer wizard coverage as claimed). ✓
- Dead/orphan claims correct: `setUserApproval` reachable only via in-process `testBackend` (users.ts:428-430), never via a /api/test/* route; `invitedEmail` never settable through seed-invite SeedBody; broadcasts/deliveries/ID-docs/edits store methods have no dedicated route; `logoutAll` never calls DELETE /api/test/login. ✓

## Low-confidence / could-not-verify
- "production never sets E2E_TEST_MODE" and "the `/api/test/*` routes all 404 ... in production" — verified that the guard returns 404 when the env is unset (every route's first statement) and that `playwright.config.ts` is the only repo place that sets it; whether a deployment env injects it is a deploy-config question not checkable from source.
- The "13-page" figure is also baked into the source comment at `complete-onboarding/route.ts:6-8` and `_helpers.ts:46`. The doc faithfully reproduces a stale comment; flagged as medium because the doc presents it as fact and it disagrees with the authoritative `QUESTIONNAIRE.pages` data (12). If the maintainers intend the comment as truth, this is a source bug the doc inherited rather than a doc-only error.
- Claim that the announcement/notification store methods are "exercised ... in the rendered UI" (doc line 203) and the consumer-attribution coverage is taken from the consumer files read here; I did not trace every E2E spec to confirm each path is actually hit at runtime (test-execution-dependent).
