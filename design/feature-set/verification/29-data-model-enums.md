# Verification — 29 data-model-enums

**Verdict:** minor-gaps  ·  checked 78 claims, verified 76.
This is an exceptionally reliable, digit-accurate catalog: every enum member list, every table column/default/index/FK, every cited file:line, and every helper-behaviour claim checks out against source. The lone defect is a self-contradicting enum **count** (says "24 pgEnums" but lists — and the source defines — 26); a couple of behavioural descriptions are mild simplifications. Nothing breaks a rebuild.

## Inaccuracies
| severity | doc claim | code reality | file:line |
|---|---|---|---|
| medium | "Enum catalog … **24** `pgEnum`s" (line 36-37) and "Stored DB enums: the **24** `pgEnum`s above" (line 114) | There are **26** `pgEnum`s. The main schema block defines 23 (`rank` … `telegram_announcement_status`) and the MCP block defines 3 (`mcp_client_auth_method`, `mcp_code_challenge_method`, `mcp_audit_outcome`) = 26. The doc's own catalog (lines 38-63) correctly lists all 26 — so the prose count is internally contradicted by its own list. | `grep -c pgEnum( schema.ts` = 26; schema.ts:31-211 (23) + 1223-1237 (3) |
| low | "`satisfyRequiredAction` flips a pending row to `completed` **only if** the completed version `meetsRequiredVersion` the required version" (line 77) | The version gate only fires when **both** `row.version` AND `completedVersion` are truthy; if either is null/absent it completes unconditionally. The doc's parenthetical "(older completion leaves gate open)" is right, but the unqualified "only if" overstates: a versionless gate or versionless completion always satisfies. | activations.ts:187-199 |

## Omissions
| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The doc lists `findUserByAuthId`, `findUserById`, `createCampUser`, etc. under burner-profile.ts but omits `setUserTelegramHandle` / `recordTelegramUserId` (they live in telegram.ts, which the doc does cover under "Telegram bot data layer" — so not misattributed, just not enumerated). No correctness impact. | telegram.ts:138-161 |
| low | `acknowledgeDelivery` also stamps `readAt` (acknowledge implies read); the doc (line 94, 243) describes it as acknowledge-only and does not mention the simultaneous `readAt` write. | broadcasts.ts:445 |

## Spot-confirmed
- All 26 enum member lists verbatim + declaration order: `rank ["captain","member"]` schema.ts:31; `approval_status ["pending","approved","rejected"]` :41; `team` 8-value list :51-60; `membership_tier ["full","build_week_only"]` :62; `recipe_status` 5-value :67; `recipe_source ["url","text","voice"]` :75; `reimbursement_status` 5-value :81; `platform ["web","ios","android"]` :89; `reimbursement_account_type ["sa","international"]` :91; `required_action_type ["questionnaire","acknowledgement","payment","profile_update"]` :99; `required_action_status ["pending","completed","waived","expired"]` :106; `questionnaire_scope ["everyone","team","team_leads","individual","opt_in"]` :114; `activation_status ["draft","open","closed"]` :122; `broadcast_kind ["announcement","team_message","lead_directive","reminder","system"]` :128; `broadcast_scope ["everyone","team","team_leads","drivers","individual"]` :136; `notification_channel ["push","in_app","both"]` :144; `push_delivery_status ["queued","sent","failed","skipped"]` :150; `broadcast_presentation ["acknowledge","popup","feed"]` :166; `task_status ["open","done","cancelled"]` :172; `inventory_update_status ["pending","approved","rejected"]` :182; `telegram_chat_kind ["main_group","announcement_channel"]` :191; `telegram_invite_status ["pending","used","expired","revoked"]` :200; `telegram_announcement_status ["queued","sent","failed"]` :208; `mcp_client_auth_method ["none","client_secret_basic","client_secret_post"]` :1223; `mcp_code_challenge_method ["S256","plain"]` :1229; `mcp_audit_outcome ["success","error"]` :1234. Every cited enum line number is exact.
- Table count = **33** (15 screened + 18 screenless, both group counts verified). All 34 cited table line numbers (users:220 … mcp_audit_log:1316) are exact.
- `rank` enum is `["captain","member"]` only; `team_lead`/`driver` are derived. The schema.ts:454 comment does say a lead "should also carry `users.rank = 'team_lead'`" and `team_lead` is NOT in the pgEnum — the doc's "stale/aspirational" flag is correct. The 3-value `Rank` zod ladder `["captain","team_lead","member"]` is at roles.ts:5.
- `isTeamLead` derives from `team_memberships.is_lead = true` (roster.ts:204-217); roster `isLead` subquery roster.ts:66-69. driver derived from `driver_profiles.intends_to_drive = true` (audience.ts:18, broadcasts.ts:73-75, mcp.ts:55-59). All confirmed.
- Account erasure: `lostCatNumber = max(...) + 1` account.ts:64; `sanitisedUserPatch` sets displayName="Lost Cat #N", authUserId=`deleted:<id>`, nulls profileImage/passport/saId/eft/emergencyContacts/telegramHandle+UserId/termsVersion+termsConsentedAt, sanitised=true, sanitisedAt=now, lostCatNumber (account.ts:26-43). DELETEs exactly the 12 child sets the doc lists incl. car_members (driver OR passenger via `or(...)`) and questionnaire_activation_targets (account.ts:74-117). Scrubs `reimbursements.account_details_encrypted` to `""` (account.ts:120-123). Note: `eft_details_encrypted` IS nulled in the patch — the doc says "nulls … EFT" — correct.
- `openActivation`: rejects `opt_in` with exact string "opt_in activations are not yet supported." (activations.ts:48), `PUSH_SCOPES = new Set(["everyone","team","team_leads","individual"])` (activations.ts:14), `onConflictDoUpdate` on `(userId, actionKey)` resetting status→pending, completedAt→null (activations.ts:111-124). `getPendingRequiredActions` filters status=pending AND blocking=true, ordered asc by createdAt (activations.ts:218-225).
- `meetsRequiredVersion`: `VERSION_RE = /^(.*)-v(\d+)$/`, same-base numeric `-vN` compare, else `completed >= required` lexicographic fallback (versions.ts:6-23). Exact.
- `validateResponses` semantics all confirmed: malformed payload → `{_root: "Malformed response payload"}` (questionnaire.ts:332); missing required → "This question is required" (line 363); slider range check (370-374); single_select/toggle/combobox option-match (376-381,416-428); multi_select filters to allowed, required→≥1 (383-390); short/long text ≤maxLength (392-397); date strict `/^\d{4}-\d{2}-\d{2}$/` + `Date.parse` (399-408); scale step match (410-413); image any string (430-433); unknown keys dropped (only catalogue questions iterated, 337-348). `diffResponses` treats multi-select as sets via sorted compare + empty/absent equal (277-289, 298-316).
- crypto.ts: `ALGO="aes-256-gcm"`, `IV_LEN=12`, `TAG_LEN=16`, `KEY_SALT="camp404-pgcrypto-v1"`, `PGCRYPTO_KEY` ≥16 else throw, base64(iv‖tag‖ciphertext), `decryptOrNull` swallows→null (crypto.ts:26-79). "pgcrypto" comments are stale; impl is Node AES-256-GCM — doc's flag correct.
- push: `chunk(tokens, 500)` push.ts:102; `PRUNE_CODES` = exactly the 3 FCM codes (push-status.ts:6-10); `deliveryPushStatus` skipped/sent/failed (push-status.ts:35-41); drain reads `pushStatus='queued' AND channel IN ('push','both')` (push.ts:143-145); status write conditional on row still queued (push.ts:188-189). `in_app` never drained — correct.
- invite codes: `findUsableInviteCode` + `consumeInviteCode` both guard `revokedAt IS NULL` AND (no expiry OR expires>now) AND (no cap OR maxUses>useCount); consume increments in same guarded UPDATE returning null on race (invite-codes.ts:34-80); `findInviteCodeByCode` ignores state (119-129); `createInviteCode` lowercases invitedEmail (103). All exact.
- broadcasts: `isOwnedAnnouncementDraft` gates kind='announcement' AND scope='everyone' AND publishedAt IS NULL AND senderId=author (broadcasts.ts:36-44); `publishAnnouncement` claims draft in UPDATE + `onConflictDoNothing` (228-290); author excluded via `computeAudience` senderId filter; `dispatchDueBroadcasts` drains publishedAt NOT NULL, dispatchedAt IS NULL, sendAt NULL/≤now, claims by flipping dispatched_at (306-386); `markRead` only snapshotted-still-unread ids (516-529); `countUnread` = readAt IS NULL (496-508); `acknowledgeDelivery` owner-scoped, presentation='acknowledge', not-yet-acked (437-457). Confirmed.
- `computeAudience` PURE: excludes isSystem + sanitised + sender, de-dups, team-scope-no-team→[] (audience.ts:29-64). Exact.
- Drivers: `createHttpDb` (no transactions), `createPooledDb` (WebSocket pool), `BUILD_PLACEHOLDER_URL="postgres://build:build@localhost:5432/build?sslmode=disable"`, `NEON_LOCAL_PROXY=1`→insecure ws `:5433/v1`, `Database = ReturnType<typeof createHttpDb>` (index.ts:13-44). Exact.
- zod mirrors: `Rank` roles.ts:5, `Team` 8-value roles.ts:10, `DietaryTag` 10-value member.ts:6, `MembershipTier` member.ts:26, `AnnouncementPresentation` announcement.ts:8, `RecipeStatus/Source` recipe.ts:4/13, `ReimbursementStatus/AccountType` reimbursement.ts:4/13, `VoiceIntentName` 5-value voice-intent.ts:3. Question kinds = 10, page kinds = 2 (verified by grep). `required` defaults true/false split exactly as doc states (questionnaire.ts:17,29,41,51,61,71,88,104,121,133). `slider.step` 1, `short_text.maxLength` 120, `long_text.maxLength` 1000.
- input schemas: `ComposeAnnouncementInput` title 1-120, body 1-5000, presentation default "acknowledge" (announcement.ts:23-27); `ReimbursementInput` amount>0, currency `/^[A-Z]{3}$/`, description 1-500, team default null, SWIFT/BIC min8 max11, SA accountNumber min4 / branchCode min3, refine requires receipt||item (reimbursement.ts:18-61); `SignupInput` saIdNumber `/^\d{13}$/`, emergencyContacts min1 max2, contact name min1/phone min3/relationship min1 (member.ts:20-45). All exact.
- MCP: `getMcpScopeRows` reads user+memberships+driver intent, scope derivation noted as living in app `lib/mcp/scope.ts` (mcp.ts:11-66); `appendMcpAuditLog`/`touchAccessToken` swallow errors (mcp.ts:89-92,111-113); `findActiveAccessToken` requires revokedAt IS NULL AND expiresAt>now (mcp.ts:120-133); `mcp_audit_log.client_id` not an FK (schema.ts:1323-1325). Exact.
- External identity: no `account`/`session`/`verification`/`user` pgTable in the package (grep confirms NONE); join via `users.auth_user_id` (schema.ts:222), header comment schema.ts:19-23. Exact.
- Defaults sampled & confirmed: users rank `member`/is_system false/dues_paid false/skills []/previous_afrikaburns 0/first_time false/approval_status `approved`/sanitised false/ai_data_consent false; invite_codes use_count 0/requires_approval false; workshops capacity 20; team_budgets currency "ZAR"; broadcasts channel `both`/presentation `feed`; notification_deliveries push_status `queued`; documents markdown ""/version 1 (int)/published false. All match schema.ts.
- Non-table read DTOs all exist as named exports in their cited files (ReferralUser relations.ts, CampManagementMember/CampMemberDetail roster.ts, PendingRequiredAction activations.ts, AnnouncementSummary/DraftInput/PendingAcknowledgement/InboxItem broadcasts.ts, InviteCodeRow invite-codes.ts, McpScopeRows mcp.ts, Telegram*Row telegram.ts, QueuedPushDelivery/PushDrainResult push.ts, QuestionnaireEditRow questionnaire-edits.ts, SplitId id-documents.ts). Confirmed.
- telegram.ts `listDueAnnouncements` default limit 25 (telegram.ts:185); questionnaire-edits.ts `listQuestionnaireEdits` default limit 20 (questionnaire-edits.ts:42); `listPendingInvitesForUser` status=pending AND (no expiry OR expiry≥now) (telegram.ts:236-247). Exact.

## Low-confidence / could-not-verify
- "Non-captain codes ALWAYS `requires_approval=true` (app rule, schema.ts:332-336)" — the schema column comment at 332-336 *states* this rule, but the actual enforcement lives in app route code, not in this unit's covered files. The doc correctly attributes it as an "app rule" so this is a fair characterization; the runtime enforcement was not traced here.
- "`telegram_user_id` captured from `chat_member` webhook" — the webhook handler is in apps/web (out of this unit's covered files); `recordTelegramUserId` (telegram.ts:138) writes it, but the webhook trigger path was not traced.
- The "Submitting/pending" and "Success/completed" state groupings (doc lines 104-105) are descriptive cross-references to other units' global-state rows; they accurately name the enum members but the actual UI state mapping is owned elsewhere and not verifiable from the data layer alone.
