# Verification — 28 theming-tokens

**Verdict:** minor-gaps  ·  checked 41 claims, verified 39.
The doc is unusually faithful: every hex mirror, OKLCH token value, line number, and consumption claim I spot-checked matched the source verbatim, including the "ugly truth" hex-inconsistency and the orphan-token analysis. The only real defect is a self-contradictory token count — the prose says "21 OKLCH colour tokens" while the table, the equality note, and the business-rules section all correctly say 19 (source has exactly 19). That single count error recurs in two places.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| medium | "It defines **21** OKLCH colour tokens (dark-only…)" (Purpose) and "**21** colour tokens, all expressed in OKLCH" (Features) | globals.css defines exactly **19** `--color-*` tokens (+1 `--radius`). The doc's own Enums table (line 85: "19 colour tokens above"), the table itself, and the business-rules section (line 127: "all 19 colour tokens + --radius") correctly say 19. "21" is wrong and internally contradicts the rest of the doc. | doc lines 15, 22 vs `packages/ui/src/styles/globals.css:12-38` (grep count = 19) |
| low | og-image constants located at "lines 13–17" | Correct as written; constants are at lines 13–17. (No error — included only because line cites were exhaustively re-checked; this one passed.) | `apps/web/lib/og-image.tsx:13-17` |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The doc names `NeonAuthUIProvider` as the carrier of next-themes but does not note that `providers.tsx` passes it NO theme-related props at all (no `forcedTheme`/`defaultTheme`/`themeProps`) — so the "dark" class behavior is entirely internal to the `@neondatabase/auth` bundle and not observable from app code. The doc's hedge ("the dark class comes from the auth provider's bundled next-themes") is accurate but the total absence of any theme prop in app source is worth stating. | `apps/web/app/providers.tsx:14-23` |
| low | `og-image.tsx` exports are described, but the doc omits that the giant-404 `<Glitch404 fontSize={320} split={14} glow={80} />` is the only call that passes a truthy `glow` (the square-icon call omits it, defaulting 0) — i.e. the glow shadow only ever renders on the share card, never the app icon. Minor; consistent with the "Satori chokes on undefined" guard the doc does cover. | `apps/web/lib/og-image.tsx:89, 124-127, 138, 172-173` |

## Spot-confirmed
- `@import "tailwindcss";` (line 1), `@source "../components/**/*.{ts,tsx}";` (line 7), `@theme {` (line 9), `--radius: 0.625rem;` (line 39) — all at the exact lines claimed. `globals.css`
- Exactly ONE `@theme` block and NO `:root`/`.dark` color override anywhere in `packages/ui/src` (grep confirmed clean of build artifacts). `packages/ui/src/styles/globals.css:9`
- Exactly ONE `@keyframes` (`cp-layer-in`, lines 59–68: `opacity 0→1`, `transform scale(0.98)→scale(1)`); consumed only at `control-panel.tsx:138` as `animate-[cp-layer-in_200ms_ease-out]`. `globals.css:59-68`, `packages/ui/src/components/control-panel.tsx:138`
- `var(--radius)` consumed in exactly ONE place outside globals.css: `control-grid.tsx:149` (`rounded-[var(--radius)]`). `packages/ui/src/components/control-grid.tsx:149`
- `@layer base` lines 42–56: `*` → `border-[color:var(--color-border)]`; `body` → `bg-[color:var(--color-background)] text-[color:var(--color-foreground)]` + `font-feature-settings: "rlig" 1, "calt" 1;`; date/time picker indicators → `filter: invert(0.85)`. `globals.css:42-55`
- All 19 OKLCH token values match the table digit-for-digit, including the deliberate equalities: `card`==`popover`==`oklch(0.26 0.08 295)`; `card-foreground`==`popover-foreground`==`oklch(0.97 0.02 330)`; `border`==`input`==`oklch(0.35 0.1 305)`; `ring`==`primary`==`oklch(0.65 0.27 340)`; `destructive-foreground`==`oklch(0.98 0 0)` (chroma 0). `globals.css:12-38`
- Hex-mirror table verified verbatim across all six files: `#0d061e` in og-image/manifest(×2)/layout/icon.svg/global-error/capacitor; `#f7ecf3` in og-image + icon.svg; `#f6eef7` in global-error (the DIFFERENT foreground hex — inconsistency confirmed); `#b29ab0` og-image; `#ef1ec1` global-error button; `#ff008c`/`#00dcff` icon.svg solid glyphs; `rgba(255, 0, 140, 0.92)`/`rgba(0, 220, 255, 0.92)` og-image MAGENTA/CYAN. `og-image.tsx:13-17`, `manifest.ts:12-13`, `layout.tsx:37`, `icon.svg:3,14-16`, `global-error.tsx:36,37,64`, `capacitor.config.ts:18`
- global-error button comment "Mirrors --color-primary oklch(0.65 0.27 340)…" present verbatim. `global-error.tsx:62`
- `SHARE_SIZE = { width: 1200, height: 630 } as const`, `SHARE_CONTENT_TYPE = "image/png"`, `SHARE_ALT = "Camp 404 — a glitched 404 logo on a midnight-violet field. A calm command centre for a chaotic desert."` — exact. `og-image.tsx:19-22`
- Share card: `Glitch404 fontSize={320} split={14} glow={80}`; kicker `letterSpacing: 18` + `color: MUTED`; error line `letterSpacing: 8` + `textShadow: -2px 0 0 ${MAGENTA}, 2px 0 0 ${CYAN}`; tagline `fontSize: 32, letterSpacing: 2`; radial bloom `radial-gradient(circle at 50% 46%, rgba(255,0,160,0.20), rgba(13,6,30,0) 58%)`. `og-image.tsx:48,65,75,79,89,96-97`
- Square icon: `fontSize = Math.round(size * 0.46)`, `split = Math.max(2, Math.round(size * 0.035))`, `glow` default `0`; glyph `fontWeight: 800`, `letterSpacing: -Math.round(fontSize * 0.05)`, `lineHeight: 1`; glow shadow `0 0 ${glow}px rgba(255,0,200,0.32)` only when `glow` truthy ("Satori chokes on textShadow: undefined"). `og-image.tsx:125-126,138,146-148,172-173`
- `apple-icon.tsx`: `size = { width: 180, height: 180 }`, `contentType = "image/png"`, calls `renderSquareIcon(180)`. `opengraph-image.tsx` and `twitter-image.tsx` both call `renderShareImage()` and re-export `SHARE_ALT`/`SHARE_CONTENT_TYPE`/`SHARE_SIZE`. `apple-icon.tsx:5-9`, `opengraph-image.tsx:14-15`, `twitter-image.tsx:13`
- icon.svg attributes: `viewBox="0 0 64 64"`, `rx="12"`, `font-family="ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace"`, `font-size="27"`, `font-weight="900"`, `letter-spacing="-1.5"`, glyph x-offsets `30.5 / 33.5 / 32`, all `y="42"`. `icon.svg:1-16`
- manifest: `name`/`short_name` "Camp 404", `description` "A calm command centre for a chaotic desert.", `start_url: "/"`, `display: "standalone"`, `background_color`/`theme_color` `#0d061e`; icons `/icon.svg` (`any`,`image/svg+xml`,`any`) and `/apple-icon` (`180x180`,`image/png`,`maskable`). `manifest.ts:7-27`
- layout: `viewport.themeColor = "#0d061e"`, imports `@camp404/ui/styles.css`, `<html lang="en" suppressHydrationWarning>`. `@camp404/ui/styles.css` resolves to `./src/styles/globals.css`. `layout.tsx:5,37,48`, `packages/ui/package.json:10`
- components.json: `style: "new-york"`, `rsc: true`, `tsx: true`, `tailwind.css: "src/styles/globals.css"`, `baseColor: "neutral"`, `cssVariables: true`, `prefix: ""`, `iconLibrary: "lucide"`; aliases components→`@camp404/ui/components`, utils→`@camp404/ui/lib/utils`, hooks→`@camp404/ui/hooks`, ui→`@camp404/ui/components`. `components.json:3-19`
- `cn() = twMerge(clsx(inputs))` (clsx + tailwind-merge). `packages/ui/src/lib/utils.ts:4-6`
- Orphan check: NONE of the 19 tokens are orphaned. The doc's flagged "utility-only" set (`card-foreground`, `popover`, `popover-foreground`, `input`, `accent`, `accent-foreground`, `destructive-foreground`) each resolve via Tailwind utility classes (e.g. `bg-popover` ×3, `border-input` ×4, `bg-accent` family ×9, `text-card-foreground` ×1, `*-destructive-foreground` ×1) with zero raw `var()` refs — exactly as the doc states. All core tokens (`background`/`foreground`/`primary`/`border`/`muted`) consumed via both utilities and `var()`. (grep over `packages/ui/src`, `apps/web/{app,lib,components}`, `apps/mobile`)
- capacitor splash `SplashScreen.backgroundColor: "#0d061e"`. `apps/mobile/capacitor.config.ts:18`

## Low-confidence / could-not-verify
- The claim that next-themes sets `class="dark"` on `<html>` (and that this is "cosmetic" with no CSS effect) lives inside the `@neondatabase/auth/react/ui` package bundle, not in app source. App code passes no theme props (`providers.tsx`), so I confirmed there is no app-level `defaultTheme`/`forcedTheme`/`prefers-color-scheme`/`.dark` override — but the actual class-setting and its lack of CSS effect depend on upstream-package internals I did not crack open. The doc's framing is consistent with everything observable in-repo.
- Whether `@source "../components/**/*.{ts,tsx}"` is strictly load-bearing for utility generation in the consuming build is a Tailwind-v4 build-pipeline behavior; I confirmed the directive exists and the comment in source asserts exactly the doc's rationale, but did not run a build with it removed to prove the failure mode.
