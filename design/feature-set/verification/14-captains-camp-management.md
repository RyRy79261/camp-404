# Verification — 14 captains-camp-management

**Verdict:** accurate  ·  checked 78 claims, verified 76.
The doc is a faithful, digit-exact account of the captains' camp-management surface across all seven covered files plus the followed imports. Every gate, view-model rule, enum, status-precedence branch, dead-field, and crypto constant was confirmed against source. The only defects are two trivial line-number citations; no behavioral claim is wrong.

## Inaccuracies
| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | "`CampManagementMember.duesPaid` and `.membershipTier` … fetched by `getCampManagementRoster` (roster.ts:58-59, **98-99**)" — implies the map lines are 98-99 | The SELECT lines 58-59 are correct (`duesPaid`, `membershipTier`); but in the `.map()` the assignments are `duesPaid: r.duesPaid` at line **101** and `membershipTier: r.membershipTier` at line **102**. Lines 98-99 are `approvalStatus`/`isLead`. The underlying claim (fetched-but-unread) is still correct. | roster.ts:58-59, 101-102 |
| low | Cites `STATUS_LABEL` (camp-roster.ts:43-49) as if exported alongside the type — it is a module-private `const` (not exported); roster.tsx redefines its own `STATUS_STYLE`. | `const STATUS_LABEL` is private to camp-roster.ts (consumed only via `statusLabel` on the row); not an exported enum. Cosmetic — the values/lines are correct. | camp-roster.ts:43-49 |

## Omissions
| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The Approve button shows the spinner (`Loader2`) on `isPending` but Reject does not swap its icon — doc says "Approve swaps its icon for a spinning `Loader2`" (correct) and that both are disabled (correct); it does not note Reject keeps its `X` icon while pending. Minor presentation detail. | camp-management-roster.tsx:467, 479-483 |

## Spot-confirmed
- `export const dynamic = "force-dynamic"` (page.tsx:10); `metadata = { title: "Camp management — Camp 404" }` (page.tsx:12). ✓
- Gating order: `getAuthenticatedUserOrRedirect()` → `ensureCampUser` → `if (!hasCampAccess(campUser, authUser.primaryEmail)) redirect("/signup/required")` → `if (!isApproved(...)) redirect("/pending-approval")` (page.tsx:20-27). ✓ auth helpers exist (auth.ts:25,40).
- `isCaptain = campUser.rank === "captain"` (page.tsx:29); `rows = isCaptain ? (await getCampManagementRoster()).map(toRosterRow) : []` (page.tsx:31-33). Locked view gets `[]`. ✓
- Back button: ghost/sm, `<a href="/">`, `ChevronLeft`, label "Captains" (page.tsx:37-41). H1 "Camp management" + exact descriptive paragraph (page.tsx:43-48). Container `mx-auto max-w-5xl px-6 py-10` (page.tsx:36). `<CampManagementRoster rows={rows} locked={!isCaptain} />` (page.tsx:51). ✓
- `STATUS_STYLE` (roster.tsx:43-49): ready emerald, onboarding amber, awaiting_approval sky, rejected rose, pending rose (rejected & pending share rose). ✓
- Filter strip `!locked` guard (roster.tsx:113); two-button toggle "All ({rows.length})" / "Awaiting approval" + badge when `awaitingCount > 0` (roster.tsx:116-145); active `bg-muted text-foreground`, inactive `text-muted-foreground hover:text-foreground`. ✓
- Search `<Input>` with leading `Search` icon, placeholder "Search name, team, country…", `aria-label="Search the roster"` (roster.tsx:146-155). Search filters on displayName/rankLabel/country/team (roster.tsx:91-103). ✓
- 7-column table headers: Member, Rank, Status, ShieldCheck "Questionnaires", Car "Driver", Flag "In SA", MapPin "Country" (roster.tsx:169-191). `colSpan={7}` empty cell (roster.tsx:200). ✓
- Rank pill colour: captain `bg-primary/15 text-primary`; else isLead `bg-sky-500/15 text-sky-400`; else `bg-muted text-muted-foreground` (roster.tsx:229-234). ✓
- Status pill `title` = `${n} outstanding action${n===1?"":"s"}` when `pendingRequiredActions > 0`, else `undefined` (roster.tsx:245-251). ✓ Singular/plural exact.
- Row `onClick={() => setSelectedId(r.id)}`, `cursor-pointer … hover:bg-muted/30` (roster.tsx:212-215). ✓
- Empty-state messages: awaiting→"Nobody is awaiting approval."; rows.length===0→"No members have signed up yet."; else "No members match your search." (roster.tsx:203-207). ✓
- `YesNo` (roster.tsx:58-71): true→emerald `Check` + sr-only "{label}: yes"; false→`text-muted-foreground/50` `Minus` + sr-only "{label}: no". sr-only labels: "Required questionnaires complete", "Registered as a driver", "In South Africa" (roster.tsx:259,263,266). ✓
- `teamLabel` splits `_`, title-cases, joins space (roster.tsx:51-56). ✓
- Locked: table wrapper `pointer-events-none select-none opacity-40 blur-[2px]` + `aria-hidden={locked}` (roster.tsx:160-164); `<PlaceholderRows>` 6×7 `h-3 w-16 rounded bg-muted` (roster.tsx:527-541); overlay `Lock` icon + "Captain access only" + exact body (roster.tsx:278-289); `MemberModal` NOT mounted when locked (`!locked` guard roster.tsx:291). ✓
- Modal: `open = row != null` (roster.tsx:321); `DialogContent` `max-h-[85vh] … sm:max-w-lg` (roster.tsx:378). Detail fetch effect: resets tab→"overview", clears actionError, sets loading, calls `getMemberDetailAction(rowId)`, `cancelled` flag for stale discard (roster.tsx:326-343). ✓
- Header: `DialogTitle` = `row?.displayName ?? "Member"`; `DialogDescription` = `member.approvalSummary` else "Loading…" (roster.tsx:380-383). Two tabs `["overview","profile"]`, `capitalize`, active `border-b-2 border-primary` (roster.tsx:388-402). ✓
- Loading body spinning `Loader2` (roster.tsx:406-409); error body `text-destructive` showing `detail.message` (roster.tsx:411-415). Overview: optional `<img>` `h-20 w-20 rounded-full`, `alt=""` + `<DetailList items={member.overview}>` (roster.tsx:416-427). Profile: empty→"No questionnaire answers on record yet.", else uppercase section title + DetailList (roster.tsx:428-445). ✓
- `DetailList` (roster.tsx:504-524): empty→"Nothing recorded."; else `<dl>` dt muted / dd font-medium. ✓
- Actions footer (roster.tsx:451-498): "Actions" caption; Approve/Reject only while `isAwaiting` (`member?.approvalStatus === "pending"`, roster.tsx:369,461); Ping always `disabled`, `title="Coming soon — nudge this member to check the app"`, `ThumbsUp` (roster.tsx:488-496). `actionError` `role="alert" text-destructive` above buttons (roster.tsx:452-456). ✓
- Client `decide()` (roster.tsx:345-366): on success optimistically patches `member.approvalStatus = decision` then `router.refresh()`; `useTransition` `isPending` disables both buttons. ✓
- `getMemberDetailAction` (actions.ts:46-68): `requireCaptain()` gate (49-50); null detail→`{ok:false,error:"Member not found."}` (52-53); decrypt `passportEncrypted`/`saIdEncrypted`, idType passport→sa_id→null (58-64); `mergeIdNumber` (65); returns `presentMemberDetail({...detail, responses})` (67). ✓
- `decideApprovalAction` (actions.ts:75-96): gate (79-80); decision whitelist→"Unknown decision." (82-84); self-decision→"You can't decide on your own account." (85-87); `decideUserApproval({userId,status,decidedByUserId})` (89-93); `revalidatePath("/captains/camp-management")` + `{ok:true}` (94-95). ✓
- `decideUserApproval`→ real backend `setUserApproval` stamps `approvalStatus`, `approvalDecidedByUserId`, `approvalDecidedAt`, `updatedAt` (users.ts:368-370; burner-profile.ts:69-84). ✓
- `requireCaptain` (actions.ts:30-43): `getAuthenticatedUser()` null→"Not signed in."; `!hasCampAccess`→"Your account isn't camp-active yet."; `rank !== "captain"`→"Captain access only."; else `{ok:true,captainId:campUser.id}`. ✓ Error strings exact.
- `getCampManagementRoster` (roster.ts:48-110): country `responses->>'country'`; intendsToDrive `coalesce(driver_profiles.intends_to_drive,false)`; isLead `exists(... team_memberships ... is_lead=true)`; teams `coalesce(array_agg(tm.team order by tm.team),'{}')`; pendingRequiredActions `count(*)::int … status='pending' and blocking=true`; onboardingComplete = `completedAt != null`; `WHERE is_system=false AND sanitised=false` (89-91); `ORDER BY display_name ASC` (92). ✓ All SQL verbatim.
- `getCampMemberDetail` (roster.ts:141-198): aliased `decider`/`inviter` joins, burner_profiles + invite_codes joins; returns null if no row (180); reads raw ciphertext (not decrypted); `responses` defaults `{}` (190). ✓
- `toRosterRow` status precedence (camp-roster.ts:60-71): !onboardingComplete→onboarding; pending→awaiting_approval; rejected→rejected; requiredComplete→ready; else→pending. ✓ `requiredComplete = pendingRequiredActions === 0` (61); `awaitingApproval = approvalStatus === "pending"` (62). Display-name fallback "Unnamed burner" (75). `inSouthAfrica = country === "ZA"` (92). Country resolved via `COUNTRY_NAME.get() ?? raw` (90). ✓
- `rankLabel` (camp-roster.ts:97-101): captain→"Captain"; isLead→"Team Lead"; else "Member". ✓
- `presentMemberDetail` (member-detail.ts:111-176): displayName fallback "Unnamed burner" (168); modal `rankLabel` = captain→"Captain" else "Member" — does NOT express Team Lead (169) ✓ (asymmetry real); `profileImageUrl` from `responses["profile.image"]` if string (116-119); overview order Country→Joined→Onboarding→Invite code ("— (founder / god account)")→Invited by→Invite note (121-149). ✓
- `profileSections` (member-detail.ts:153-164): iterate `QUESTIONNAIRE.pages`, skip `kind !== "questions"`, drop pages with zero answered questions, title = page.title. ✓
- `renderAnswer` (member-detail.ts:46-85): null/empty→null; `image`→null; `multi_select`→comma-joined labels (null if empty); `single_select`/`scale`/`toggle`/`combobox`→optionLabel (scale uses `steps`, others `options`); `slider`/`date`/`short_text`/`long_text`/default→`String(raw)`. ✓ `scale` carries `steps` confirmed in types (questionnaire.ts types:80-85) and questionnaire.ts:199,201.
- `describeApproval` (member-detail.ts:87-109): approved→"Approved by {name} on {date}"/"Approved"; rejected analogously; pending/default→"Awaiting a captain's decision"; `Intl.DateTimeFormat("en-ZA",{dateStyle:"medium"})` (41). ✓
- Enums: `RosterStatus` (camp-roster.ts:8-13) 5 members ✓; `STATUS_LABEL` values (43-49) ✓; `Filter` `"all"|"awaiting"` (roster.tsx:73) ✓; modal tabs (roster.tsx:317,388) ✓; `DetailState` (roster.tsx:303-306) ✓; `rankEnum ["captain","member"]` (schema.ts:31) ✓; `approvalStatusEnum ["pending","approved","rejected"]` (schema.ts:41-45) ✓; `teamEnum` 8 values exactly as listed (schema.ts:51-60) ✓; `membershipTierEnum ["full","build_week_only"]` (schema.ts:62-65) ✓.
- id-documents: `ID_NUMBER_KEY="id.number"`, `ID_TYPE_KEY="id.type"` (7-8); `idColumnsFor` sa_id→saIdEncrypted else passportEncrypted (43-49); `mergeIdNumber` returns responses unchanged if no idNumber, keeps id.type (28-38). `id.number` is a `short_text` question (questionnaire.ts:121-122). ✓
- Crypto: AES-256-GCM, `KEY_SALT="camp404-pgcrypto-v1"`, key from `PGCRYPTO_KEY` (≥16 chars), base64(iv‖tag‖ciphertext), IV_LEN 12, TAG_LEN 16, `decryptOrNull` swallows errors→null (crypto.ts:26-29,36,53,72-79). ✓
- `QUESTIONNAIRE.version = "2026.05.29-v8"` (questionnaire.ts:60); all 11 page titles match verbatim ("Add a profile photo", "About you", "A bit about you", "Your ideas for this year's burn", "Team interests", "Cooking competency", "Hardware competency", "Leadership & logistics", "Burn history", "Coming to burn this year?", "Dietary requirements"). ✓
- Special country "ZA" pinned top of COUNTRIES, ISO alpha-2 codes (countries.ts:30-31). ✓
- Schema field names on `users` (schema.ts:220-303): all listed columns + WRITE set match `setUserApproval`. ✓ Table line ranges for burner_profiles (352), driver_profiles (393), team_memberships (446), required_actions (570) confirmed.
- DEAD/orphaned: Ping permanently disabled (roster.tsx:488-496) ✓; `isTeamLead` (roster.ts:204-217) used only by home/control-panel via users.ts→page.tsx:14,76, NOT by camp-management ✓; `duesPaid`/`membershipTier`/`onboardingVersion` fetched but unread by view-models ✓ (grep returned zero hits in camp-roster.ts/member-detail.ts/roster.tsx); `RosterRow.driverProfileComplete` derived (camp-roster.ts:88) but Driver column uses `r.isDriver` (roster.tsx:263) ✓.

## Low-confidence / could-not-verify
- "Approving unblocks the app on their next load; rejecting holds them at the blocking screen" — the camp-management code stamps `approvalStatus` correctly; the *downstream* unblocking/blocking behavior lives in `isApproved` (users.ts:231-236, confirmed) + the pending-approval gate elsewhere (not in this unit's files). The local persistence is verified; the cross-surface enforcement is asserted by the doc but enforced in unit 05.
- "Unknown decision." action error is validated server-side (actions.ts:82-84) but unreachable from the only client caller, whose `decide()` is typed to `"approved" | "rejected"` (roster.tsx:345). Doc lists it as a possible action-error; technically a defensive branch, not a user-facing state. Not flagged as an inaccuracy since the action genuinely can return it.
- `revalidatePath` + `router.refresh()` re-fetch behavior asserted as updating the status pill — the data path is correct, but the visual refresh depends on Next.js runtime cache semantics not observable from static source.
