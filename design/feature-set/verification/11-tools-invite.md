# Verification — 11 tools-invite

**Verdict:** accurate  ·  checked 78 claims, verified 77.
The doc is an unusually faithful, digit-exact mirror of the real source: every line:line citation I sampled landed on the cited code, all enum/numeric constants (350ms, 30/60_000, 8 retries, 1500ms, MAX_USES_LIMIT=100, 3–48) match verbatim, and the two "ugly truths" (test/prod existence mismatch, client/server hint divergence) are real and correctly flagged. The only defect is one cosmetic mislabel of an export style; no high- or medium-severity inaccuracies.

## Inaccuracies
| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | `InviteForm` is a "default export-ish client component" (Sub-components) | `InviteForm` is a plain **named** export (`export function InviteForm(...)`); there is no default export in the file. The whole module is `"use client"`, so "client component" is right, but "default export-ish" is wrong. | apps/web/app/tools/invite/invite-form.tsx:1, 30 |

## Omissions
| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The check route imports both `isE2ETestMode` (from `@/lib/test-mode`) and `testStore`. The doc mentions the E2E branch but not the explicit `isE2ETestMode()` import/guard wiring — fully covered in spirit, just not the import detail. | apps/web/app/api/tools/invite/check/route.ts:9-10, 57 |
| low | `createInviteCode` insert also writes schema defaults via `?? null`: `expiresAt: input.expiresAt ?? null` and `requiresApproval: input.requiresApproval ?? false`. Doc covers the columns this surface sets but doesn't note the DAL's fallback defaults explicitly (harmless; behavior is as described). | packages/db/src/invite-codes.ts:101, 104 |

## Spot-confirmed
- `export const dynamic = "force-dynamic"` at page.tsx:8; `metadata = { title: "Invite — Camp 404" }` at page.tsx:10. ✓
- Page gating: `getAuthenticatedUserOrRedirect()` + `ensureCampUser(authUser)` (page.tsx:13-14); `!hasCampAccess → redirect("/signup/required")` (page.tsx:15-17); `!isApproved → redirect("/pending-approval")` (page.tsx:18-20). ✓
- `hasCampAccess` = `isGodEmail(email) || !!user.inviteCode` at users.ts:219-224 (doc said 219-224). ✓ `isApproved` = `isGodEmail(email) || approvalStatus === "approved"` at users.ts:231-236. ✓
- Layout `<main className="mx-auto max-w-xl px-6 py-10">` (page.tsx:23) — confirmed `max-w-xl`, and tools/page.tsx:61 uses `max-w-2xl` (so the doc's "wider than global max-w-lg" framing is plausible; the immediate sibling hub is even wider). ✓ Ghost back-button "Tools" + `ChevronLeft` → `/tools` (page.tsx:24-28). ✓
- `<InviteForm isCaptain={campUser.rank === "captain"} />` at page.tsx:29 — sole rank input. ✓
- Title "Invite a member" (invite-form.tsx:107); captain/member description branch text verbatim (invite-form.tsx:109-111). ✓
- `multiUse = isCaptain && Number(maxUses) > 1` (invite-form.tsx:39); email label flips "Lead recipient's email (optional)" vs "Their email address" and `required={!multiUse}` (invite-form.tsx:118, 124). ✓ Placeholder `sara@example.com`, `type="email"`, `autoComplete="off"` (invite-form.tsx:123-126). ✓
- Note field: label "Why you're inviting them (optional)", `<Textarea rows={3}>`, placeholder "Kitchen lead from last burn; great with sourdough." (invite-form.tsx:131-137). ✓
- Captain block rendered only when `isCaptain`; else muted notice "Anyone who signs up with this code will need a captain's approval before they can use the app." (invite-form.tsx:140-152). ✓
- Code input: `name="code"`, `value={code}`, `onChange` lowercases (`e.target.value.toLowerCase()`), `spellCheck={false}`, `autoComplete="off"`, `className="font-mono"` (invite-form.tsx:157-165); seeded once via `useState(() => generateInviteCode())` (invite-form.tsx:31). ✓ Shuffle outline icon-button, aria-label "Generate a new silly code", re-rolls (invite-form.tsx:166-174). ✓
- Error banner `role="alert"` destructive box gated on `result && !result.ok` showing `result.error` (invite-form.tsx:179-186). ✓
- Submit "Create invite", `className="w-full"`, `isPending` → `<Loader2 className="animate-spin"/> Creating…` (invite-form.tsx:188-205). Disabled set = `isPending || checking || taken || invalid` — NOT idle/available (invite-form.tsx:190-195). ✓
- `useActionState(createInviteAction, null)` (invite-form.tsx:41-44); on `result?.ok` swaps to `<SuccessPanel>` (invite-form.tsx:93-102). ✓
- Effect on `[code]`: empty→idle (49-51); `!isSyntacticallyValidCode`→`{invalid, hint:"3–48 chars, lowercase letters / digits / hyphens."}` (53-58, trailing period, no "(no spaces)"); checking→debounce 350ms→fetch with AbortController (60-86); body map: `available`→available, `reason==="taken"`→taken, `reason==="invalid"`→`{invalid, hint: body.hint ?? "Invalid code."}`, else→idle; AbortError swallowed, other errors→idle (73-85); cleanup `ctrl.abort()` + `clearTimeout` (87-90). ✓
- Check route `runtime = "nodejs"` (route.ts:12); 401 `{error:"unauthorized"}` for no user (18-22); rate key `invite-check:${user.id}`, `limit: 30`, `windowMs: 60_000`, 429 `{error:"rate_limited"}` with `retry-after` header (24-37); `.trim().toLowerCase()` (40); empty→`{available:false, reason:"empty", hint: CODE_RULES_HINT}` (42-48); invalid→`reason:"invalid"` + hint (49-55); existence: E2E `testStore.findUsableInviteCode(raw)` else `findInviteCodeByCode(raw)` (57-59); existing→`reason:"taken"`, else `{available:true}` (61-64). ✓
- **Ugly truth #1 (test/prod existence mismatch) is real:** prod `findInviteCodeByCode` matches ANY row regardless of revoked/expired/exhausted (docstring + query, invite-codes.ts:111-129); E2E `testStore.findUsableInviteCode` returns null for revoked/expired/exhausted (test-store.ts:339-346). So a dead code reads "available" in E2E but "taken" in prod. ✓
- `generateInviteCode()` = `` `${pick(ADJECTIVES)}-${pick(NOUNS)}-${pick(NOUNS)}` `` (invite-words.ts:45-47), `pick` is `Math.random()` uniform (36-39); two independent noun picks → dup possible. ✓ Docstring warns collision-by-luck + caller MUST re-check (invite-words.ts:7-11). ✓
- `isSyntacticallyValidCode`: length 3-48 inclusive AND `CODE_PATTERN = /^[a-z0-9]+(-[a-z0-9]+)*$/` (invite-words.ts:55-60). ✓
- `CODE_RULES_HINT = "3–48 chars, lowercase letters / digits / hyphens (no spaces)."` (invite-words.ts:62-63). ✓ Divergence from client hint string confirmed (client at invite-form.tsx:56 lacks "(no spaces)"). ✓
- Word banks: ADJECTIVES len **50 with duplicate "merry"** (49 unique) — doc's "(50 entries incl. a duplicate "merry")" is exactly right (invite-words.ts:13-22). NOUNS len 50, all unique (invite-words.ts:24-34). ✓
- Action: `getAuthenticatedUser()` null→"Not signed in." (52-53); `ensureCampUser`+`hasCampAccess` fail→"Your account isn't camp-active yet." (55-58); `isCaptain` recomputed server-side (59); form reads string-guarded default "" (61-72); `preApprove = isCaptain && get("preApprove")==="on"`, `requiresApproval = !preApprove` (77-78); maxUses default 1, captain-only parse, must be integer in [1,100] else error "Max uses must be a whole number between 1 and 100." (80-93); email trim+lowercase, `emailRequired = maxUses === 1`, EMAIL_PATTERN check + required check both→"Enter a valid email address." (98-105); `note = noteRaw.trim() || null` (106); code trim+lowercase, valid-syntax check→"Invite code must be 3–48 chars, lowercase letters/digits/hyphens.", `findInviteCodeByCode`→`'${code}' is already taken.`, else `generateUnusedCode()` (111-125); insert with `assignedRank: null` (127-136); try/catch→"Couldn't save invite. Try a different code." (137-141); success `{ok:true, code, invitedEmail: email, maxUses, requiresApproval}` (143). ✓
- `generateUnusedCode`: loop `attempt < 8`, fallback `` `${generateInviteCode()}-${Date.now().toString(36)}` `` (actions.ts:146-155). ✓
- Action has **no** E2E/test-store import (grep confirms NONE) — always real-DB path. ✓
- `EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/` (actions.ts:24); `MAX_USES_LIMIT = 100` (actions.ts:28). ✓
- SuccessPanel: title "Invite ready"; desc `email ? "Share this code with ${email}. " : "Share this code. "`; `usesLine` single-use vs "Up to ${maxUses} people can sign up with it." (329-332); `approvalLine` "They'll need a captain's approval before they get access." vs " They're pre-approved — straight in after onboarding." (333-335); code box `font-mono text-lg`; Copy → `navigator.clipboard.writeText(code)`, "Copied" for 1500ms (347-361); "Send another" outline link → `/tools/invite` (362-364). ✓
- `CaptainOptions` (invite-form.tsx:257-315): "Captain options" heading; checkbox `name="preApprove"`; maxUses `type="number" min={1} max={100} className="w-28"`; dynamic helper "Up to N people…" vs "Single-use — once someone signs up, the code is spent." ✓
- `AvailabilityHint` (invite-form.tsx:212-255): returns null on empty/idle; Loader2 checking / Check emerald-400 available / X destructive taken+invalid; taken copy "<code> is already taken — pick another." ✓
- Schema `invite_codes` (schema.ts:312-342): `code` text PK (315); `created_by_user_id` uuid → users.id ON DELETE set null + index `invite_codes_created_by_idx` (316-318, 340); `note` text (319); `max_uses` integer nullable (320); `use_count` integer NOT NULL default 0 (321); `expires_at`/`revoked_at` nullable (322-323); `assigned_rank` rankEnum nullable (327); `invited_email` text (331); `requires_approval` boolean NOT NULL default false (336); `created_at` defaultNow (337). ✓ `rankEnum = pgEnum("rank", ["captain", "member"])` at schema.ts:31. ✓
- `InviteCodeRow` interface (invite-codes.ts:7-19) camelCase, `createdAt: Date`; `createInviteCode` re-lowercases via `input.invitedEmail?.toLowerCase()` (invite-codes.ts:103). ✓ `TestInviteCode` identical shape (test-store.ts:45-57). ✓
- Tools hub entry: `/tools/invite`, "Invite a member", description "Mint a single-use code to bring someone onto Camp 404.", `Mail` icon (tools/page.tsx:28-34). ✓
- Rate limiter is in-memory token bucket, per-process (rate-limit.ts:1-63). ✓
- No other consumers of `AvailabilityHint`/`CaptainOptions`/`SuccessPanel` outside invite-form.tsx (grep empty) — components are live, used by `InviteForm`, not orphaned. `findUsableInviteCode`/`consumeInviteCode` are live but belong to redemption (used elsewhere), correctly excluded as unit 03. ✓

## Low-confidence / could-not-verify
- The doc's claim that "No onboarding gate here" is an intentional product decision (an onboarding-incomplete-but-approved user could reach the page) is correct as a code observation — only invite + approval gates run in page.tsx — but whether this is *intended* vs an oversight is a design question I can't settle from source. The doc itself frames it as an "edge case", which is fair.
- "captain-tier codes can NOT be minted here — only from the CLI" — verified that this surface always inserts `assignedRank: null` (actions.ts:133); the existence of a CLI path that *does* set a non-null rank is asserted by the docstring (actions.ts:33-36) but the CLI itself is outside the files covered, so I did not open it.
- The `max-w-xl` "deliberate-or-stray width deviation" note: confirmed `max-w-xl` here vs `max-w-2xl` on the tools hub; "global max-w-lg" is asserted by the doc but I did not enumerate every page to confirm `max-w-lg` is in fact the global default.
