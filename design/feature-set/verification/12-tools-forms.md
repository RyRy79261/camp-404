# Verification — 12 tools-forms

**Verdict:** accurate  ·  checked 58 claims, verified 56.
The doc is a faithful, digit-exact map of the My-forms / form-replay surface: every file path, line range, copy string, enum, schema field, and registry fact checked out against live source, including the subtle ID-redaction and no-op-replay behaviours. The only blemishes are two trivial cosmetic citation slips; no high- or medium-severity defects, and the doc itself already self-flags the one genuine code-vs-comment contradiction (the `completedAt` overwrite).

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | Test-store serial pushed as `` `test-edit-${nextSerial++}` `` (`test-store.ts:281-297`). | Actual literal uses the module state object: `` `test-edit-${S.nextSerial++}` ``. The `S.` prefix is dropped in the doc. | apps/web/lib/test-store.ts:289 |
| low | "ID type values … `id-documents.ts:15,43-49`". | `idColumnsFor` spans lines 43-50 (closing brace at 50); line 15 is the `idType` JSDoc on the `SplitId` interface. Off-by-one tail on the range; substance ("passport" default \| "sa_id" \| null) is correct. | packages/db/src/id-documents.ts:13-26,43-50 |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The doc does not mention that `validateResponses` only iterates `page.questions` and *drops* unknown keys via the per-question loop (no per-page persistence of stray keys) — it is described accurately in prose under "Validation" but the mechanism (`responses[q.id] = result.value` only when defined; unknown keys never copied) is left implicit. Not misleading; behaviour is correctly summarised. | packages/types/src/questionnaire.ts:334-352 |
| low | `QuestionnaireResponses` is a `z.record(z.string(), QuestionnaireResponseValue)` — so "Malformed response payload" only fires on a value-shape mismatch, not on extra keys (extra keys pass the record parse and are then dropped by the question loop). The doc's framing is consistent with this but does not state the record-schema detail. | packages/types/src/questionnaire.ts:198-201 |

## Spot-confirmed
- Forms list: `force-dynamic` (page.tsx:20), `listCompletedForms` call (page.tsx:41), title "My forms" + exact subtitle copy (page.tsx:53-57), Tools back-link with `ChevronLeft` (page.tsx:45-51), card → `/tools/forms/${form.key}` with `ChevronRight` (page.tsx:69-86), "Last edited {date}" (page.tsx:79-81), `lastEdited = form.updatedAt ?? form.completedAt` (page.tsx:67), empty-state "You haven't completed any forms yet." (page.tsx:60-63).
- List gating chain exact and in order: `getAuthenticatedUserOrRedirect` → `ensureCampUser` → `!hasCampAccess` → `/signup/required` → `!profile?.completedAt` → `/onboarding/questionnaire` → `!isApproved` → `/pending-approval` (page.tsx:28-39).
- Detail page: `getReplayableForm(key)` then `notFound()` (`[key]/page.tsx:44-45`), `form.load`, `!state?.completedAt` → `redirect("/tools/forms")` (`[key]/page.tsx:47-51`), header `form.title` + "Step back through the form…Last edited {date}." with `state.updatedAt ?? state.completedAt` (`[key]/page.tsx:54,65-71`), "My forms" back-link (`[key]/page.tsx:58-64`), renders `<FormReplay>` then `<ChangeLog edits={edits}>` (`[key]/page.tsx:73-79`), `listFormEdits(campUser.id, form.key)` default-20 (`[key]/page.tsx:53`), gate-parity comment present (`[key]/page.tsx:35`), `force-dynamic` (`[key]/page.tsx:16`).
- ChangeLog sub-component lives inline (`[key]/page.tsx:84-126`): heading "Change log" + exact intro copy (87-91), empty-state "No edits yet…" (92-95), `<ol>` of edit cards with `dateFmt.format(edit.createdAt)` (97-105), per-field `change.label` bold, `change.from` with `line-through`, `aria-hidden` "→", `change.to` in foreground colour (106-118), edits keyed `edit.id`, changes keyed `change.fieldId` (100,108).
- FormReplay shell: wizard seeded with `initialResponses` (form-replay.tsx:43-53), `persistProgress={false}`, `submitLabel="Save changes"`, `action={(responses, final) => saveFormReplay(formKey, responses, final)}` (44-48), `onComplete` sets `saved` + `router.refresh()` (49-52), `role="status"` banner with `CheckCircle2` and exact "Saved. Your answers — and the change log below — are up to date." (32-42).
- `saveFormReplay`: auth+`hasCampAccess` re-gate only, no onboarding/approval recheck (actions.ts:28-32); unknown key → `{ ok:false, errors:{ _root:"Unknown form." } }` (34-35); `if (!final) return { ok: true }` (38); `validateResponses` failure passthrough (40-41); reload + `!state?.completedAt` → `_root:"This form hasn't been completed yet."` (43-49); `diffResponses(...).filter(c => c.fieldId !== ID_NUMBER_KEY)` (54-58); `form.save` (60); `changes.length > 0` → `recordFormEdit({userId, questionnaireKey, version, editedByUserId: campUser.id, changes})` (62-70); `revalidatePath` of `/tools/forms/${key}` and `/tools/forms` (72-73); `SaveResult` union type (11-13).
- Registry: single entry `BURNER_PROFILE` (forms.ts:49,89); `getReplayableForm` = `REGISTRY.find` (91-93); key/title/description/questionnaire fields verbatim (49-54); `load` merges decrypted ID via `mergeIdNumber` and returns `{responses, completedAt, updatedAt}` (55-71); `save` does `splitIdNumber` → `upsertBurnerProfile({version: QUESTIONNAIRE.version, markComplete:true})` → conditional `setIdDocuments` → `satisfyBurnerProfileAction` (72-86); registry-extensibility comment present (23-32); `listCompletedForms` skips entries without `completedAt` (107-123).
- Change-log routing: `recordFormEdit`/`listFormEdits` E2E-mode branch to `testStore` else DB (forms.ts:137-175); default `limit = 20` (forms.ts:154); DB `recordQuestionnaireEdit` single insert with caller-skips-empty comment (questionnaire-edits.ts:15-35); DB `listQuestionnaireEdits` `and(userId, questionnaireKey)` `orderBy(desc(createdAt)).limit(limit)` with null→`[]` coercion (40-68); test-store record pushes `{id, createdAt: new Date()}` (281-297) and list filters+sorts-desc+slices default 20 (298-309).
- Schema `questionnaireEdits` (schema.ts:530-562): `id` uuid defaultRandom PK; `userId` uuid not-null FK cascade; `questionnaireKey` text not-null; `version` text not-null; `editedByUserId` uuid FK set-null (nullable, no `.notNull()`); `changes` jsonb `$type<QuestionnaireFieldChange[]>()` not-null default `[]`; `createdAt` timestamp mode:date not-null defaultNow; index `questionnaire_edits_user_key_created_idx` on `(userId, questionnaireKey, createdAt)` (556-560). Comment "no full version history… no-op replay records no row" present (520-528).
- `QuestionnaireFieldChange` zod (questionnaire.ts:211-219): `fieldId z.string().min(1)`, `label z.string()` (captured at edit time per comment), `from z.string()`, `to z.string()`.
- `FormEdit` shape `{id, version, editedByUserId, changes, createdAt}` — drops `questionnaireKey` (forms.ts:129-135); `QuestionnaireEditRow` includes `questionnaireKey` (questionnaire-edits.ts:6-13); `TestQuestionnaireEdit` includes `userId` + `questionnaireKey` (test-store.ts:35-43).
- `diffResponses` (questionnaire.ts:298-316): iterates `flattenQuestions` in order, `sameValue` compare, multi-select set-compare with sorted arrays (282-288), empty/absent equality via `isEmptyValue` (268-275), `from`/`to` via `displayResponseValue`. `EMPTY_DISPLAY = "—"` (232,243-245); multi-select labels joined `", "` (259-262), default kind join `", "` (264).
- `validateResponses` (questionnaire.ts:324-352): `safeParse` fail → `{_root:"Malformed response payload"}`; per-question `validateOne`; required-missing → "This question is required"; unknown keys not copied. Cross-field local validation in wizard `validatePageLocally` validates `id.number` vs `id.type` via `validateIdNumber` (wizard.tsx:79-101,90-96).
- Wizard mechanics: `persistProgress` default true (wizard.tsx:53), local-only Next when false (105-108), `submitLabel` default "Finish" (55), `firstStepSignOut` default false (56), Skip vs Next label logic for lone optional unanswered question (160-172), submit/back buttons disabled on `isPending` and back disabled on page 0 (250,255), `FORM_ERROR_KEY="_form"`/`ROOT_ERROR_KEY="_root"` (21-22), formError banner `role="alert"` (189-196).
- Enums/config: `QUESTIONNAIRE.version = "2026.05.29-v8"` (lib/questionnaire.ts:60); `ID_NUMBER_KEY="id.number"`, `ID_TYPE_KEY="id.type"` (id-documents.ts:7-8); date format `Intl.DateTimeFormat("en-ZA", {dateStyle:"medium", timeStyle:"short"})` (page.tsx:22-25, [key]/page.tsx:18-21).
- `isApproved` treats only `approvalStatus === "approved"` (or god email) as approved (users.ts:231-236); `hasCampAccess` is invite/god only (users.ts:219-224); `satisfyBurnerProfileAction` is a no-op under E2E (users.ts:204-209); `BurnerProfileSummary` shape (users.ts:155-160); ID columns `passportEncrypted`/`saIdEncrypted` via `idColumnsFor`, AES-256-GCM (users.ts:396,402-403, id-documents.ts:43-50).
- `upsertBurnerProfile` insert/`onConflictDoUpdate` on `userId`, sets `version`, `responses`, `updatedAt=now`, and `completedAt=now` when `markComplete` else preserves via `sql` self-reference (burner-profile.ts:134-161). This actively confirms the doc's "ugly truth" finding: a replay (markComplete:true) bumps `completedAt` to the re-submit time, contradicting the forms.ts:78-79 inline "idempotent on completedAt" comment.
- No orphaned/dead code: `saveFormReplay`, `getReplayableForm`, `listCompletedForms`, `listFormEdits`, `recordFormEdit` have their only production consumers inside this surface (grep, source-only). `REGISTRY` has exactly one entry. `/tools/forms` is linked from the Tools surface (apps/web/app/tools/page.tsx:36-37, title "My forms"). The `final === false` action branch is genuinely unreachable in the replay flow (persistProgress=false), exactly as the doc states.

## Low-confidence / could-not-verify
- The `completedAt`-overwrite "ugly truth" (doc line 169): code confirms the *behaviour* (replay bumps `completedAt`), but whether this is *intended* vs a latent bug is a design question the doc rightly leaves as a `<!-- low-confidence -->`. Cannot be settled from source alone.
- Cross-subsystem trust: `validateIdNumber` (lib/id-validation), `encrypt`/`decryptOrNull` (PGCRYPTO_KEY crypto), and the `QUESTIONNAIRE` catalogue contents belong to other units (04/20/29); their internals were not exhaustively re-verified, only their call sites and signatures used here.
- "Re-satisfies the onboarding gate" end-to-end (forms.ts:85 → `satisfyBurnerProfileAction` → `dbSatisfyRequiredAction`): the wiring is confirmed but the actual required_actions row flip is a DB-side effect not exercised here; behaviour under a captain re-activation at a new version is asserted by comment, not observed.
