# Verification — 00 overview

**Verdict:** accurate  ·  checked 58 claims, verified 54.
The overview is unusually reliable: every OKLCH token, enum, file:line citation, route, and primitive list I checked confirmed digit-for-digit against source. Defects cluster in two spots — (a) `control-grid.tsx` is presented as a live "desktop counterpart" but is Storybook-only/unused (the doc flags `quadrant-nav` as v0 but does not flag `control-grid`), and (b) the §4 "spinner in-button" grammar is cited to `wizard.tsx:255`, which only disables — no spinner lives in that file.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| medium | §3 / §4: "the grid is the desktop counterpart that shows all layers at once" and Captain-only-locked grammar cite `control-grid.tsx:50`/`:151` as live behaviour; `control-grid` is presented as a functional primitive with no prepared-but-unused caveat (only `quadrant-nav` is flagged v0). | `ControlGrid` is imported **only** by `control-grid.stories.tsx` — zero app consumers. It is Storybook-only/dead in production. `design-system.md` does not mention it at all (it documents `quadrant-nav` v0 and `control-panel`, but not `control-grid`). The doc's own §0 hint warns "control-grid / quadrant-nav are prepared-but-unused — verify"; this confirms control-grid is unused yet the prose treats it as live. | grep: only importer `packages/ui/src/components/control-grid.stories.tsx:13`; component `control-grid.tsx:50`,`:151` |
| low | §4 "Submitting/pending" row: "Disable the submit control, **spinner in-button** (`isPending`, `wizard.tsx:255`)". | `wizard.tsx:255` is `<Button type="submit" disabled={isPending}>` — it disables but renders **no** `Loader2`/`animate-spin`/spinner. No spinner exists anywhere in `wizard.tsx`. (The in-button-spinner grammar is real elsewhere — invite-form, recorder-panel, report-bug-dialog use `Loader2` — but not at the cited line.) | `apps/web/components/questionnaire/wizard.tsx:255-257` |
| low | §3 wizard: "top progress bar ('Step N of M', `wizard.tsx:263`)". | Line 263 is `function ProgressBar(...)`; the literal "Step {current} of {total}" string is at `:274`. Citation points at the component, not the string — off by ~11 lines. | `apps/web/components/questionnaire/wizard.tsx:263` vs `:274` |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | §1 "belt-and-braces" legacy gate. The doc names the gating spine (invite → required_actions → approval) but omits the legacy `completedAt` fallback that runs between `nextGate` and the approval gate — a second redirect to `/onboarding/questionnaire` when `!profile?.completedAt`. Harmless for a rebuild (it's a transitional safety net), but it is a real production gate step the §7 "ordered spine" doesn't list. | `apps/web/app/page.tsx:53-56` |
| low | §3 ack-gate poll: doc says "polls every 45 s + on focus" and §7.5 "stale-response superseding" — both correct — but does not surface that the supersession is implemented via a monotonic token guarding overlapping interval-vs-focus polls (relevant to "drop no functionality"). | `apps/web/app/acknowledgement-gate.tsx:33`, `:56`, `:61` |

## Spot-confirmed
- §1 rank model: `rankEnum = ["captain","member"]` `schema.ts:31`; team-lead derived from `team_memberships.is_lead` `schema.ts:455`; driver from `driver_profiles.intends_to_drive` `schema.ts:397`; `isTeamLead` `apps/web/lib/users.ts:244`.
- §1 server-only: `dynamic = "force-dynamic"` `app/page.tsx:27`; no client data layer — grep found zero react-query/SWR/IndexedDB/localStorage in `apps/web/{app,components,lib}`.
- §1 gating spine + line cites: `hasCampAccess` gate `app/page.tsx:40` (`users.ts:219`); `nextGate` gate `app/page.tsx:47` (`required-actions.ts:7`,`:23-30`); `isApproved` gate `app/page.tsx:61` (`users.ts:231`); viewerRank reconcile `app/page.tsx:73`; whole spine `:29-63`. `nextGate` skips routeless actions — `ACTION_ROUTES` only maps `burner_profile` `required-actions.ts:7`, loop returns null otherwise `:23-30`.
- §1 onboarding sign-out escape: `firstStepSignOut` → `<a href="/auth/sign-out">Sign out</a>` `wizard.tsx:239-244`.
- §1 anti-scope: `team_budgets` is plain assigned/perceived note `schema.ts:723` (`assignedAmount`/`perceivedAmount`/`notes`, no over-target); reimbursements "a log, not a finance system" `schema.ts:669-717` (comment `:673`).
- §1 test seam: `testStore` hangs off `globalThis`, "Only ever loaded under E2E_TEST_MODE; production never imports" `apps/web/lib/test-store.ts:104`,`:109`. `getAuthenticatedUser` bypasses Neon Auth via test cookie `apps/web/lib/auth.ts:25`. `neon-auth.ts`/`test-store.ts` exist.
- §2 OKLCH tokens, digit-exact vs `globals.css:9-39`: background `oklch(0.15 0.05 295)` `:12`; foreground `oklch(0.97 0.02 330)` `:13`; primary `oklch(0.65 0.27 340)` `:14`; secondary `oklch(0.42 0.18 320)` `:34`; accent `oklch(0.62 0.18 255)` `:29`; muted `oklch(0.22 0.06 295)` `:18`; card `oklch(0.26 0.08 295)` `:20`; border/input `oklch(0.35 0.1 305)` `:25`/`:26`; destructive `oklch(0.65 0.22 18)` `:36`; ring `oklch(0.65 0.27 340)` `:38`; radius `0.625rem` `:39`. All match.
- §2 `RANK_LABEL` `control-panel.tsx:18` ("Camp Member"/"Team Lead"/"Captain"); `RANK_ORDER` low→high `control-panel.tsx:16`; approval-status separate axis `schema.ts:41` (`pending`/`approved`/`rejected`).
- §2 entity icons (lucide, in source): `GitBranch` family tree `tools/page.tsx:46`; `ClipboardList` forms `tools/page.tsx:40`; `Mail` invite `tools/page.tsx:3`; `Shuffle` mint `tools/invite/invite-form.tsx:173`; `Megaphone`/`MessageSquare`/`Bell` map acknowledge/popup/feed `notifications/page.tsx:19-21`; `Mic`/"TALK" centre default `control-panel.tsx:195-196`, passed as `centre={{label:"TALK"}}` `app/page.tsx:94`.
- §3 primitives list = exactly the 13 non-nav components present: avatar, button, card, checkbox, combobox, command, dialog, input, label, popover, select, slider, textarea. button CVA variants `default/outline/ghost/destructive/secondary` `button.tsx:12-19`, sizes `default/sm/lg/icon` `:23-26`, `asChild` via Radix `Slot` `:2,:40,:44-45`. `cn()` at `packages/ui/src/lib/utils.ts:4`.
- §3 control-panel: push-to-talk centre `w-[22%]` `control-panel.tsx:205` (overview's "~22%" is more accurate than design-system.md's "30%"); visible-but-locked `opacity-45`+lock+`aria-disabled` `control-panel.tsx:331-338`; tiles take `{label,hint,href,icon,badge}` `:37-46`; badge hides on null/""/0 `:313-314`; `ControlPanelRank` UI-local `camp_member|team_lead|captain` `:13`; quadrant-nav flagged v0 in `docs/design-system.md:60`.
- §3 wizard: per-page validation incl. id-number-vs-id-type cross-field `wizard.tsx:79-101`; `_form`/`_root` banner contract `:21-24`,`:176`; lone-optional → "Skip" not "Next" `:160-172`.
- §3 question kinds: renderer `switch` `question.tsx:100-241` has exactly 10 cases — slider, single_select, multi_select, short_text, long_text, date, scale, toggle, combobox, image; inline error `role="alert"` `question.tsx:78-79`; required `*` marker `:62-64`; scale vertical full-height on mobile (`h-[70dvh]`, vertical Slider, `md:hidden`) `:336-347`; toggle segmented `inline-flex` `:264`.
- §3 voice pipeline: all cited files exist — `use-voice-recorder.ts`, `recorder-panel.tsx`, `waveform.tsx`, `dictate-button.tsx`, `app/api/voice/transcribe/route.ts`, `lib/voice-prompts.ts`.
- §3 feedback layer: `AcknowledgementGate` + `FeedbackGate` mounted in `app/layout.tsx:52-55`; ack-gate `POLL_INTERVAL_MS = 45_000` `:26`, focus listener `:61`, Acknowledge at end of scroll `:135`; FeedbackGate gated on live client session `useSession()` `feedback-gate.tsx:17,23-24`, files GitHub issue `report-bug-dialog.tsx:40,131`, "Improve with AI" `:234,:246`, `aiAvailable={!!process.env.ANTHROPIC_API_KEY}` `layout.tsx:55`.
- §4 validation-error inline `role="alert"` `question.tsx:78`; captain-only-locked `control-panel.tsx:333` + `control-grid.tsx:151` (grid is real code, just unused — see Inaccuracies).
- §5 IA: all 17 routes (`/auth`, `/auth/[path]`, `/signup/required`, `/onboarding/questionnaire`, `/pending-approval`, `/profile`, `/profile/edit`, `/notifications`, `/tools`, `/tools/invite`, `/tools/forms`, `/tools/forms/[key]`, `/family-tree`, `/captains/camp-management`, `/captains/announcements`, `/captains/tools`, `/mcp/connect`) exist as `page.tsx`. Cross-cutting API/crons exist: `/api/avatar`, `/api/uploads/avatar`, `/api/push/tokens`, `/api/notifications/{acknowledge,pending}`, `/api/cron/notifications/{dispatch,push,reminders}`, `/api/test/*`, `/api/voice/transcribe`. Error boundaries `app/{error,global-error,not-found}.tsx` all present.
- §7 enums: ranks `captain|member` `schema.ts:31`; approval `pending|approved|rejected` `:41`; 8 `team` values `:51-60`; presentation `acknowledge|popup|feed` `:166-170`; 10 question kinds (confirmed in switch + `lib/questionnaire.ts`). `access-control.ts`/`test-mode.ts` exist.

## Low-confidence / could-not-verify
- Companion brief `docs/design/camp-404-design-system-port-briefing.md` is **confirmed absent** (the doc's own §0 HTML comment flags this honestly); `docs/design-system.md` does exist. Not scored as an inaccuracy because the doc self-discloses it.
- §3 scale "horizontal on md+": confirmed the `md:hidden` mobile vertical block; assumed (not line-verified) that a complementary md-visible horizontal block follows below `:357` — high confidence given the `md:hidden` gate, but not read line-for-line here.
- §3 "every screen re-skins the primitives" / "Used by: every screen" — breadth claims not exhaustively traced across all 30 surfaces; representative usages confirmed.
- Better Auth / Neon Auth internals (`neon-auth.ts` upstream behaviour) and the voice transcription server contract trusted at the file-exists level, not line-audited (out of this unit's scope; covered by units 21/23).
