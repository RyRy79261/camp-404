# Verification — 17 mcp-connect

**Verdict:** accurate  ·  checked 71 claims, verified 67.
The doc is a high-fidelity, digit-exact description of the MCP consent/bridge surface: line citations, enum values, error codes, TTLs, gate ordering, and schema fields all match production source. The only real defect is an Omission — four of the six `scope.ts` capability predicates plus `redactIdDocuments` are test-only (no production caller), yet the doc presents them as the live "what the granted token can do per call" surface without flagging them as orphaned the way it correctly flags `isAllowedRedirectUri` and `plain` PKCE.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | "Advertised as `scopes_supported: ["mcp:user"]` … (oauth-authorization-server/route.ts:23)" | `scopes_supported` is on line **22**; line 23 is `response_types_supported`. The protected-resource cite (route.ts:19) is correct. | apps/web/app/api/mcp/well-known/oauth-authorization-server/route.ts:22 |
| low | Well-known metadata fields block cited as "route.ts:23-30"; VERCEL_URL issuer comment cited as "oauth-authorization-server/route.ts:11-14" | Fields span lines **22-30**; the issuer rationale comment is lines **10-13** (GET begins at 14). Off-by-one citations. | apps/web/app/api/mcp/well-known/oauth-authorization-server/route.ts:22-30, 10-14 |
| low | `onGoogle` "calls `authClient.signIn.social({ provider: "google", callbackURL: window.location.href })`" | Actual `callbackURL` is guarded: `typeof window !== "undefined" ? window.location.href : "/"`. Functionally equivalent in-browser; the doc drops the SSR guard. | apps/web/app/mcp/connect/page.tsx:42 |
| low | "CSP `form-action 'self'` silently drops cross-origin 302s … (route.ts:259-263)" stated as established fact | The `form-action 'self'` CSP is only referenced in a code **comment** in this route; no app-level CSP setting `form-action 'self'` is present in source (the only CSP in build artefacts is Next's metadata-route `script-src 'none'…sandbox`). The `htmlRedirect` technique itself is real; the CSP rationale is unverifiable from source. | apps/web/app/api/mcp/oauth/authorize/route.ts:261 |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| medium | The doc presents `scope.ts` predicates `canReadTeamOps` / `canWriteTeam` / `canApproveCrossTeam` / `canAdmin` as the live capability surface ("defines what the granted token can do per call"), but **none of the four has any production caller** — they appear only in `scope.test.ts`. Only `getMcpScope` (via `tool-utils.ts:53`) and `canSeeIdDocuments` (via `people.ts:133`) are wired in. The doc's "Dead/orphaned" section flags `isAllowedRedirectUri` and `plain` PKCE but not these four. | apps/web/lib/mcp/scope.ts:70-90 (callers: only apps/web/lib/mcp/__tests__/scope.test.ts) |
| medium | `redactIdDocuments` is described as the active "second line of defence" (doc §consent.ts, §business-rules) but has **zero production callers** — `people.ts` does its own conditional include via `canSeeIdDocuments` and never calls `redactIdDocuments`. Only `consent.test.ts` exercises it. Not flagged as orphaned. | apps/web/lib/mcp/consent.ts:33-45 (callers: only apps/web/lib/mcp/__tests__/consent.test.ts) |

## Spot-confirmed
- Bridge page is `"use client"`, Suspense fallback `<Shell>Loading…</Shell>` (page.tsx:1,19); inner reads `useSearchParams()` (page.tsx:26) + `safeNext` (page.tsx:27,93-98); `authClient.useSession()` (page.tsx:28); auto-forward effect `if (isPending || !session?.user) return; window.location.replace(next)` (page.tsx:31-36); `isPending` → "Checking session…" (page.tsx:47-49); signed-in → "Continuing to {next}…" (page.tsx:52); footnote "New to Camp 404?" → "Sign in first" href `/auth/sign-in` (page.tsx:74-79).
- `safeNext` returns `/` for empty / non-`/`-prefixed / protocol-relative `//` with the explicit "protocol-relative attack" comment (page.tsx:94-97).
- `AuthorizeQuery`: `response_type` literal `"code"`, `client_id` min 1, `redirect_uri` `.url()`, `code_challenge` min 1, `code_challenge_method` `z.enum(["S256"]).default("S256")`, `scope`/`state` optional (route.ts:21-29).
- GET flow: parse → `resolveClientOrError` (route.ts:37-58,75-76) → no session → redirect `/mcp/connect?next=<encoded authorize>` (route.ts:78-84) → no camp profile → `errorPage(403,"no_camp_account", "Open the app and enter your invite code…")` (route.ts:86-93) → `mcpAccessError` over `hasCampAccess`/`!!profile?.completedAt`/`isApproved` (route.ts:98-104) → `consentHtml({clientName, scope, displayName: campUser.displayName ?? authUser.primaryEmail ?? "You"})` (route.ts:106-111).
- POST: form parse fail → `errorPage(400,"invalid_request","Form body required.")` (route.ts:119-122); strips `action`, rebuilds params, re-validates (route.ts:124-137); deny → `htmlRedirect` w/ `error=access_denied` (+state) **before** session/gate re-check (route.ts:142-149); approve → re-auth (`errorPage(401,"unauthenticated","Session expired. Try again.")` route.ts:152-153) → re-resolve user → `mcpAccessError` → `issueAuthCode` → `htmlRedirect` w/ `code` (+state) (route.ts:151-186).
- `consentHtml` (route.ts:202-257): hidden inputs from params (route.ts:208-213), form `method="POST" action="/api/mcp/oauth/authorize"` (route.ts:243), `Signed in as <strong>{displayName}</strong>` (route.ts:240), `Scope:` block (route.ts:242), Deny/Approve `<button name="action" value="deny|approve">` (route.ts:246-247); hard-coded hex palette `#0a0a0a`/`#fafafa`/`#171717`/`#262626` (route.ts:224,228) vs app's OKLCH brand tokens (packages/ui/src/styles/globals.css:12-14) — "ugly truth" note accurate.
- `htmlRedirect` meta-refresh + `window.location.replace(${JSON.stringify(target)})`, "Redirecting… Continue if not redirected." (route.ts:264-277); `escapeHtml` 5-entity escaping (route.ts:193-200); `errorPage` (route.ts:298-314); `redirectError` (route.ts:285-296, used only for `invalid_scope`); `buildRedirectUrl` (route.ts:279-283).
- `resolveClientOrError`: unknown client → `errorPage(400,"unknown_client")`; redirect_uri not in `client.redirectUris` → `errorPage(400,"invalid_redirect_uri")`; bad scope → `redirectError(...,"invalid_scope",...)` (route.ts:37-58). Confirms doc's "validates against stored `client.redirectUris`, not `isAllowedRedirectUri`."
- `consent.ts`: `canSeeIdDocuments` self short-circuit `if (scope.campUserId === subject.id) return true` (consent.ts:21), else `scope.isCaptain && subject.aiDataConsent` (consent.ts:22); `redactIdDocuments` strips `passportEncrypted`/`saIdEncrypted`/`eftDetailsEncrypted` when gate false (consent.ts:33-45); "second line of defence" comment (consent.ts:30-31). `canSeeIdDocuments` is genuinely used in production at people.ts:133 to gate decryption.
- `scope.ts`: `resolveMcpScope` builds `memberTeams`/`leadTeams` from `teamMemberships`, `isCaptain = rank === "captain"`, `isDriver = rows.driverIntent`, `aiDataConsent` (scope.ts:36-52); `getMcpScope` returns null when rows null (scope.ts:58-62); freshness comment (scope.ts:9-13). `McpScope` interface fields (scope.ts:15-28) and `McpScopeRows` (mcp.ts:14-22) match.
- `oauth.ts`: `DEFAULT_SCOPE = "mcp:user"` (oauth.ts:13), `ALLOWED_SCOPES = new Set([DEFAULT_SCOPE])` (oauth.ts:14), "Single coarse scope" comment (oauth.ts:12); `isAllowedScope` splits `/\s+/`, filters empties, `.every` in set (oauth.ts:16-21); `issueAuthCode` inserts `mcpAuthCodes` w/ `expiresAt = now + AUTH_CODE_TTL_SEC*1000` (oauth.ts:129-144); `findClient` (oauth.ts:99-107); `isAllowedRedirectUri` loopback http|https else https + claude.ai/anthropic.com (sub)domains (oauth.ts:27-42) — called ONLY by /register/route.ts:64, not authorize. TTLs: `AUTH_CODE_TTL_SEC=5*60`, `ACCESS_TOKEN_TTL_SEC=24*60*60`, `REFRESH_TOKEN_TTL_SEC=30*24*60*60` (oauth.ts:8-10).
- `access.ts`: `mcpAccessError` precedence `no_camp_access` > `onboarding_incomplete` > `pending_approval`, returns null only when all three hold; exact description strings match doc (access.ts:18-41).
- `users.ts`: `hasCampAccess = isGodEmail(email) || !!user.inviteCode` (users.ts:219-224); `isApproved = isGodEmail(email) || approvalStatus === "approved"` (users.ts:231-236) — confirms god bypass and rejected-collapses-to-pending. `isGodEmail` checks GOD_EMAILS csv case-insensitively (access-control.ts:28-32).
- `origin.ts`: priority `MCP_PUBLIC_URL` → `x-forwarded-host`/`-proto` → `host` → `VERCEL_URL` → `http://localhost:3000`; explicit "VERCEL_URL is the wrong issuer" rationale (origin.ts:17-36). Well-known routes both use `getPublicOrigin(req)` and advertise `scopes_supported: ["mcp:user"]`, `response_types_supported:["code"]`, `grant_types_supported:["authorization_code","refresh_token"]`, `code_challenge_methods_supported:["S256"]`, `token_endpoint_auth_methods_supported:["none","client_secret_basic","client_secret_post"]`, `bearer_methods_supported:["header"]`.
- Enums: `rankEnum` `["captain","member"]` (schema.ts:31); `approvalStatusEnum` `["pending","approved","rejected"]` (schema.ts:41-45); `teamEnum` 8 values exactly as listed (schema.ts:51-60); `mcpClientAuthMethodEnum` `["none","client_secret_basic","client_secret_post"]` (schema.ts:1223-1227); `mcpCodeChallengeMethodEnum` `["S256","plain"]` (schema.ts:1229-1232) — authorize accepts only S256; `mcpAuditOutcomeEnum` `["success","error"]` (schema.ts:1234-1237).
- `users` table (schema.ts:220-303): `aiDataConsent` `boolean("ai_data_consent").notNull().default(false)` (schema.ts:298); `aiDataConsentAt` nullable (schema.ts:299); `passport_encrypted`/`sa_id_encrypted`/`eft_details_encrypted` (schema.ts:241-243); `inviteCode` nullable + "NULL = god account" comment (schema.ts:256-260); `approvalStatus` default `"approved"` (schema.ts:267-269).
- MCP tables: `mcp_oauth_clients` (schema.ts:1242-1254) clientName NOT NULL, redirectUris text[] NOT NULL, scope nullable; `mcp_auth_codes` (schema.ts:1260-1284) code PK, clientId/userId cascade FKs, scope NOT NULL, consumedAt nullable, indexes `mcp_auth_codes_client_idx` + `mcp_auth_codes_expires_idx`; `mcp_access_tokens` (schema.ts:1289-1311); `mcp_audit_log` (schema.ts:1316-1341). Doc correctly notes auth codes are written by `issueAuthCode`, tokens/audit are not written by this surface.
- `getMcpScopeRows`: single-round-trip read of user row + team memberships + driver intent, returns null if no user row (mcp.ts:30-66); `team_memberships` read mcp.ts:47-53, `driver_profiles.intendsToDrive` mcp.ts:55-59.

## Low-confidence / could-not-verify
- CSP `form-action 'self'` rationale (doc §validation, route.ts:261): only present as a code comment; no actual `form-action` CSP directive found in source. May be set at the Vercel/platform layer — cannot confirm from repo. The doc states it as established fact.
- `aiDataConsent` toggle UI location: doc's own inline note already flags this as not present under `apps/web/app`; confirmed no app-route UI writes `aiDataConsent` (only schema field + reads). The "set elsewhere" claim is unverified within this unit's scope.
- Whether the four orphaned `scope.ts` predicates and `redactIdDocuments` are intended scaffolding for unreleased tools (forward-looking, per unit 29) vs genuinely dead — cannot determine intent from code; flagged as Omission because the doc presents them as the live grant surface without the caveat.
