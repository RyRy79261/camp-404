# Verification — 05 pending-approval

**Verdict:** accurate  ·  checked 64 claims, verified 62.
The doc is a faithful, digit-accurate map of `/pending-approval`. Every line citation, enum, copy string, redirect, and audit-stamp claim checked out against real production source. The only defects are cosmetic: a slightly imprecise `AuthView` line range and an incomplete Button-size list — neither affects a rebuild.

## Inaccuracies

| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | "Sub-components → Button … sizes default/sm/lg/icon" (line 144) | The size enum has **five** members, not four: `default`, `sm`, `lg`, `icon`, **`icon-lg`** (`"h-14 w-14"`). Off-by-one on the size list. Variant claim ("only outline is used here") is correct. | packages/ui/src/components/button.tsx:22-28 |
| low | "`apps/web/app/auth/[path]/page.tsx:43-47` — `<AuthView path={path} />`" (lines 41, 149) | `<AuthView path={path} />` is on line 45; lines 43-48 are the wrapping `<main>`. The cited range is off by a line or two but lands on the right block. | apps/web/app/auth/[path]/page.tsx:43-48 |

## Omissions

| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | The doc lists camp-management / tools / announcements as examples of other protected routes that `redirect("/pending-approval")`, but **eleven** routes do so (also `tools/forms`, `tools/forms/[key]`, `tools/invite`, `tools/page`, `family-tree`, `profile`, `profile/edit`). The doc's "EVERY protected route" prose is correct; only the example list is partial (it is explicitly labelled "e.g.", so not misleading). | apps/web/app/tools/forms/page.tsx:38; tools/invite/page.tsx:19; family-tree/page.tsx:20; profile/page.tsx:35; profile/edit/page.tsx:27; tools/page.tsx:57; tools/forms/[key]/page.tsx:41 |

## Spot-confirmed

- `export const dynamic = "force-dynamic"` (page.tsx:14) and `metadata.title = "Application pending — Camp 404"` (page.tsx:16-18) — both verbatim; title is hardcoded and does NOT change on the rejected branch (the doc's "stale-truth" note at line 139 is correct).
- Page body order: `getAuthenticatedUserOrRedirect()` (page.tsx:28) → `ensureCampUser(authUser)` (page.tsx:29) → `!hasCampAccess` → `/signup/required` (page.tsx:32-34) → `isApproved` → `/` (page.tsx:36-38) → `getBurnerProfile` + `!profile?.completedAt` → `/onboarding/questionnaire` (page.tsx:41-44). Order matches the doc exactly (invite → approved → onboarding).
- `getAuthenticatedUserOrRedirect` redirects unauthenticated to `/auth/sign-in` (auth.ts:40-44, redirect at :42); `AuthenticatedUser { id, primaryEmail: string|null, displayName: string|null }` (auth.ts:13-17). Both as documented.
- Synthetic non-persisted row for invite-less signed-in user: `id: ""`, `inviteCode: null`, `approvalStatus: "approved"` (users.ts:86-94); god account auto-created `approved`, `inviteCode: null` (users.ts:70-80). As documented.
- `hasCampAccess` = `isGodEmail(email) || !!user.inviteCode` (users.ts:219-224); `isApproved` = `isGodEmail(email) || approvalStatus === "approved"` (users.ts:231-236). Verbatim.
- `const rejected = campUser.approvalStatus === "rejected"` (page.tsx:46). Icon badge: 14×14 (`h-14 w-14`) rounded-full holding a 7×7 (`h-7 w-7`) icon; rejected = `ShieldX` + `bg-destructive/10 text-destructive`; pending = `Clock` + `bg-amber-500/15 text-amber-400`; both `aria-hidden` (page.tsx:51-63). Every class string matches, including the literal `amber-500/15` / `amber-400` pair.
- Headings `Application not approved` / `Application submitted` and both body strings (page.tsx:67-83) match the doc word-for-word, including the name interpolation ``Thanks{campUser.displayName ? `, ${campUser.displayName}` : ""}`` (page.tsx:78) and the body class `text-balance text-sm text-[color:var(--color-muted-foreground)]`, heading `text-2xl font-bold`.
- Sign-out escape: `<Button asChild variant="outline" className="w-full"><a href="/auth/sign-out">Sign out</a></Button>` (page.tsx:86-88). Only interactive control, plain anchor, no client JS.
- `AuthShell` is `"use client"`, props `{ children, className?, footer?, hideBack? }`; renders `min-h-svh` on `bg-[color:var(--color-muted)]`, `max-w-sm`, `Card` + `CardContent`; `!hideBack` renders a ghost Back button calling `router.back()` (auth-shell.tsx:1-59). Page passes only `hideBack`, no footer (page.tsx:49). All confirmed.
- Home gate: `if (!isApproved(campUser, user.primaryEmail)) redirect("/pending-approval")` (page.tsx:61-63). Captain-surface holds confirmed at camp-management:26, tools:47, announcements:26. e2e asserts the protected-route hold (authenticated.spec.ts:90-92), "Application submitted" (:88), "Application not approved" (:134), set-approval POST (:128).
- `claimInviteCode` returns env codes `requiresApproval: false` (access-control.ts:48-49); `isGodEmail` reads `process.env.GOD_EMAILS`, CSV, `.toLowerCase()` case-insensitive (access-control.ts:28-32). `inviteCodes.requiresApproval` boolean `notNull().default(false)` with the "codes minted by non-captains are ALWAYS true" comment (schema.ts:333-336).
- `redeemInviteForUser` (users.ts:111-153): first-time `approvalStatus: claimed.requiresApproval ? "pending" : "approved"` (users.ts:149); existing-row tighten `if (claimed.requiresApproval && existing.approvalStatus !== "pending") setUserApprovalStatus(existing.id, "pending")` (users.ts:135-137); god/code-holder short-circuit `{ ok: true }` (users.ts:122-124). The doc's subtle note that a `rejected` user (`!== "pending"`) WOULD be re-set to `pending` on re-redeem (line 134) is logically correct.
- `decideApprovalAction(userId, decision)` (actions.ts:75-96): `requireCaptain` checks signed-in + `hasCampAccess` + `rank === "captain"` (actions.ts:30-43, rank at :39-41); validates `decision !== "approved" && decision !== "rejected"` (actions.ts:82-84); blocks `userId === gate.captainId` (actions.ts:85-87); calls `decideUserApproval` then `revalidatePath("/captains/camp-management")` (actions.ts:89-94). `ApprovalDecisionResult = { ok:true } | { ok:false; error:string }` (actions.ts:22-24).
- `decideUserApproval` (users.ts:253-260) accepts only `status: "approved" | "rejected"` → backend `setUserApproval`. Real DB `setUserApproval` stamps `approvalStatus`, `approvalDecidedByUserId`, `approvalDecidedAt`, `updatedAt` (burner-profile.ts:69-84); `setUserApprovalStatus` (no decider) stamps only `approvalStatus` + `updatedAt` (burner-profile.ts:54-63). Exactly as the doc's audit-vs-queue-drop split claims.
- `approvalStatusEnum("approval_status") = ["pending","approved","rejected"]` (schema.ts:41-45), `.notNull().default("approved")` (schema.ts:267-269). `rankEnum = ["captain","member"]` (schema.ts:31). `users` table 220-303; `approvalDecidedByUserId` self-FK `onDelete: "set null"` (schema.ts:270-273); `approvalDecidedAt` (schema.ts:274). `burner_profiles` 352-364 with `completedAt` nullable (schema.ts:362). `invite_codes` 312-342, `assignedRank` rankEnum nullable (schema.ts:327).
- TS mirrors: `type ApprovalStatus = "pending" | "approved" | "rejected"` (users.ts:33); `TestApprovalStatus` identical (test-store.ts:10). `CampUser` shape (users.ts:39-47); `toCampUser` defaults absent `approvalStatus` to `"approved"` (users.ts:478). `BurnerProfileSummary { responses, completedAt: Date|null, updatedAt: Date|null, version: string|null }` (users.ts:155-160). `TestUser` carries `approvalDecidedByUserId`/`approvalDecidedAt` (test-store.ts:20-21).
- Test seam `POST /api/test/set-approval`: `runtime = "nodejs"` (route.ts:11), 404 unless `isE2ETestMode()` (route.ts:19-21), validates `status ∈ pending|approved|rejected` with 400 on bad body (route.ts:23-33), forces via `testStore.setUserApprovalStatus` (no decider, route.ts:41). E2E-only.
- AcknowledgementGate-is-a-different-surface contrast (doc line 64): `POLL_INTERVAL_MS = 45_000` confirmed (acknowledgement-gate.tsx:26, setInterval :56). The pending-approval page itself has no polling/realtime — confirmed by absence of any interval/poll in page.tsx.
- Backends: `realBackend` (Drizzle via `@camp404/db/burner-profile`) and `testBackend` (in-memory `testStore`) both implement `UserBackend` (users.ts:264-298), selected by `isE2ETestMode()`. `seedBurnerProfileAction`/`satisfyBurnerProfileAction` are no-ops and `getPendingRequiredActions` returns `[]` under test mode (users.ts:192-217), so `completedAt` is the operative onboarding check in tests — as the doc states.

## Low-confidence / could-not-verify

- Cross-unit ordering claims ("Full gating-chain ordering is unit 23", data-model agreement with unit 29) are deferred to those units by design and were not re-derived here; the local order in `home page.tsx:39-63` and `pending-approval/page.tsx:32-44` is internally consistent with the doc.
- The doc's assertion that the real captain UI "isn't drivable under E2E_TEST_MODE" is sourced from the route.ts:5-9 comment and matches the codebase split (captain reads `@camp404/db/roster` live DB); not independently exercised against a running app.
- Whether `revalidatePath("/captains/camp-management")` alone is sufficient for the approving member's `/pending-approval` to auto-clear is a Next.js cache-semantics question — the doc's mechanism ("next page load redirects home via `isApproved`") is correct because `/pending-approval` is `force-dynamic`, but cross-request cache behaviour was not runtime-verified.
