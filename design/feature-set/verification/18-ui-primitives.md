# Verification — 18 ui-primitives

**Verdict:** accurate  ·  checked 62 claims, verified 61.
The doc is a faithful, digit-exact catalog of the `@camp404/ui` primitive layer; every component file, variant, default, token, and consumer claim I checked confirmed against source. The single defect is a cosmetic line-range citation for `DialogContent`/`DialogFooter` (the underlying behaviour is correct). No high- or medium-severity inaccuracies.

## Inaccuracies
| severity | doc claim | code reality | file:line |
|---|---|---|---|
| low | DialogContent close affordance "(dialog.tsx:50-78)" and DialogFooter "(dialog.tsx:94-118)" — the cited 50-78 range overlaps but the close button block is 70-78 and the footer Close button is 112-116. Ranges are loose but encompass the real code. | `DialogContent` is `50-82`; `showCloseButton` default `true` at 53; X-close block at 70-78. `DialogFooter` is `94-119`; `showCloseButton=false` at 96; outline Close button at 112-116. | dialog.tsx:50-82, 94-119 |
| low | Doc lists package `exports` map as having no aggregate index; implies only `components/*` and `lib/utils` (States §, Validation §). | Correct that there is no barrel index. Map also exposes `./styles.css` and `./hooks/*` — the latter points to `./src/hooks/*.ts`, but **no `hooks/` dir exists**. Not a doc claim, but the doc's enumeration of import paths is incomplete (omits `./styles.css`). | packages/ui/package.json (exports), no `src/hooks/` |

## Omissions
| severity | missing behavior/state/enum | file:line |
|---|---|---|
| low | Slider thumb className includes `disabled:pointer-events-none disabled:opacity-50` in addition to the documented `data-[disabled]:opacity-50` on root and `hover:ring-4 focus-visible:ring-4` on thumb. Doc captures the salient ones. | slider.tsx:56 |
| low | `globals.css` defines a `@keyframes cp-layer-in` animation and `@source "../components/**/*.{ts,tsx}"` directive (Tailwind v4 cross-package scan). Doc references globals.css for tokens only (correctly scoped to unit 28), so not in-scope; noted for completeness. | globals.css:7,59-68 |

## Spot-confirmed
- Button: CVA `buttonVariants` at button.tsx:7-35; 6 variants `default/destructive/outline/secondary/ghost/link` (button.tsx:11-20) with exact class strings as quoted; 5 sizes `default(h-10 px-4 py-2)/sm(h-9 rounded-md px-3)/lg(h-11 rounded-md px-8)/icon(h-10 w-10)/icon-lg(h-14 w-14)` (button.tsx:22-28); defaults `variant:default`, `size:default` (button.tsx:31-32); base string includes `[&_svg]:size-4 [&_svg]:shrink-0 [&_svg]:pointer-events-none`, `disabled:pointer-events-none disabled:opacity-50`, `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` (button.tsx:8); `asChild` default false → Slot vs "button" (button.tsx:44-45); forwardRef<HTMLButtonElement> + spread props (button.tsx:43,51). All exact.
- Button consumer reality: `icon-lg` has NO consumer anywhere outside its definition (grep across apps+packages → only button.tsx:27) — doc's DEAD/ORPHANED claim confirmed. `variant="link"` IS used at apps/web/app/signup/required/invite-gate-form.tsx:63 (on an `<a href="/auth/sign-out">` via `asChild`) — confirmed exactly.
- button.stories.tsx UNDER-DOCUMENTED claim: argTypes variant options `["default","outline","ghost","destructive","secondary"]` (omits `link`); size options `["default","sm","lg","icon"]` (omits `icon-lg`); `Sizes` story renders only sm/default/lg — all three confirmed (button.stories.tsx:11,15,31-39).
- Input: `InputProps = React.InputHTMLAttributes<HTMLInputElement>` (input.tsx:4); forwards `type`, spreads props (input.tsx:7,16); class string has `placeholder:text-muted-foreground`, `disabled:cursor-not-allowed disabled:opacity-50`, `file:border-0 file:bg-transparent file:text-sm file:font-medium` (input.tsx:12). Exact.
- Textarea: `TextareaProps = React.TextareaHTMLAttributes<HTMLTextAreaElement>` (textarea.tsx:4); `min-h-[80px]` (textarea.tsx:11). Exact.
- Label: `"use client"` (label.tsx:1); `labelVariants` is a base-only CVA `text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70` (label.tsx:8-10) — doc's "no-op CVA / no variant axes" is correct.
- Checkbox: `"use client"`; lucide `Check` inside `CheckboxPrimitive.Indicator` (checkbox.tsx:5,21-25); `data-[state=checked]:bg-primary data-[state=checked]:text-primary-foreground` + `disabled:cursor-not-allowed disabled:opacity-50` (checkbox.tsx:16). Indicator only shows Check (no dash) — tristate has no distinct visual. Confirmed.
- Select: 10 exports exactly as listed (select.tsx:148-159); `SelectTrigger` ChevronDown + `[&>span]:line-clamp-1` (select.tsx:21,28); `SelectContent` `position="popper"` default, `max-h-96 min-w-[8rem]`, popper translate offsets + viewport sized to `--radix-select-trigger-height/width` (select.tsx:72,77-91); `SelectItem` left-anchored `Check` ItemIndicator + `focus:bg-accent focus:text-accent-foreground` + `data-[disabled]:pointer-events-none data-[disabled]:opacity-50` (select.tsx:120,125-129); scroll buttons render ChevronUp/ChevronDown (select.tsx:46,63). `"use client"` (select.tsx:1). Exact.
- Slider: function component, NOT forwardRef (slider.tsx:8); `min=0 max=100` defaults (slider.tsx:12-13); `_values` memo = `value` ⊳ `defaultValue` ⊳ `[min,max]` (slider.tsx:16-24); renders one Thumb per value via `Array.from({length:_values.length})` (slider.tsx:52-58); vertical orientation classes incl `data-[orientation=vertical]:min-h-44` (slider.tsx:34); `data-slot` on root/track/range/thumb (slider.tsx:28,40,46,54); `data-[disabled]:opacity-50` on root, `hover:ring-4 focus-visible:ring-4` on thumb (slider.tsx:34,56). `"use client"`. All exact, incl the "no array → 2-thumb [min,max]" edge-case logic.
- Card: 6 exports; `Card` `rounded-xl border bg-card text-card-foreground shadow-sm` (card.tsx:11); `CardHeader` `flex flex-col space-y-1.5 p-6` (card.tsx:25); `CardTitle` `<h3>` `text-2xl font-semibold` (card.tsx:35,38); `CardDescription` `<p>` `text-sm text-muted-foreground` (card.tsx:50,52); `CardContent` `p-6 pt-0` (card.tsx:62); `CardFooter` `flex items-center p-6 pt-0` (card.tsx:72). All forwardRef. Exact.
- Dialog: 10 exports as function components (dialog.tsx:147-158); imports `Button` + `XIcon` (dialog.tsx:5,8); `DialogContent` centered `top-[50%] left-[50%]` translate −50%, `max-w-[calc(100%-2rem)] sm:max-w-lg`, over `DialogOverlay` `bg-background/80`, zoom+fade anims (dialog.tsx:42,64); built-in X-close gated by `showCloseButton=true` w/ `XIcon` + sr-only "Close" (dialog.tsx:53,70-77); `DialogFooter` own `showCloseButton=false` → `<Button variant="outline">Close</Button>` in `DialogPrimitive.Close asChild` (dialog.tsx:96,112-116); header `text-center sm:text-left`, footer `flex-col-reverse ... sm:flex-row sm:justify-end` (dialog.tsx:88,106). `"use client"`. Confirmed.
- Popover: `Popover`=Root, `PopoverTrigger`=raw Trigger re-export, `PopoverContent` forwardRef (popover.tsx:8,10,12); defaults `align="center"`, `sideOffset=4`, `w-72`, portalled, `origin-[--radix-popover-content-transform-origin]` (popover.tsx:15,22). `"use client"`. Exact.
- Command: cmdk-based; `Command` `bg-popover` (command.tsx:16); `CommandInput` leading `Search` icon in `border-b` wrapper, `h-11` (command.tsx:28-33); `CommandList` `max-h-[300px]` (command.tsx:49); `CommandEmpty` `py-6 text-center` (command.tsx:62); `CommandGroup` `[cmdk-group-heading]` styling (command.tsx:76); `CommandItem` `data-[selected='true']:bg-accent`, `data-[disabled=true]:opacity-50`, `[&_svg]:size-4` (command.tsx:104); `CommandShortcut` `ml-auto` span (command.tsx:118-120). `CommandShortcut` has NO consumer in apps/web (grep confirms zero outside command.tsx) — doc's orphan note correct. `"use client"`. 8 exports confirmed.
- Combobox: `"use client"`; composes Popover+Command+Button `variant="outline" role="combobox"` (combobox.tsx:61-62); props `options: ReadonlyArray<ComboboxOption>`, `value: string | undefined`, `onChange: (value:string)=>void` (combobox.tsx:23-25); defaults `placeholder="Select…"`(45), `searchPlaceholder="Search…"`(46), `emptyMessage="Nothing found."`(47); local `open` state (combobox.tsx:52); `onSelect` → `onChange(o.value)` then `setOpen(false)` (combobox.tsx:92-95); trigger shows `selected.label` or muted `placeholder` (combobox.tsx:67,72); `selected = value ? options.find(...) : undefined` → unset OR not-found shows placeholder (combobox.tsx:53); trailing `ChevronsUpDown` (combobox.tsx:74); leading `Check` in fixed `inline-flex h-4 w-4` slot via conditional render w/ Tailwind-v4 quirk comment (combobox.tsx:97-103); popover width `w-[var(--radix-popover-trigger-width)]` (combobox.tsx:79); `CommandItem value={o.label}` (filters by label) (combobox.tsx:91); provenance doc-comment "copied from RyRy79261/intake-tracker" + country-picker rationale (combobox.tsx:34-40). `ComboboxOption {value:string; label:string}` (combobox.tsx:17-20). All exact.
- Combobox consumer: questionnaire passes `options={question.options}`, persists chosen `value` string (apps/web/components/questionnaire/question.tsx, `combobox` case ~221-231) — confirmed; the only Combobox importer in apps/web.
- Avatar: `"use client"`; `Avatar` `h-10 w-10` circular overflow-hidden (avatar.tsx:19); `AvatarImage` `aspect-square ... object-cover` (avatar.tsx:33); `AvatarFallback` centered on `bg-[color:var(--color-secondary)] text-[color:var(--color-secondary-foreground)] font-semibold` (avatar.tsx:46); doc-comment "used by the profile page, the profile editor, and the home header" (avatar.tsx:7-11). Consumers verified: apps/web/app/home-header.tsx:50-53, apps/web/app/profile/page.tsx:46-51, and profile editor via apps/web/components/profile/avatar-upload.tsx (used by apps/web/app/profile/edit/edit-form.tsx). AvatarFallback is the ONLY live primitive referencing the secondary palette via `var(--color-*)` (grep: only other `var(--color-` is in card.stories.tsx, a story) — confirmed.
- cn(): `twMerge(clsx(inputs))` (utils.ts:5). utils.test.ts pins: `cn("px-2","px-4")==="px-4"`, `cn("text-red-500","text-blue-500")==="text-blue-500"`, falsy dropped (`cn("base",false&&...,null,undefined,"")==="base"`), nested array/object flatten (`cn(["a","b"],{c:true,d:false})==="a b c"`) (utils.test.ts:9-23). All exact.
- Tokens (globals.css `@theme`): `--color-primary: oklch(0.65 0.27 340)`, `--color-primary-foreground: oklch(0.99 0.005 340)`, `--color-accent: oklch(0.62 0.18 255)`, `--color-accent-foreground: oklch(0.99 0.005 255)`, `--color-secondary: oklch(0.42 0.18 320)`, `--color-secondary-foreground: oklch(0.98 0.01 330)`, `--color-destructive: oklch(0.65 0.22 18)`, `--color-destructive-foreground: oklch(0.98 0 0)`, `--color-background: oklch(0.15 0.05 295)`, `--color-foreground: oklch(0.97 0.02 330)`, `--color-card: oklch(0.26 0.08 295)`, `--color-popover: oklch(0.26 0.08 295)`, `--color-muted: oklch(0.22 0.06 295)`, `--color-muted-foreground: oklch(0.7 0.05 325)`, `--color-input: oklch(0.35 0.1 305)`, `--color-ring: oklch(0.65 0.27 340)`, `--radius: 0.625rem`. Every value digit-for-digit exact (globals.css:9-39).
- lucide-react `^1.16.0` in both packages/ui and apps/web package.json (line 35 each). Embedded icons: `Check` (checkbox/select/combobox), `ChevronDown`/`ChevronUp` (select), `ChevronsUpDown` (combobox), `Search` (command), `XIcon` (dialog) — all import sites confirmed.
- No barrel: package.json `exports` = `./styles.css`, `./components/*`, `./hooks/*`, `./lib/utils` — no aggregate index (confirmed); per-path import model correct.
- Out-of-scope unit-19 files exist in same folder: control-grid.tsx, control-panel.tsx, quadrant-nav.tsx (+ stories) — confirmed present and correctly excluded.
- States §: no `loading`/`isLoading`/`pending`/`spinner` prop anywhere in these files (grep-confirmed); no error variant; no success variant — all consumer-owned. Confirmed.

## Low-confidence / could-not-verify
- "Radix auto-hides AvatarImage on load error and shows fallback" / "Avatar shows fallback while image loading (Radix loading-status)" — upstream `@radix-ui/react-avatar ^1.1.11` internal behaviour; the primitive itself adds no logic, so this is correct by delegation but the load/error semantics live in the package, not Camp 404 source.
- "Slider step ... Radix default 1" — the Camp Slider does not set `step`; the default-1 claim is upstream `@radix-ui/react-slider` behaviour (not pinned in repo code).
- Storybook "under-documents a few real variants" framing depends on these stories being the canonical docs; verified the specific argTypes/Sizes omissions against source (accurate), but the editorial judgement is design-intent, not a code fact.
