# 21 — docs-spec-discipline

**Headline:** Camp 404 has excellent, voluminous docs and *zero* drift control — six verified-stale claims live in the repo right now — so take the donor's cheap honesty mechanics (dated correction blocks, `UNRESOLVED` markers, a doc-status header, a `.gitattributes`, a `.github/` governance set) and leave its Requirement-ID machine behind.

**Camp 404 today:** 13 top-level `docs/*.md`, 31 `design/feature-set/*`, 27 per-surface specs under `design/spec/impl/app/`, and a genuinely good index-as-rulebook at `design/spec/README.md` — but no header convention outside two families (`design/feature-set/00-overview.md:3-6`, `docs/superpowers/specs/2026-05-30-notifications-engine-design.md:3-5`), no single precedence chain, and `grep -rn UNRESOLVED --include=*.md` returns **0**. `.github/` contains only `workflows/` — no ISSUE_TEMPLATE, no PR template, no CODEOWNERS — and the repo root has no `CONTRIBUTING.md`, `SECURITY.md`, `.gitattributes`, `commitlint.config.*` or `.husky/` (all re-verified this pass).

## Take these

| item | verdict | rec | value | effort | donor path | Camp 404 destination | why |
|---|---|---|---|---|---|---|---|
| `.gitattributes` (`*.pen -merge`, `eol=lf`, binary markers) | MISSING | COPY | high | XS | `.gitattributes:1-34` | `/home/ryan/repos/Personal/camp-404/.gitattributes` (new) | `design/app.pen` is line-mergeable by git today; a *successful* merge is the danger |
| Inline dated correction blocks | MISSING | COPY | high | S | `docs/roadmap.md:30-33` | `README.md`, `AGENTS.md`, `docs/configurable-teams-plan.md`, `DEFERRED.md`, `apps/web/tests/e2e/README.md` | direct remedy for six verified-stale claims; keeps the superseded reasoning a rewrite destroys |
| `.prettierignore` fencing extracted sources | MISSING | COPY | high | XS | `.prettierignore:11-19` | `/home/ryan/repos/Personal/camp-404/.prettierignore` | `pnpm format` rewrites `design/.spec-extract/index.md` **today** (verified) |
| `turbo.json` incident comment | MISSING | COPY | high | XS | `turbo.json:25-39` | `turbo.json` above line 31 | the `!.next/dev/**` fix shipped (c270377) with no explanation; a tidy-up deletes it |
| PR template (Database + Risk + collapsed appendix) | MISSING | COPY | high | S | `.github/pull_request_template.md:1-74` | `.github/pull_request_template.md` | agent-authored PRs bury Database/Risk under volume; 19 append-only migrations make it load-bearing |
| 4 typed issue forms + `blank_issues_enabled: false` | MISSING | ADAPT | high | S | `.github/ISSUE_TEMPLATE/{bug,feature,design,copy,config}.yml` | `.github/ISSUE_TEMPLATE/` | public repo storing SA IDs/passports/bank details; the two required privacy confirmations are the point |
| `issue-forms.test.ts` — the doc↔config test | MISSING | COPY | high | S | `packages/core/src/__tests__/issue-forms.test.ts:1-142` | `packages/core/src/__tests__/issue-forms.test.ts` | a doc claim a test can check stops being a doc claim; **blocked** on the forms + taxonomy |
| AGENTS.md `## Verification` section | MISSING | COPY | high | S | `AGENTS.md:216-259` | `AGENTS.md` | the whole current treatment is one bullet (`:180-182`) that is itself false |
| AGENTS.md four operational traps | PARTIAL | ADAPT | high | S | `AGENTS.md:71-90` | `AGENTS.md` (beside `:54-58`) | Camp 404 has the genre with one entry; traps 1/2/4 are domain-free |
| The house rule + `CONTRIBUTING.md` | MISSING | COPY | high | S | `CONTRIBUTING.md:60-71` | new `CONTRIBUTING.md` or `AGENTS.md` | the instinct is already native (`tile-catalogue.ts` ships 8 tiles as `comingSoon`) |
| `SECURITY.md` + repo-settings checklist | MISSING | ADAPT | high | S | `SECURITY.md:1-124` | `/SECURITY.md` | public repo, no private-reporting path; also the only place that can record "branch protection requires exactly `ci-pass`" |
| `UNRESOLVED (flagged <date>)` markers | MISSING | COPY | high | S | `docs/accounts-security-spec.md:76-80` | `AGENTS.md`, `apps/web/tests/e2e/README.md` | zero hits today; one live contradiction (e2e "disabled" vs `ci.yml:179`) needs the maintainer's call, not a guess |
| Doc metadata header (4 rows) + index-as-rulebook + 3-level precedence | PARTIAL | ADAPT | high | S | `docs/README.md:45-58`, `:97-104`, `:145-175` | `docs/README.md` (new), all `docs/*.md` | the convention is half-invented already; `docs/` has no index at all |
| `## Product laws (violating these is a bug, not a style choice)` | PARTIAL | ADAPT | high | S | `AGENTS.md:143-215` | `AGENTS.md` | Camp 404's invariants are scattered across three docs and read as preferences |
| Coverage ratchet with per-file 100% floors | MISSING | ADAPT | high | M | `packages/core/vitest.config.ts:1-128` | `packages/core/vitest.config.ts` + a CI job | nothing exists; `turbo.json:46` declares `coverage/**` outputs against nothing |
| Builder v2 Phase E target set | MISSING | ADAPT | high | M | `docs/questionnaire-spec.md:247-311` | `docs/questionnaire-builder.md` §9 Phase E | the one content item landing on a named unbuilt phase |
| `GITHUB_LABELS` taxonomy + `labels:sync` | PARTIAL | ADAPT | medium | S+S | `packages/core/src/report.ts:162-345`, `report-server/labels-sync.ts:1-100` | `apps/web/lib/github-feedback.ts`, `scripts/` | today the issue vocabulary is "whatever the API invented" (`github-feedback.ts:33-34`) |
| Selector-traps catalogue | PARTIAL | ADAPT | high | S | `e2e/README.md:228-272` | `apps/web/tests/e2e/README.md` | Camp 404 runs Next 16 → the same `role="alert"` route-announcer trap applies |
| Deploy runbook with an effect-when-unset table | MISSING | ADAPT | high | M | `docs/deploy.md:1-234` | new `docs/deploy.md` | four operator actions in `DEFERRED.md:82-85` have invisible status |
| commitlint + husky + CI job | MISSING | ADAPT | medium | **M** | `commitlint.config.mjs:1-53` | root | see Corrections — it is *not* free and *not* immediately green |

**`.gitattributes` (4 lines, the cheapest win in the unit).** Donor `.gitattributes:25` is `*.pen -merge`, with the reasoning at `:1-25`: a line-level merge of a Pencil canvas "produces STRUCTURALLY VALID JSON that is semantically wrong: duplicated node ids, a frame carrying children from two different versions… A file that fails to open is an inconvenience; a file that opens looking almost right is a defect nobody catches." `:27-34` adds `design/brand/** binary`, `* text=auto eol=lf` (matters — the maintainer is on WSL) and png/jpg/ico binary markers. Camp 404 has **no** `.gitattributes` and `design/app.pen` exists; `scripts/pencil/run.sh` already guards the *concurrency* half, nothing guards the merge half.

**Dated correction blocks — the format, and the six targets.** Donor `docs/roadmap.md:30-33` keeps the superseded release sequence intact and puts a blockquote above it: `> _Correction, 27 Jul 2026: R0 grew a third app… The rest of this release sequence still stands._` The point is that a silent rewrite destroys *why* the decision was made. Verified-stale targets, all re-checked this pass: `README.md:108` ("Phase 0 — Setup. Scaffold only; no runtime functionality wired up beyond `/api/health`" + a `Next: Phase 1` line at `:109`); `README.md:89-91` lists 3 crons against 5 in `apps/web/vercel.json:4-8` (`notifications/dispatch` and `notifications/push` missing); `AGENTS.md:22-27` omits `packages/core` and `packages/telegram`, both of which exist; `AGENTS.md:180-182` says Playwright "is disabled pending a preview deployment" while `.github/workflows/ci.yml:179` runs it on every `src` PR; `docs/configurable-teams-plan.md:1-4` is headed "(PARKED) … No feature code is written yet" after Phases 1–3 shipped; `DEFERRED.md:59-60` calls shake-to-report "**not built**" against `packages/core/src/shake.ts`; and `apps/web/tests/e2e/README.md:63` and `:114` both say "13-page wizard" against exactly **11** pages in `apps/web/lib/questionnaire.ts` (`profile_photo, about_you, bio, burn_ideas, team_interests_intro, team_interests, cooking_competency, leadership_logistics, burn_history, burn_intent, dietary`). `docs/first-time-setup.md:86-87` is the counter-example worth preserving: it correctly records that `GOD_EMAILS` still works and deprecation is deferred — exactly the nuance a clean rewrite deletes.

**The doc header + precedence chain, sized for Camp 404.** Take four of the donor's five rows and drop `Requirement IDs` (nothing to cite): `Category` (Product | Architecture | Engineering Spec | Security | Operational | Planning), `Doc status` (Active | Historical | Draft — **Historical must state why in its own cell**, donor exemplar `docs/synthesis.md:6`), `Normative language` (RFC 2119 applies | Descriptive only), `Owner / Updated`. Ship it with `docs/README.md` as an index-as-rulebook, and copy `docs/README.md:97-104` — the rule that the prose must **not** duplicate a field the index already carries, because "a hand-maintained roster… is guaranteed to go stale." That is Camp 404's exact failure mode (`README.md:89-91` is a hand-copy of `vercel.json`'s cron list, and it went stale). State a **three**-level precedence chain — WP issues + `design/spec/` for what to build, `AGENTS.md` for process (wins over `README.md`), `packages/db/src/schema.ts` for the data model — replacing today's four disconnected local statements (`design/spec/README.md:31-34`, `design/feature-set/00-overview.md:178`, `docs/questionnaire-builder.md:17`, `AGENTS.md:66`).

**The Verification section (the single most portable passage).** `AGENTS.md:216-259`. Four dated failures, then five rules: *measure before you claim*; *attribute before you blame* (run it on `main`, in isolation, several times); *a test that cannot fail proves nothing* (break it on purpose and watch it go red); *a test that passes for the wrong reason is worse than no test*; *report what happened — "Verified" gets read literally*. Two named shapes, both invisible in coverage: **asserting an absence against a page that has not rendered** (`toHaveURL` resolves before paint, so `toHaveCount(0)` is satisfied by an empty document — assert something PRESENT first), and **a fixture whose values are outside the domain vocabulary** (`officerKey: "safety"`/`"lnt"` matched no enum member, so deleting the consent filter entirely left all 54 tests green). Camp 404 has 97 test files, 28 `pgEnum`s and 11 Playwright specs — both shapes are live risks, and it can supply its own incident from `DEFERRED.md` rather than borrowing.

**The `.github/` governance set, in dependency order.** `config.yml` (`blank_issues_enabled: false` + a security-advisory contact link → needs `SECURITY.md` first) → `bug.yml` (drop the "Which app?" dropdown at `:15-26`; keep the "stop at the `?`" URL instruction at `:64-68`, the console-paste key warning at `:95-98`, and the two `required: true` confirmations at `:113-116`) → `GITHUB_LABELS` as a plain-object module (drop the 3 `app:` labels; collapse the 10 `area:` values onto roster/questionnaires/notifications/invites/mcp/telegram/push/privacy/ui/db; record the 100-char description cap that caused a real 422) → `labels:sync` → `issue-forms.test.ts`. The test's first assertion is the generalisable one: `files.length > 0`, because "a glob that matches nothing passes every `for` loop under it in silence" — Camp 404 has no test anywhere that asserts a glob matched.

## Already covered in Camp 404

- **`CI pass` aggregate gate** — `ci-pass` at `.github/workflows/ci.yml:216`, `if: always()` at `:217`, and Camp 404's split unconditional/gated result check (`:231-238` / `:246-254`) is *better* than the donor's. The donor's `ci.yml:326` names Camp 404 as the origin. Nothing to import.
- **Flows that name their enforcing code** — `design/spec/flows.md:12-13` ("Live spine = `apps/web/app/page.tsx:29-63` + `apps/web/lib/required-actions.ts`"), `design/spec/information-architecture.md:23`.
- **Adversarial verification culture** — `design/feature-set-verification-report.md:24-26` (2097 claims / 2027 verified / 96.66% / 0 HIGH) over 31 per-unit files.
- **Component library discipline** — `design/spec/component-library.md` folds 57 candidates to 49 canonical with a `mapsTo` legend and a build order.
- **Notifications engine** — `packages/db/src/broadcasts.ts` (`resolveAudience:52`, `dispatchDueBroadcasts:306`) + `docs/superpowers/specs/2026-05-30-notifications-engine-design.md`; deeper than the donor's spec. Only the *laws* are missing.
- **Seeding law satisfied by construction** — no `db:seed` script in `packages/db/package.json`; first boot is `apps/web/lib/bootstrap.ts` + `/setup`. (Write the rule anyway; note `apps/web/app/api/test/seed-invite/route.ts` exists and is E2E-gated, so the rule must be written against what's actually there.)
- **Core purity rule with its reason** — `packages/core/src/index.ts:4-8`.
- **e2e honest-limitations section** — `apps/web/tests/e2e/README.md:100-125`.

## Deliberately skip

- **Requirement-ID protocol + 9-step regeneration** — assumes an external append-only 584-requirement spec with its own change record. Camp 404 has none, and the donor's own rule is "this repo only ever *cites* those IDs, never mints its own". A naive port creates an empty top slot in the precedence chain. The 12 open WP issues (#125–#136) are already stably numbered with a change record.
- **`design/qa/audit.py` harness** — needs a live Pencil JSONRPC bridge (the pencil MCP server failed to connect this session), and the canvas phase is finished (`design/exports/` and `design/pages/` are both empty). Its `FORBIDDEN` regex also encodes AfrikaBurn's never-payments law and would flag Camp 404's wanted dues/Finances copy (WP10 #134). Take only the process rules: a whitelist entry without a reason is invalid; never whitelist to make a number go green; "the finding must be GONE from the output — looks fixed doesn't count."
- **The e2e persona harness** — three-app, eight-persona, built on a "no database back doors" constraint that Camp 404's `E2E_TEST_MODE` in-memory store violates by design. Take the Selector traps, leave the harness.
- **Donor broadcast audience enum + email digest** — `broadcastScopeEnum` (`schema.ts:164-170`) is already the flat single-camp equivalent, and Camp 404 has no mail provider in `turbo.json` `globalEnv` at all.
- **Full triage actor model** — over-scale for one maintainer. Take two paragraphs only: priority is never set by the reporter (a live decision — `github-feedback.ts:19-26` accepts an AI-derived `severity` on exactly the field the donor refused), and the required `Cause:` line as one of exactly three things.
- **Deliberate section-numbering gaps** — correct but thin here; adopt when the situation arises, not as a task.
- **A named "cast" document** — the fence it enforces ("never the seed") is moot with no seed. One line in `AGENTS.md`.
- **Route→design-frame index and measured component usage counts** — the canvas is done; `design/spec/information-architecture.md:46-96` already carries 30 route rows.

## DB changes this unit implies

**None.** This unit owns no tables. The only persistent artefact is the GitHub label taxonomy, which is code, not schema. (If the `SECURITY.md` kill-switch idea is later taken up, it lands in the existing `camp_settings` jsonb `config` column at `packages/db/src/schema.ts:1439-1444` — a physically-enforced singleton, exactly the "a DB row the request path reads on every invocation" shape — with no migration needed.)

## Quick wins (effort S)

1. Create `/home/ryan/repos/Personal/camp-404/.gitattributes` from donor `.gitattributes:1-34` — keep `*.pen -merge` with its reasoning comment and `* text=auto eol=lf`; swap `design/brand/**` for Camp 404's binary paths. Test: `git check-attr merge design/app.pen` prints `merge: unset`.
2. Add `design/.spec-extract/` to `.prettierignore` (donor `.prettierignore:11-19` for the comment shape). Test: `npx prettier --list-different "**/*.md"` no longer lists `design/.spec-extract/index.md` — it does today.
3. Copy donor `turbo.json:25-39` above Camp 404 `turbo.json:31`, keeping the measured numbers (5.7 GB after one dev-mode e2e run; ~550 GB over a dozen cycles) and citing c270377. **Adapt:** drop the `scripts/e2e-local.sh` clause — Camp 404 has no such script, so it has only the copy half of the fix; say so.
4. Copy donor `.github/pull_request_template.md` → `.github/pull_request_template.md`. Renames: drop the `web·org·suppliers` scope comment at `:5-6`/`:24`. Keep Database pre-filled `None.` and the collapsed `<details>Supplementary context</details>`.
5. Copy donor `AGENTS.md:216-259` (Verification) and `:71-90` (four traps) into Camp 404 `AGENTS.md`; put the traps beside the existing Pencil-singleton entry at `:54-58`, and mark trap 3 as already fixed with the `turbo.json` pointer.
6. Add dated correction blockquotes to the six stale claims listed above. Test: re-run the greps — `README.md:108`, `README.md:89-91` vs `vercel.json:4-8`, `AGENTS.md:22-27` vs `ls packages/`, `AGENTS.md:180-182` vs `ci.yml:179`, `docs/configurable-teams-plan.md:1-4`, `DEFERRED.md:59-60` vs `packages/core/src/shake.ts`, `apps/web/tests/e2e/README.md:63,:114` vs the 11 wizard pages.
7. Copy donor `CONTRIBUTING.md:60-71` (the house rule + its four examples) into a new `CONTRIBUTING.md` or `AGENTS.md`. Verbatim-portable; the third example ("a 'saved' toast only after something saved") is a live risk per `DEFERRED.md`.
8. Add a `SECURITY.md` from donor `SECURITY.md:8-28` (private reporting), `:30-72` (never-test-against-live, in/out of scope) and `:95-124` (settings checklist). Record: branch protection must require exactly `ci-pass`; the 7 unexplained GHSAs in `package.json:45-53` need a stated reason each; `apps/web/lib/bootstrap.ts:14` hard-codes `FOUNDER_CODE = "meowzit"` in a public repo.

## Corrections applied

- **commitlint (the one REFUTED row): `rec_holds=False`, effort is M not S.** The claim that Camp 404 "already conforms de facto" is wrong. Re-verified this pass: **18 of the last 40 subjects exceed 72 characters** (14 of them non-merge), so `header-max-length: 72` is a deliberate decision, not a free win. And the proposed scope enum is wrong — actual scopes in the last 60 commits are `web, db, ui, types, e2e, turbo, deps, invite, questionnaire, questionnaire-builder, design, db+web, web+ui`. Worse, `design(questionnaire-builder): …` uses `design` as a **type**, which `@commitlint/config-conventional`'s `type-enum` rejects outright. Adopting this means either widening the enums or rewriting the convention; either way it is not a drop-in.
- `docs/` holds **13** top-level `.md` files, not 15 (repeated twice in the original framing). `docs/superpowers/` holds 7 specs + 1 plan.
- `turbo.json`'s outputs array is line **31**, not `:30` (the original said `:30` twice). Re-verified.
- Camp 404 has **28** `pgEnum`s, not 29; **41** non-story `.tsx` in `packages/ui/src/components/`, not 40; **12** open WP issues (#125–#136), not 13; `packages/core/vitest.config.ts` is **8** lines, not 7.
- The core-purity **rule** is at `packages/core/src/index.ts:4-8`, not `:26-30` (`:26-28` is the "deliberately NOT here" exclusion list).
- Donor `e2e/README.md`'s Selector traps catalogue is at **`:228-272`**, not `:136-141`.
- Donor `design/spec/README.md`'s "boards win, divergence is flagged" lands on line **34** (paragraph `:31-34`).
- Donor CODEOWNERS is **40** lines and its honest caveat is at `.github/CODEOWNERS:8-9`, **not** `CONTRIBUTING.md:279-289` (that is the Review section).
- Donor `docs/technical-spec.md`'s per-component DoD is at `docs/component-spec.md:66-70`, not `:62-66` — and it names a "Storybook-less visual check", the opposite of Camp 404's Storybook-per-component setup, so it needs adapting.
- `design/spec/impl/build-coverage-audit.md` is **43 lines** — a verdict summary, not a gap table; only `coverage-check.md` (347 lines) is one.
- The `control-panel.tsx` / `control-grid.tsx` / `quadrant-nav.tsx` divergence is **eight** `design/feature-set/*.md` files with dangling citations, not two.
- `packages/core/src/report-server/labels-sync.ts` is **not** dependency-free — it imports `GITHUB_LABELS` from `../report`, a zod module. Extracting the taxonomy to a plain-object module is a **hard prerequisite**, not an ordering preference.
- The coverage-floor candidate list should include `shake.ts` and `text-utils.ts` alongside the six named — all nine files in `packages/core/src/` are equally pure.
- The `.prettierignore` claim was upgraded from low to high value (it is the enforcement half of the never-edit-sources rule) — but **narrowed** by my own spot-check: only `design/.spec-extract/index.md` is currently prettier-dirty; the 59 `boards-json/*.json` files happen to be clean today. The exposure is real but one file wide right now.

## Confidence notes

- Everything I spot-checked this pass held: no `.gitattributes`; `pnpm format` really does rewrite `design/.spec-extract/index.md`; all six stale doc claims are digit-exact; `turbo.json:31`; 11 wizard pages vs the "13-page" claim; no `coverage-v8` or `thresholds` anywhere; `UNRESOLVED` = 0 hits; 7 unexplained GHSAs; `FOUNDER_CODE = "meowzit"` at `bootstrap.ts:14`.
- **Unverified, treat as lower confidence:** the `DEFERRED.md` stage-2→3 `startTransition` incident cited as Camp 404's own verification anecdote; donor `AGENTS.md:115-118` and `docs/build-spec.md:424-425` as further correction-block exemplars; donor `docs/roadmap.md:88-90`'s candidate-graduation test.
- **Two donor-wide cautions.** (1) The donor's own statistics disagree with each other across its docs (45 vs 44 tables; three different e2e counts; 51 vs 49 UI components) — treat every donor number as approximate, and note this is the exact drift class its metadata machinery does *not* catch. (2) Branch protection is **not** enabled on the donor either (`AGENTS.md:342-345`), so its CODEOWNERS "does nothing at all" and `CI pass` is not actually required. Copying the machinery without flipping the switch reproduces the gap.
- **Maintainer preference to respect:** the donor's `AGENTS.md:263-268` says "No skills, and no new tooling layers, without asking… Suggest, don't add." Every tooling item here (commitlint, husky, `@vitest/coverage-v8`, `labels:sync`, `audit.py`) should be proposed before it is applied. The doc-only items — corrections, headers, `.gitattributes`, `.prettierignore`, `SECURITY.md`, `CONTRIBUTING.md` — carry no such cost.
