// @camp404/games/inkblot/art: the cat's frames and the palette, as plain data
// with no React, for pictures drawn elsewhere (Join's link preview).

export { CAT_FRAMES, CAT_H, CAT_W } from "./cat";
export { COLOURS, type Sprite } from "./sprites";
