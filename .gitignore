# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
# Written by next dev, next build and next typegen, each pointing at its own
# types folder, so a committed copy flips on every run (Next's docs say ignore it).
next-env.d.ts
out/
.turbo/
dist/
build/

# storybook
storybook-static/

# capacitor
apps/mobile/ios/App/App/public/
apps/mobile/android/app/src/main/assets/public/
apps/mobile/ios/Pods/
apps/mobile/ios/build/
apps/mobile/android/.gradle/
apps/mobile/android/build/
apps/mobile/android/app/build/

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editors / OS
.DS_Store
.vscode/
.idea/
*.swp

# test artefacts
coverage/
playwright-report/
playwright-report-db/
test-results/
# e2e-db auth mail capture (AUTH_EMAIL_CAPTURE_FILE)
.e2e-mail/

# TypeScript incremental build info
*.tsbuildinfo

# Generated by `next dev` from Next 16.3 (see node_modules/next/dist/server/
# lib/generate-agent-files.js). apps/web/AGENTS.md would SHADOW the repo-root
# AGENTS.md for anything working inside apps/web, and CLAUDE.md is a one-line
# pointer at it — neither is ours to keep, and both reappear on every dev run.
apps/web/AGENTS.md
apps/web/CLAUDE.md
apps/join/AGENTS.md
apps/join/CLAUDE.md
