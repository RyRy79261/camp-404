// Where the join site sends people. The console is still at the apex
// (camp-404.com); when it moves to app.camp-404.com, change APP_URL here.
export const APP_URL = "https://camp-404.com";

/** The console's own sign-up: open to anyone, the invite check comes after. */
export const SIGN_UP_URL = `${APP_URL}/auth/sign-up`;
export const SIGN_IN_URL = `${APP_URL}/auth/sign-in`;
export const PRIVACY_URL = `${APP_URL}/privacy`;
export const TERMS_URL = `${APP_URL}/terms`;

/** This site's own address, for page metadata. */
export const JOIN_URL = "https://join.camp-404.com";
